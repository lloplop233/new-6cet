# P1 领域模型实施计划

> **供 Agent 执行：** REQUIRED SUB-SKILL：使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务逐项实施。所有步骤使用复选框跟踪。

**目标：** 建立项目自有、JSON 可序列化、与第三方 FSRS 解耦的 P1-1 领域类型及契约测试。

**架构：** 类型按词库、复习、会话、设置和备份五个职责文件拆分，依赖方向固定为 `word -> review -> study -> settings -> backup`。测试集中在 `src/types/domain.test.ts`，每个任务先增加失败的编译期契约，再实现使其通过；P1-2 才补充 FSRS 算法内部状态。

**技术栈：** TypeScript 6、Node.js 22 内置测试运行器、Vue 3 项目的 `vue-tsc`、ESLint、pnpm。

**规格：** `P1领域模型设计.md`

## 全局约束

- 只实现 `项目实施计划.md` 的 P1-1，不选择或安装 FSRS 库。
- 手写领域类型必须使用 `.ts`；不得修改 `src/types/` 下 5 个自动生成的 `.d.ts`。
- 不修改 `pages/`、`components/`、`stores/`、`services/`、`composables/`、设计 token 或旧项目。
- `WordId` 是经过 Unicode `NFKC`、首尾去空白和小写转换后的单词字符串；规范化函数在 P6 实现。
- 所有可持久化时间均为 UTC Unix epoch milliseconds，类型名为 `Timestamp`，不得使用 `Date`。
- 项目领域类型不得 import 第三方 FSRS 类型。
- 词库本体不得进入 `BackupEnvelope`。
- 每个任务严格执行 RED → GREEN → 验证 → commit，不把后续任务内容提前实现。
- Git 作者身份使用 `lloplop233 <239779306+lloplop233@users.noreply.github.com>`，通过单次 `git -c` 参数传入，不修改 Git 配置。
- 不执行 `push`；远端写入需要用户另行授权。

## 实施后审查修订

2026-08-31 的只读代码审查发现原计划未覆盖严格空值语义，并把两个业务稀疏映射写成了总映射。最终实现以 `P1领域模型设计.md` 和 `c7f454c` 为准：

- `tsconfig.json` 开启 `strictNullChecks`；
- `StudySession.results` 使用 `Readonly<Partial<Record<WordId, SessionResult>>>`；
- `BackupData.reviews` 使用 `Readonly<Partial<Record<WordId, ReviewState>>>`；
- 契约测试补充空值、不可变性、缺失键和备份不含词库本体的负面用例。

下方任务卡保留初始执行计划，用于追溯原始 RED → GREEN 顺序；审查修订不回写伪装成最初设计。

## 文件结构

| 文件 | 职责 |
|---|---|
| `src/types/word.ts` | 不可变词库实体：`WordId`、`WordSense`、`WordExample`、`Word` |
| `src/types/review.ts` | 时间、三档评分、稳定业务复习状态 |
| `src/types/study.ts` | 双模式学习会话、会话结果和恢复结构 |
| `src/types/settings.ts` | 已确认的用户设置结构 |
| `src/types/backup.ts` | 不含词库本体的 JSON 备份信封 |
| `src/types/domain.test.ts` | 编译期类型契约和 JSON 往返运行时测试 |
| `P1领域模型设计.md` | 实现完成后同步书面状态 |
| `项目实施计划.md` | 验证通过后将 P1-1 标记为 `[x]` |
| `交接文档.md` | 同步当前阶段、文件结构和下一任务 |
| `P1交接文档.md` | 记录 P1-1 修改、验证、决策和剩余风险 |

---

### Task 1：定义不可变词库实体

**文件：**

- Create：`src/types/domain.test.ts`
- Create：`src/types/word.ts`

**接口：**

- Consumes：无项目内接口。
- Produces：`WordId`、`WordSense`、`WordExample`、`Word`。

- [ ] **Step 1：先写失败的 `Word` 契约测试**

创建 `src/types/domain.test.ts`：

```ts
import assert from 'node:assert/strict'
import { describe, it } from 'node:test'

import type { Word } from './word'

export const WORD_FIXTURE = {
  id: 'dignity',
  text: 'dignity',
  rank: 12,
  phonetic: "'dɪɡnəti",
  senses: [
    { partOfSpeech: 'n', definition: '尊严；庄重' },
  ],
  examples: [
    { english: 'They have to be treated with dignity.', chinese: '必须有尊严地对待他们。' },
  ],
  realExamYears: ['2013.6'],
} satisfies Word

// @ts-expect-error Word 必须包含完整的词库字段
const INCOMPLETE_WORD: Word = { id: 'dignity' }
void INCOMPLETE_WORD

describe('domain types', () => {
  it('keeps a word fixture JSON-serializable', () => {
    assert.deepEqual(JSON.parse(JSON.stringify(WORD_FIXTURE)), WORD_FIXTURE)
  })
})
```

- [ ] **Step 2：运行 RED，确认因目标模块缺失而失败**

Run：

```powershell
pnpm typecheck
```

Expected：FAIL，包含 `Cannot find module './word'` 或等价的 `TS2307`，失败原因必须是 `word.ts` 尚不存在。

- [ ] **Step 3：实现最小 `Word` 类型**

创建 `src/types/word.ts`：

```ts
export type WordId = string

export interface WordSense {
  readonly partOfSpeech: string | null
  readonly definition: string
}

export interface WordExample {
  readonly english: string
  readonly chinese: string | null
}

export interface Word {
  readonly id: WordId
  readonly text: string
  readonly rank: number
  readonly phonetic: string | null
  readonly senses: readonly WordSense[]
  readonly examples: readonly WordExample[]
  readonly realExamYears: readonly string[]
}
```

- [ ] **Step 4：运行 GREEN 验证**

Run：

```powershell
pnpm typecheck
node --test src/types/domain.test.ts
pnpm exec eslint src/types/word.ts src/types/domain.test.ts
```

Expected：三条命令全部退出码 `0`；Node 测试为 1/1 通过。

- [ ] **Step 5：提交词库类型**

```powershell
git add -- src/types/word.ts src/types/domain.test.ts
git -c user.name="lloplop233" -c user.email="239779306+lloplop233@users.noreply.github.com" commit -m "feat(types): define word domain types"
```

---

### Task 2：定义评分与稳定复习状态

**文件：**

- Modify：`src/types/domain.test.ts`
- Create：`src/types/review.ts`

**接口：**

- Consumes：`WordId` from `src/types/word.ts`。
- Produces：`Timestamp`、`Rating`、`ReviewPhase`、`ReviewState`。

- [ ] **Step 1：扩展测试 import 和类型契约**

在 `src/types/domain.test.ts` 的 `Word` import 后加入：

```ts
import type { Rating, ReviewState, Timestamp } from './review'
```

在 `WORD_FIXTURE` 后、`describe` 前加入：

```ts
export const NOW: Timestamp = 1788105600000

export const REVIEW_FIXTURE = {
  wordId: WORD_FIXTURE.id,
  phase: 'review',
  dueAt: NOW,
  lastReviewedAt: NOW - 86_400_000,
  reviewCount: 3,
  lapseCount: 1,
} satisfies ReviewState

const VALID_RATING: Rating = 'good'
void VALID_RATING

// @ts-expect-error Rating 只允许 again、hard、good
const INVALID_RATING: Rating = 'easy'
void INVALID_RATING

// @ts-expect-error Timestamp 必须是 number，不能保存 Date
const INVALID_TIMESTAMP: Timestamp = new Date()
void INVALID_TIMESTAMP
```

在现有 `describe` 内加入：

```ts
  it('keeps review timestamps as JSON numbers', () => {
    assert.equal(typeof REVIEW_FIXTURE.dueAt, 'number')
    assert.equal(typeof REVIEW_FIXTURE.lastReviewedAt, 'number')
  })
```

- [ ] **Step 2：运行 RED**

Run：

```powershell
pnpm typecheck
```

Expected：FAIL，包含 `Cannot find module './review'` 或等价的 `TS2307`。

- [ ] **Step 3：实现最小复习类型**

创建 `src/types/review.ts`：

```ts
import type { WordId } from './word'

export type Timestamp = number

export type Rating = 'again' | 'hard' | 'good'

export type ReviewPhase = 'new' | 'learning' | 'review' | 'relearning'

export interface ReviewState {
  wordId: WordId
  phase: ReviewPhase
  dueAt: Timestamp
  lastReviewedAt: Timestamp | null
  reviewCount: number
  lapseCount: number
}
```

- [ ] **Step 4：运行 GREEN 验证**

```powershell
pnpm typecheck
node --test src/types/domain.test.ts
pnpm exec eslint src/types/review.ts src/types/domain.test.ts
```

Expected：全部退出码 `0`；Node 测试为 2/2 通过。

- [ ] **Step 5：提交复习类型**

```powershell
git add -- src/types/review.ts src/types/domain.test.ts
git -c user.name="lloplop233" -c user.email="239779306+lloplop233@users.noreply.github.com" commit -m "feat(types): define review domain state"
```

---

### Task 3：定义双模式学习会话

**文件：**

- Modify：`src/types/domain.test.ts`
- Create：`src/types/study.ts`

**接口：**

- Consumes：`WordId`、`Rating`、`Timestamp`。
- Produces：`StudyMode`、`SessionStatus`、`SessionResult`、`StudySession`。

- [ ] **Step 1：扩展会话契约测试**

在 `src/types/domain.test.ts` 的 type imports 中加入：

```ts
import type { StudyMode, StudySession } from './study'
```

在 `REVIEW_FIXTURE` 后、`describe` 前加入：

```ts
export const SESSION_FIXTURE = {
  id: 'session-2026-08-31-001',
  mode: 'flashcard',
  queue: [WORD_FIXTURE.id],
  currentIndex: 0,
  results: {
    [WORD_FIXTURE.id]: {
      wordId: WORD_FIXTURE.id,
      rating: 'good',
      ratedAt: NOW,
    },
  },
  status: 'completed',
  startedAt: NOW - 60_000,
  updatedAt: NOW,
  completedAt: NOW,
} satisfies StudySession

const VALID_MODE: StudyMode = 'multiple-choice'
void VALID_MODE

// @ts-expect-error StudyMode 不允许未定义的模式
const INVALID_MODE: StudyMode = 'spelling'
void INVALID_MODE
```

在 `describe` 内加入：

```ts
  it('keys session results by stable WordId', () => {
    assert.equal(SESSION_FIXTURE.results.dignity.wordId, WORD_FIXTURE.id)
    assert.equal(SESSION_FIXTURE.queue[0], WORD_FIXTURE.id)
  })
```

- [ ] **Step 2：运行 RED**

```powershell
pnpm typecheck
```

Expected：FAIL，包含 `Cannot find module './study'` 或等价的 `TS2307`。

- [ ] **Step 3：实现最小会话类型**

创建 `src/types/study.ts`：

```ts
import type { Rating, Timestamp } from './review'
import type { WordId } from './word'

export type StudyMode = 'flashcard' | 'multiple-choice'

export type SessionStatus = 'active' | 'completed'

export interface SessionResult {
  wordId: WordId
  rating: Rating
  ratedAt: Timestamp
}

export interface StudySession {
  id: string
  mode: StudyMode
  queue: readonly WordId[]
  currentIndex: number
  results: Readonly<Record<WordId, SessionResult>>
  status: SessionStatus
  startedAt: Timestamp
  updatedAt: Timestamp
  completedAt: Timestamp | null
}
```

- [ ] **Step 4：运行 GREEN 验证**

```powershell
pnpm typecheck
node --test src/types/domain.test.ts
pnpm exec eslint src/types/study.ts src/types/domain.test.ts
```

Expected：全部退出码 `0`；Node 测试为 3/3 通过。

- [ ] **Step 5：提交会话类型**

```powershell
git add -- src/types/study.ts src/types/domain.test.ts
git -c user.name="lloplop233" -c user.email="239779306+lloplop233@users.noreply.github.com" commit -m "feat(types): define study session types"
```

---

### Task 4：定义用户设置

**文件：**

- Modify：`src/types/domain.test.ts`
- Create：`src/types/settings.ts`

**接口：**

- Consumes：`Timestamp`、`StudyMode`。
- Produces：`Settings`。

- [ ] **Step 1：扩展设置契约测试**

在 type imports 中加入：

```ts
import type { Settings } from './settings'
```

在 `SESSION_FIXTURE` 后、`describe` 前加入：

```ts
export const SETTINGS_FIXTURE = {
  targetWordCount: 2500,
  preferredMode: 'flashcard',
  autoPronounce: false,
  updatedAt: NOW,
} satisfies Settings

// @ts-expect-error Settings 必须显式包含 autoPronounce
const INCOMPLETE_SETTINGS: Settings = {
  targetWordCount: 2500,
  preferredMode: 'flashcard',
  updatedAt: NOW,
}
void INCOMPLETE_SETTINGS
```

在 `describe` 内加入：

```ts
  it('keeps confirmed settings explicit', () => {
    assert.equal(SETTINGS_FIXTURE.targetWordCount, 2500)
    assert.equal(SETTINGS_FIXTURE.autoPronounce, false)
  })
```

- [ ] **Step 2：运行 RED**

```powershell
pnpm typecheck
```

Expected：FAIL，包含 `Cannot find module './settings'` 或等价的 `TS2307`。

- [ ] **Step 3：实现最小设置类型**

创建 `src/types/settings.ts`：

```ts
import type { Timestamp } from './review'
import type { StudyMode } from './study'

export interface Settings {
  targetWordCount: number
  preferredMode: StudyMode
  autoPronounce: boolean
  updatedAt: Timestamp
}
```

- [ ] **Step 4：运行 GREEN 验证**

```powershell
pnpm typecheck
node --test src/types/domain.test.ts
pnpm exec eslint src/types/settings.ts src/types/domain.test.ts
```

Expected：全部退出码 `0`；Node 测试为 4/4 通过。

- [ ] **Step 5：提交设置类型**

```powershell
git add -- src/types/settings.ts src/types/domain.test.ts
git -c user.name="lloplop233" -c user.email="239779306+lloplop233@users.noreply.github.com" commit -m "feat(types): define study settings"
```

---

### Task 5：定义备份信封与 JSON 往返契约

**文件：**

- Modify：`src/types/domain.test.ts`
- Create：`src/types/backup.ts`

**接口：**

- Consumes：`WordId`、`ReviewState`、`Timestamp`、`StudySession`、`Settings`。
- Produces：`BackupData`、`BackupEnvelope`。

- [ ] **Step 1：扩展备份契约测试**

在 type imports 中加入：

```ts
import type { BackupEnvelope } from './backup'
```

在 `SETTINGS_FIXTURE` 后、`describe` 前加入：

```ts
export const BACKUP_FIXTURE = {
  schemaVersion: 1,
  appVersion: '0.1.0',
  exportedAt: NOW,
  data: {
    settings: SETTINGS_FIXTURE,
    reviews: {
      [WORD_FIXTURE.id]: REVIEW_FIXTURE,
    },
    activeSession: SESSION_FIXTURE,
  },
} satisfies BackupEnvelope

// @ts-expect-error BackupEnvelope 必须包含 appVersion
const INCOMPLETE_BACKUP: BackupEnvelope = {
  schemaVersion: 1,
  exportedAt: NOW,
  data: BACKUP_FIXTURE.data,
}
void INCOMPLETE_BACKUP
```

在 `describe` 内加入：

```ts
  it('round-trips a complete backup through JSON', () => {
    const restored = JSON.parse(JSON.stringify(BACKUP_FIXTURE))

    assert.deepEqual(restored, BACKUP_FIXTURE)
    assert.equal('words' in restored.data, false)
  })
```

- [ ] **Step 2：运行 RED**

```powershell
pnpm typecheck
```

Expected：FAIL，包含 `Cannot find module './backup'` 或等价的 `TS2307`。

- [ ] **Step 3：实现最小备份类型**

创建 `src/types/backup.ts`：

```ts
import type { ReviewState, Timestamp } from './review'
import type { Settings } from './settings'
import type { StudySession } from './study'
import type { WordId } from './word'

export interface BackupData {
  settings: Settings
  reviews: Readonly<Record<WordId, ReviewState>>
  activeSession: StudySession | null
}

export interface BackupEnvelope {
  schemaVersion: number
  appVersion: string
  exportedAt: Timestamp
  data: BackupData
}
```

- [ ] **Step 4：运行 GREEN 验证**

```powershell
pnpm typecheck
node --test src/types/domain.test.ts
pnpm exec eslint src/types/backup.ts src/types/domain.test.ts
```

Expected：全部退出码 `0`；Node 测试为 5/5 通过。

- [ ] **Step 5：提交备份类型**

```powershell
git add -- src/types/backup.ts src/types/domain.test.ts
git -c user.name="lloplop233" -c user.email="239779306+lloplop233@users.noreply.github.com" commit -m "feat(types): define backup envelope"
```

---

### Task 6：完成 P1-1 文档同步和全量验收

**文件：**

- Modify：`P1领域模型设计.md`
- Modify：`项目实施计划.md`
- Modify：`交接文档.md`
- Create：`P1交接文档.md`

**接口：**

- Consumes：Tasks 1～5 的全部领域类型和验证结果。
- Produces：P1-1 完成状态、可供 P1-2 直接接手的事实记录。

- [ ] **Step 1：更新设计与任务状态**

在 `P1领域模型设计.md` 顶部将状态改为：

```markdown
> 状态：P1-1 已实现并验证  
```

在 `项目实施计划.md` 的 P1-1 中将：

```markdown
- 状态：`[ ]`
```

改为：

```markdown
- 状态：`[x]`
```

并在“当前推荐起点”中把下一任务改为 P1-2，保持 P1-2～P1-4 的原顺序。

- [ ] **Step 2：更新总交接文档**

在 `交接文档.md` 的“已完成”中增加：

```markdown
- P1-1 领域类型已定义并通过契约测试：词库、复习、会话、设置和备份类型均使用项目自有接口
```

在 `src/` 文件结构中记录 5 个领域类型文件和 `domain.test.ts`，并将下一步改为 P1-2 FSRS 实现策略。

- [ ] **Step 3：创建 P1 交接记录**

创建 `P1交接文档.md`，内容为：

```markdown
# P1 领域模型与存储协议 — 模型交接文档

> 最后更新：2026-08-31
> 当前覆盖任务：P1-1

## 已完成

- 定义 `WordId`、`WordSense`、`WordExample` 和不可变 `Word`。
- 定义 `Timestamp`、三档 `Rating`、`ReviewPhase` 和稳定业务 `ReviewState`。
- 定义双模式 `StudySession`，评分结果以 `WordId` 为键，避免一次会话重复计分。
- 定义 `Settings` 和不含词库本体的 `BackupEnvelope`。
- 建立编译期类型契约与 JSON 往返测试。

## 修改文件

- `src/types/word.ts`：词库实体。
- `src/types/review.ts`：时间、评分和复习状态。
- `src/types/study.ts`：学习模式和会话状态。
- `src/types/settings.ts`：用户设置。
- `src/types/backup.ts`：备份信封。
- `src/types/domain.test.ts`：领域契约测试。
- `P1领域模型设计.md`：P1-1 设计依据。

## 验证

- `pnpm typecheck`：通过。
- `pnpm lint`：通过。
- `pnpm test`：通过。
- `pnpm build:pro`：通过。
- `pnpm check`：通过。
- 工程规范扫描：通过。

## 关键决策

- 同一规范化拼写只对应一个 `Word` 和一份复习进度，`WordId` 使用规范化小写单词。
- 所有跨模块时间使用 UTC Unix epoch milliseconds，不保存 `Date`。
- 项目领域类型不依赖第三方 FSRS 类型。
- 词库本体不进入备份，备份只引用稳定 `WordId`。

## 未完成或风险

- P1-2 尚未选择 FSRS 实现，也未定义算法内部持久化状态。
- P1-3 尚未冻结存储键、`schemaVersion`、迁移和损坏恢复。
- P1-4 尚未冻结 Store、Service、Composable 和工具函数接口。
- 类型不能单独强制正整数、队列去重和时间先后关系，运行时校验在后续任务实现。

## 下一任务与风险

- 下一任务：P1-2 选择 FSRS 实现策略。
- 风险等级：L3。
- 最低能力需求：能够可靠处理 FSRS 技术选型、算法边界、时间转换和持久化兼容性；不指定具体模型。
- 不要在 P1-2 提前实现 Store、页面或真实词库转换。
```

- [ ] **Step 4：运行完整质量门禁**

```powershell
pnpm check
```

Expected：`typecheck`、Lint、全部 Node 测试和生产构建均退出码 `0`；允许保留已记录的 Vite `NODE_ENV` 和 PostCSS `from` 非阻断警告。

- [ ] **Step 5：运行工程规范扫描**

```powershell
$issues = @()
$vanVars = rg -n --glob '*.vue' --glob '*.ts' -- '--van-' src
if ($LASTEXITCODE -eq 0) { $issues += $vanVars }
$vanClasses = rg -n '\.van-' src
if ($LASTEXITCODE -eq 0) { $issues += $vanClasses }
$hex = rg -ni --glob '!src/styles/**' --glob '!src/assets/**/*.svg' --glob '!*.svg' '#[0-9a-f]{3,8}\b' src
if ($LASTEXITCODE -eq 0) { $issues += $hex }
$literalUnits = rg -n --glob '*.vue' --glob '*.ts' '(?:\b\d+(?:\.\d+)?(?:px|rem|em|vw|vh)\b)' src/components src/pages
if ($LASTEXITCODE -eq 0) { $issues += $literalUnits }
if (Test-Path -LiteralPath 'src/api') { $issues += '存在 src/api' }
if ($issues.Count -gt 0) { $issues; exit 1 }
```

Expected：退出码 `0`，无输出。

- [ ] **Step 6：检查手术式改动范围**

```powershell
git status --short
git diff --stat
git diff --name-only
```

Expected：只出现本任务列出的领域类型、测试和文档文件；5 个自动生成的 `.d.ts` 不得出现。

- [ ] **Step 7：提交 P1-1 文档与最终状态**

```powershell
git add -- 'P1领域模型设计.md' '项目实施计划.md' '交接文档.md' 'P1交接文档.md'
git -c user.name="lloplop233" -c user.email="239779306+lloplop233@users.noreply.github.com" commit -m "docs: complete P1-1 handoff"
```

- [ ] **Step 8：最终核验**

```powershell
git status --short --branch
git log --oneline -8
```

Expected：工作区干净；最新提交为 `docs: complete P1-1 handoff`；P1-1 的五个实现提交和设计/计划提交均可追溯。
