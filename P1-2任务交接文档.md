# P1-2 FSRS 实现策略 — 新对话任务交接

> **历史快照，禁止作为当前启动指令。** 本文记录 P1-2 开始前的仓库状态，其中 branch、commit、worktree 和远端差异已经过时；当前事实只看 `交接文档.md` 和 Git。正文保留用于追溯当时的任务边界。
>
> 最后更新：2026-08-31
> 目标任务：执行 `项目实施计划.md` 的 P1-2
> 项目位置：`D:\工作\ddff`
> 当前分支：`main`
> 当前提交：`c1d17af3f42ca1dbfff08156d82607f0ffa28d98`

## 一、仓库当前事实

- P0-1～P0-4 已完成。
- P1-1 已完成、经过两轮只读代码审查，并已 fast-forward 合并到本地 `main`。
- 当前只有主工作树 `D:\工作\ddff`，没有遗留 feature worktree 或 feature 分支。
- 当前工作区在创建本文档前是干净的。
- 本地 `main` 比 `origin/main` 领先 11 个 commit；P1-1 尚未 `push` 到 GitHub。
- GitHub 远端：`https://github.com/lloplop233/new-6cet.git`。
- 不要把聊天记录当作任务状态；以 `项目实施计划.md`、`P1交接文档.md` 和 Git 状态为准。

## 二、开始前必须完整阅读

按以下顺序读取：

1. `AGENTS.md`
2. `交接文档.md`
3. `模型与Agent协作指南.md`
4. `项目实施计划.md`
5. `P1交接文档.md`
6. `P1领域模型设计.md`
7. `P1领域模型实施计划.md`

P1-1 已冻结的源码必须同时阅读：

- `src/types/word.ts`
- `src/types/review.ts`
- `src/types/study.ts`
- `src/types/settings.ts`
- `src/types/backup.ts`
- `src/types/domain.test.ts`
- `tsconfig.json`

## 三、P1-1 已冻结的前置契约

- `WordId` 是规范化小写单词字符串；同一规范化拼写只对应一个学习单元和一份进度。
- `Rating` 只有 `again | hard | good`，产品没有 `easy` 按钮。
- 所有跨模块时间使用 UTC Unix epoch milliseconds，领域对象不保存 `Date`。
- `ReviewState` 是项目稳定业务状态，不得直接替换成第三方 FSRS 类型。
- 项目已开启 `strictNullChecks`。
- `StudySession.results` 和 `BackupData.reviews` 是稀疏映射，读取缺失键时必须处理 `undefined`。
- 词库本体不进入备份，备份只引用稳定 `WordId`。
- 项目领域类型不得泄漏第三方 FSRS 类型。

## 四、P1-2 的准确目标

`项目实施计划.md` 对 P1-2 的定义：

- 优先选择成熟库，不手写 FSRS 核心算法。
- 冻结三档产品评分到 FSRS 等级的映射。
- 冻结领域时间戳与库时间类型之间的转换方式。
- 冻结初始化参数、默认参数和序列化字段。
- 形成一份短决策记录和最小验证用例。

P1-2 完成后，后续代码不应再需要重新讨论“使用哪个 FSRS 实现、如何映射三档评分、哪些算法字段要持久化”。

## 五、必须研究并明确回答的问题

开始实现前，应通过当前官方文档、官方仓库、npm 包元数据或原始论文核对，不凭模型记忆选择库：

1. 当前适合浏览器端 TypeScript 项目的成熟 FSRS 实现有哪些？
2. 候选库是否支持当前 FSRS 版本、ESM、浏览器构建和 TypeScript 类型？
3. 包体积、维护活跃度、许可证、序列化方式和 API 稳定性是否适合无后端 H5？
4. 产品三档评分 `again | hard | good` 分别映射到什么库枚举？缺少 `easy` 时是否仍符合算法预期？
5. 库内部若使用 `Date`，项目边界如何与 `Timestamp` 双向转换？时区和毫秒精度如何处理？
6. 新词初始化、复习状态转换、重新学习和到期时间如何表达？
7. 哪些算法字段必须持久化，哪些可以运行时计算？
8. 默认期望记忆率、参数权重、最大间隔等是否沿用库默认值？如果调整，证据是什么？
9. 升级库版本后，已持久化算法状态是否仍可读取？需要为 P1-3 留下哪些版本字段？
10. 如何用确定性最小测试证明三档映射、时间转换和 JSON 往返成立？

## 六、推荐执行顺序

1. 建立事实：检查仓库状态、依赖和现有领域类型。
2. 本任务风险为 L3；使用具备复杂技术选型、算法边界和跨模块兼容性推理能力的模型，并以实时官方资料研究候选 FSRS 实现。
3. 先写 P1-2 决策草案，列出候选、证据、取舍和推荐方案。
4. 明确三档评分映射、时间边界、初始化策略和持久化字段。
5. 经设计确认后，再创建隔离工作树和实现计划；建议分支名 `codex/p1-fsrs-strategy`。
6. 按 TDD 先写失败的最小验证，再添加依赖或适配类型/纯函数。
7. 运行类型检查、Lint、全部 Node 测试、生产构建和工程规范扫描。
8. 更新 `项目实施计划.md`、`交接文档.md` 和 `P1交接文档.md`，记录修改文件、验证结果、关键决策和剩余风险。
9. 完成只读代码审查后，再由用户选择本地合并、PR 或保留分支。

## 七、P1-2 范围外内容

本任务不要提前实现：

- P1-3 的 `localStorage` 键、迁移链、损坏恢复和写入失败处理。
- P1-4 的 Store、Service、Composable 和工具函数总接口。
- P2 的学习页面、状态机、会话恢复和模拟数据闭环。
- P3～P8 的正式 UI、真实词库、备份界面、TTS、字体、PWA 和部署。
- 任何页面、Vant 主题或设计 token 改动。
- 旧项目 `E:\everlasting\项目\六级复习工具` 的修改。

## 八、Git 与网络注意事项

- 不要修改全局 Git 配置、系统代理或浏览器代理，不要索要 GitHub 密码。
- 已知全局 Git 配置 `C:\Users\OS\.gitconfig` 仍指向失效代理 `127.0.0.1:10808`。
- 浏览器代理曾检测为 `127.0.0.1:7890`，但不要据此擅自修改 Git 配置。
- 之前使用单次命令参数 `git -c http.proxy= -c https.proxy= ...` 可以访问 GitHub；这不会持久修改配置。仍应在执行任何远端写入前取得用户授权。
- 当前本地 `main` 领先 `origin/main` 11 个 commit。不要误以为 P1-1 已上传。
- P1-2 不需要 `push`；除非用户明确要求，否则只在本地提交。
- 添加新依赖若被供应链联网校验或 `npmmirror` 阻断，不要修改代理配置绕过；记录实际错误并与用户确认下一步。

## 九、验证方式

正常质量命令是：

```powershell
pnpm check
```

当前宿主可能在任何 `pnpm` 命令前强制执行供应链联网校验，并因无法直连 `npmmirror` 而阻断。若项目依赖已经完整安装，可以运行等价分项门禁：

```powershell
node .\node_modules\vue-tsc\bin\vue-tsc.js --noEmit
node .\node_modules\eslint\bin\eslint.js .
node --test
node .\node_modules\vite\bin\vite.js build --mode production
```

还必须运行 `P1领域模型实施计划.md` 中的工程规范扫描。允许保留两个已记录的非阻塞构建警告：

- `.env.production` 中的 `NODE_ENV=production`。
- PostCSS 插件没有传递 `from` 参数。

无法取得 `pnpm check` 整体退出码时，交接文档必须如实记录，不能写成已通过。

## 十、P1-2 完成标准

只有以下条件全部满足，才可将 P1-2 标记为 `[x]`：

- FSRS 库选择有当前官方证据和清晰取舍。
- 不手写 FSRS 核心调度算法。
- 三档评分映射明确且有测试。
- `Timestamp` 与库时间类型转换明确且有测试。
- 初始化参数、默认参数和持久化字段明确。
- 算法状态通过项目自有类型封装，不污染 P1-1 领域类型。
- 最小验证用例通过，并覆盖 JSON 序列化边界。
- 全量分项质量门禁和工程规范扫描通过。
- 设计决策、修改文件、验证证据和剩余风险已写入交接文档。
- 没有提前侵入 P1-3、P1-4 或页面开发。

## 十一、可直接发给新对话的任务指令

```text
请在 D:\工作\ddff 继续执行项目实施计划中的 P1-2。

开始前完整阅读 AGENTS.md、交接文档.md、模型与Agent协作指南.md、项目实施计划.md、P1交接文档.md、P1-2任务交接文档.md、P1领域模型设计.md 和 P1领域模型实施计划.md，并核对 Git 状态，不要从聊天记录猜测阶段。

P1-2 的任务风险为 L3：运行时选择能够可靠处理复杂技术选型、算法边界和跨模块兼容性的模型，通过当前官方资料研究并选择成熟的浏览器端 TypeScript FSRS 实现，不手写核心算法。先形成并确认短决策记录，明确 again/hard/good 三档映射、Timestamp/Date 边界、初始化参数、默认参数、持久化字段和升级兼容性，再创建隔离工作树，按 TDD 实现最小适配与验证用例。

不要修改任何代理配置，不要索要 GitHub 密码，不要 push，不要提前实现 P1-3/P1-4、Store、页面、真实词库或 UI。若 pnpm 供应链校验被 npmmirror 阻断，如实记录并先继续可离线完成的工作。

完成后更新项目状态与 P1 交接文档，运行分项质量门禁和工程规范扫描，做只读代码审查，再让我选择如何集成分支。
```
