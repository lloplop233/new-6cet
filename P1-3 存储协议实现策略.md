# P1-3 存储协议实现策略

> 定位：P1-3 的决策记录、冻结契约和 P5 交接约束。属于历史性 / 专项性文档：文中「当前状态」类文字只描述本任务收口时的 checkpoint，不构成项目当前事实；项目当前状态以 `交接文档.md` 为准。
>
> 风险等级 L3。用户已于 2026-09-03 确认四项决策（单一键、降级拒绝、默认目标词量 2500、进度字段命名 `progress`），本文是这些决策的书面载体。

## 一、背景与目标

P1-2 在集成时冻结了存储协议必须遵守的边界：

- 存储协议必须拥有独立 `schemaVersion`，用于选择校验和迁移路径；`libraryVersion` 只记录生成 FSRS 状态时使用的第三方库版本，不能代替、推导或兼任 `schemaVersion`。
- scheduler 元数据（算法名、库名、库版本、完整参数快照）放在存储协议的统一位置，不在每个单词上重复保存。
- 每条已有进度必须包含 `ReviewState` 与 `FsrsSchedulingState`，两者序列化键集合不相交，使用嵌套组合。
- 不持久化 `Date`、库类实例、`Map`、`Set`、函数或可重新计算的派生值。

本任务在上述边界内回答任务卡的六个问题：**存储键、`schemaVersion`、默认值、迁移、损坏恢复、写入失败处理**，并满足约束「词库本体不重复存进 `localStorage`」。验收标准是能解释**升级、降级和异常数据**各自会发生什么。

## 二、Scope 与 Non-goals

**Scope**

1. 协议文档（本文件）。
2. `src/types/storage.ts`：持久化信封、单词进度、scheduler 元数据、读写结果类型、`StorageLike` 注入接口、迁移函数类型。
3. `src/constants/storage.ts`：存储键、当前 `SCHEMA_VERSION`、`APP_VERSION`、默认设置。
4. `src/services/storage.ts`：`loadState` / `saveState` / `createDefaultEnvelope` / `getBrowserStorage`、形状校验、迁移链遍历。
5. `src/services/storage.test.ts`：契约测试与关键失败模式的反证基础。

**Non-goals**

- 不接线 Store、页面或真实 `localStorage` 调用（P5-1）。
- 不实现真实版本迁移：`SCHEMA_VERSION = 1` 是首个版本，迁移链为空，仅以测试内注入的模拟迁移验证机制（真实迁移随未来版本演进出现在 P5-2）。
- 不做 JSON 备份导出导入（P5-3）、不做用户可见的恢复 UI（P5-4）。
- 不修改 P1-1 冻结的领域类型文件（`backup.ts`、`review.ts`、`settings.ts`、`study.ts`、`word.ts`）和任何 V0 页面。
- 不为 `localStorage` 增加分键、压缩、IndexedDB 或异步存储。

## 三、决策记录

### D1 单一键布局（用户已确认）

全部持久化状态存进**单一键** `cet6-vocab:state`，内容为一个完整 `PersistedEnvelope`。

理由：

- **一致性**：读取、写入、迁移都以信封为单位，不存在「设置是新版、进度是旧版」的跨键错位状态；迁移永远是单对象变换。
- **体积可控**：按每条单词进度约 200–250 字节 JSON 估算，5651 词全部有进度时约 1.2–1.4 MB，低于 `localStorage` 常见的 5 MB 配额；单人使用、打分时才写入，全量重写频率和成本可接受。
- **与备份对齐**：信封结构与 `BackupEnvelope` 同族，P5-3 的导出 / 导入可以复用同一套校验和迁移路径。

代价：每次保存全量重写。若未来（例如接入真实 5651 词库后）成为瓶颈，正确演进路径是**提升 `SCHEMA_VERSION` 并通过迁移拆键**，而不是在 v1 内打补丁。

键名 `cet6-vocab:state` 取自 package name，冒号前缀构成命名空间，与 `pinia-plugin-persistedstate` 默认键（store id，如 `route-cache`）天然不冲突。

### D2 `schemaVersion` 语义

- 当前版本为 **1**，是本项目存储结构的第一个版本。
- `schemaVersion` 是**项目存储结构**的版本，决定读取时走「直接校验」还是「迁移链」；`libraryVersion` 是生成 FSRS 状态所用**第三方库**的版本，只是 scheduler 元数据中的一个被如实记录的字段。两者职责不同（P1-2 已冻结该区分）。
- 存储协议的 `schemaVersion` 与 `BackupEnvelope.schemaVersion` 属于**同一版本空间**：备份文件是持久化信封的快照投影，导入备份即导入某个历史版本的数据，必须走同一套版本判断和迁移。两个格式当前形状不完全一致（见第六节 P5-3 交接约束），统一工作是 P5-3 的任务。
- `SCHEMA_VERSION` 常量与类型字面量绑定：`PersistedEnvelope.schemaVersion` 的类型是 `typeof SCHEMA_VERSION`，构造非当前版本信封会在编译期报错；测试冻结当前值为 `1`。

### D3 读取行为矩阵（核心不变量：读永不写盘、失败永不覆盖）

| 磁盘 / 存储状态 | `loadState` 行为 |
|---|---|
| `getItem` 抛错（存储被禁用等） | `error / unavailable`，`raw: null` |
| 键不存在（新用户） | `fresh`，返回 `createDefaultEnvelope()`，**不写盘** |
| 内容不是合法 JSON | `error / corrupted`，`raw` 保留原始字符串 |
| 是 JSON 但顶层不是对象 | `error / invalid`，`raw` 保留 |
| `schemaVersion` 缺失或不是安全整数 | `error / invalid`，`raw` 保留 |
| `schemaVersion > SCHEMA_VERSION`（降级：旧代码读新数据） | `error / future-version`，`raw` 保留，**绝不静默吞掉新数据**（用户已确认） |
| `schemaVersion < SCHEMA_VERSION` 且迁移链完整 | 迁移后校验，通过则 `loaded` |
| 迁移链缺失步骤、迁移抛错或产物非法 | `error / migration-failed`，`raw` 保留 |
| `schemaVersion = SCHEMA_VERSION` 但形状非法 | `error / invalid`，`raw` 保留 |

判断顺序：解析 → 顶层对象 → 版本可读 → 版本比较 → （迁移）→ 完整形状校验。版本比较先于形状校验，因此「未来版本 + 内容垃圾」仍归类为 `future-version`。

`raw` 随错误返回，供 P5-4 做诊断和保守恢复（例如导出原始数据）；协议层自身**不提供**任何自动覆盖、清理或隔离写操作。

### D4 写入行为矩阵（核心不变量：任何失败路径不破坏磁盘已有数据）

| 阶段 | 行为 |
|---|---|
| `JSON.stringify` 失败（调用方塞入了不可序列化值） | `error / serialize`，**不触碰 storage** |
| `setItem` 抛配额类错误（`name === 'QuotaExceededError'`） | `error / quota`，旧值保持原样 |
| `setItem` 抛其他错误（隐私模式等） | `error / unavailable`，旧值保持原样 |
| 全部成功 | `saved` |

`saveState` 不做写前校验：`PersistedEnvelope` 的类型即契约，调用方（未来 P5-1）必须传入由 `loadState` 结果、`createDefaultEnvelope()` 或类型检查过的构造产物。协议层承诺的是「写失败不破坏旧数据」，`localStorage` 的 `setItem` 语义（要么整体成功要么抛错、不留半写状态）与此匹配。

### D5 迁移链机制

- 迁移注册表是 `ReadonlyMap<number, Migration>`：键为**源版本 N**，值为把 N 版形状变换为 N+1 版形状的纯函数，函数自身负责把 `schemaVersion` 写为 N+1。
- 读取时从数据版本出发依次应用 `MIGRATIONS[N]`、`MIGRATIONS[N+1]`…直到 `SCHEMA_VERSION - 1`，再对最终产物做完整形状校验。链上任何一步缺失、抛错，或产物校验失败，都归类为 `migration-failed`。
- v1 是首个版本，生产注册表为空；任何 `schemaVersion < 1` 的整数（如 0、负数）都因「无迁移路径」落入 `migration-failed`。
- 注册表作为 `loadState` 的可选参数注入（默认生产表），测试用注入表模拟迁移，**不修改生产注册表**。
- 迁移只发生在内存：读取路径永不写盘，迁移结果在下次 `saveState` 时才落盘。

### D6 默认值（用户已确认）

新用户第一次加载（键不存在）时，`createDefaultEnvelope()` 返回：

- `schemaVersion: SCHEMA_VERSION`、`appVersion: APP_VERSION`、`savedAt: 0`（「从未保存」哨兵）。
- scheduler 元数据 = 当前安装版本的 `FSRS_ALGORITHM` / `FSRS_LIBRARY` / `FSRS_LIBRARY_VERSION` + `createFsrsParametersSnapshot()` 参数快照。注意：读取**已有**数据时保留数据中自带的 scheduler 元数据，不用当前值覆盖——元数据记录的是「生成这批数据时用的参数」。
- `settings = DEFAULT_SETTINGS`：`targetWordCount: 2500`（与 V0 验收展示一致）、`preferredMode: 'flashcard'`、`autoPronounce: false`（TTS 未接入，保守关闭）、`updatedAt: 0`。
- `progress: {}`、`activeSession: null`。

`DEFAULT_SETTINGS` 是冻结的共享常量，修改前必须复制（`{ ...DEFAULT_SETTINGS, ... }`）；`createDefaultEnvelope` 每次返回全新对象，调用方可自由修改。

### D7 `pinia-plugin-persistedstate` 边界

该插件已在 `src/stores/index.ts` 注册，但业务数据（settings / progress / activeSession）**不走它**：它对 store state 做无版本的 `JSON.stringify`，没有 `schemaVersion`、迁移和损坏恢复控制，与本协议的目标冲突。插件仅允许用于 UI 噪声状态（如 `route-cache`），键空间（store id）与协议键 `cet6-vocab:state` 天然分离。P5-1 接线时不得给业务 store 开启 `persist`。

### D8 校验深度原则：结构，不语义

形状校验刻意停留在**结构层**，语义校验留给未来的消费层：

- **校验**：顶层与嵌套对象的键集合精确匹配（多一个少一个都算 `invalid`）；枚举字段（`phase` / `mode` / `rating` / `status`）取值合法；时间字段是 `[0, MAX_TIMESTAMP]` 内的安全整数（复用 P1-2 冻结的时间边界）；计数是非负安全整数；`progress` 键与 `review.wordId` 一致；`activeSession.currentIndex` 在 `[0, queue.length]` 内；`queue` 无重复且非空；`results` 键都来自 `queue` 且与条目内 `wordId` 一致；参数快照是结构完整的 `FsrsParametersSnapshot`（`requestRetention ∈ (0,1)`、`maximumInterval` 为 ≥ 1 的安全整数、非空有限数权重数组、非空的 `m/h/d` 步长字符串数组）。
- **不校验**：`targetWordCount` 的产品范围（1000–5000 属于 UX 层）；`stability` / `difficulty` 的算法值域（属于 P4-1 重建 `Card` 时）；`scheduler` 元数据与当前安装常量的一致性（旧数据合法携带旧元数据）；`weights` 数组长度（不同参数代际合法共存）；未知 `wordId` 是否存在于词库（词库不在存储层）。
- **未知 `wordId` 保留不删**：进度中存在词库外的 `wordId` 时照常加载，数据保全优先；它对调度是惰性的，如何呈现属于后续阶段。

键集合精确匹配同时是「词库本体不进 `localStorage`」的类型级证明之一：`PersistedData` 没有 `words` 字段，`@ts-expect-error` 反证 + 运行时键断言 + 测试三方锁定。

### D9 `StorageLike` 注入与浏览器适配器

- 协议只依赖 `StorageLike`（`getItem` / `setItem` / `removeItem`），测试用内存实现，运行行为可完全确定。
- `getBrowserStorage()` 返回 `globalThis.localStorage` 的适配结果：访问不到、或探测性写入（写后删 `cet6-vocab:probe`）抛错时返回 `null`，由调用方决定降级策略；存储可用性问题在读写时也会以 `unavailable` / `quota` 分类暴露。

## 四、模块与文件归位

| 文件 | 层 | 职责 |
|---|---|---|
| `src/types/storage.ts` | 类型 | 协议全部类型；仅类型导入，零运行时依赖 |
| `src/constants/storage.ts` | 常量 | `STORAGE_KEY`、`SCHEMA_VERSION`、`APP_VERSION`、`DEFAULT_SETTINGS`；仅类型导入 |
| `src/services/storage.ts` | 数据访问层 | 读写、校验、迁移链、默认信封、浏览器适配器；碰 `localStorage`，按 `工程规范.md` 归 `services/` |
| `src/services/storage.test.ts` | 测试 | 契约测试，`node --test` 直跑 |

依赖方向：`services/storage.ts` → `utils/fsrs.ts`（仅取 `createFsrsParametersSnapshot` 构造默认 scheduler 元数据）→ `ts-fsrs`。当前无任何应用代码导入本服务，production bundle 不受影响（构建后另行核验）。Node 测试路径的 `.ts` 扩展导入沿用了 P1-2 的 `@ts-expect-error` 惯例（已知 Minor，P1-4 再统一定夺）。

## 五、与既有契约的关系

- **P1-1 领域类型**：`Settings`、`StudySession`、`ReviewState`、`WordId` 原样复用，不修改。`WordProgress` 是存储层新增的组合类型，嵌套形状与 P1-2 测试中 `persistCard` 的 `{ review, scheduling }` 一致。
- **P1-2 FSRS 边界**：`MAX_TIMESTAMP`（时间边界）、`createFsrsParametersSnapshot`（默认参数）、`FSRS_ALGORITHM` / `FSRS_LIBRARY` / `FSRS_LIBRARY_VERSION`（元数据）直接复用；`APP_VERSION` 与 `package.json` 的绑定测试沿用 `FSRS_LIBRARY_VERSION` 与依赖版本绑定的同款做法。
- **`BackupEnvelope`**：形状同族（`schemaVersion` + `appVersion` + 时间戳 + `data`），但 `BackupData.reviews` 的值是 `ReviewState`（无 FSRS 状态），与 `PersistedData.progress`（含 `scheduling`）不同。这是**已知的 P5-3 张力**，本任务不改 `backup.ts`。

## 六、P5 交接约束

1. **P5-1（Settings 与 Progress Service）**：必须通过 `loadState` / `saveState` 存取业务数据，不得绕过协议直写 `localStorage`、不得给业务 store 开启 `pinia-plugin-persistedstate`；保存前从 `LoadResult` 的信封出发构造新信封，不要手工拼 `schemaVersion`。
2. **P5-2（迁移框架）**：真实迁移直接在 `services/storage.ts` 的注册表追加条目，函数必须是纯函数且自带版本号写入；新增版本必须同步更新本文的行为矩阵理解和测试。
3. **P5-3（导出导入）**：导入的备份必须当作「未知版本的外部数据」走同一套解析 → 版本判断 → 迁移 → 校验路径；备份格式与持久化格式的统一（`reviews` 是否扩展 FSRS 状态）在 P5-3 决策，决策时必须保证「导出不丢调度信息」。
4. **P5-4（异常恢复）**：消费 `LoadFailureReason` / `SaveFailureReason` 分类做用户提示；`raw` 可用于导出原始数据做保守恢复。协议层永不自动覆盖损坏数据，任何「重置」都必须是用户显式确认后的 `saveState`。
5. **P1-4（模块接口冻结）**：`services → utils` 的依赖方向、`.ts` 扩展导入的 `@ts-expect-error` 惯例需要在此正式定夺。

## 七、验收清单

1. `pnpm check` 退出码 0（TypeScript、ESLint、全部 Node 测试、production build）。
2. 行为矩阵每一格都有对应测试；「读不写盘」「失败不覆盖」两类不变量在每个失败用例上断言。
3. 关键失败模式完成反证实验：注入目标失败模式（校验放行、版本检查移除、迁移吞错、配额误分类、读取写盘）后，对应测试必须变红；结果记录在第八节。
4. 本文档能回答「升级、降级、异常数据会发生什么」（第三节 D3 / D4 / D5）。
5. 词库本体不进 `localStorage`：`PersistedData` 无 `words` 字段（类型反证 + 键断言 + 测试）。
6. production 产物不含 `ts-fsrs`（构建后核验）。
7. 独立 L3 Review 通过（Critical 0 / Important 0），结论记录在第八节。

## 八、验证记录（2026-09-03 实施会话回填）

- 完整 `pnpm check` 退出码 0：TypeScript、ESLint、91/91 Node 测试（其中存储协议 69 项）、Vite production build。
- production 产物扫描：`dist/assets/` 中无 `ts-fsrs` 引用——服务尚未被应用代码导入，FSRS 依赖未进入 bundle。
- 反证实验（一次性注入失败模式、确认测试变红后还原，未进入 commit）：
  1. **E1** 把 `validateEnvelope` 置为空操作 → **39 项测试变红**：形状校验是 `invalid` 分类的唯一防线，不是摆设。
  2. **E2** 移除 `future-version` 检查 → **恰好 2 项** future-version 测试变红：降级拒绝是显式行为，不是形状校验的副作用。
  3. **E3** 把迁移失败改为静默返回默认值 → **3 项** migration-failed 测试变红：不存在「迁移失败静默回退默认数据」的路径。
  4. **E4** 把配额错误统一归类为 `unavailable` → **1 项** quota 分类测试变红。
  5. **E5** 在 fresh 路径写入 storage → **1 项**「读永不写盘」断言变红。
  - 实验后从备份还原源文件、复跑 67/67 通过（补测 2 项后最终 69/69）。
- 补测：反证自查发现空 `wordId` 键与 `queue` 非字符串成员两个校验分支没有测试锁定，已补充对应 mutation 用例。
- 独立 L3 Review（2026-09-03，独立 Agent 对最终实现审查）：结论 **APPROVED WITH CONDITIONS**，Critical 0、Important 1、Minor 2。
  - **Important 1**（唯一条件）：「读取保留数据自带 scheduler 元数据」是 D6 与 P1-2 升级约束冻结的行为，但无测试锁定，未来「读取时刷新元数据」的回归不会被现有测试捕获。已补 round-trip 测试（旧 `FSRS-5` / `5.0.0` / 自定义参数快照原样保留），并以 **E6 反证**验证：在 `loadState` 注入「读取后用当前默认值覆盖 `scheduler`」的回归 → 恰好该测试变红（69/70），移除后 70/70 通过。条件已满足。
  - **Minor 1**：`validateScheduling` 错误消息缺 `wordId` 定位（其余校验函数均带）。已修复，`scheduling` 相关 `invalid()` 消息统一带 `data.progress[${wordId}]` 前缀。
  - **Minor 2**：校验比 D8 字面声明略严的两处（步长数组要求非空、`maximumInterval` 要求 ≥ 1）。确认为有意收紧，已把 D8 文字补齐（「非空的 `m/h/d` 步长字符串数组」「`maximumInterval` 为 ≥ 1 的安全整数」）。
- Review 修复后最终验证：`pnpm check` 退出码 0，92/92 Node 测试（存储协议 70 项）、TypeScript、ESLint、production build 全部通过。
