# P2-3 最小会话恢复 — 实施计划

> 本文是 P2-3 的执行说明书，供接手模型独立实施。
> 计划产出时间：2026-09-04；基准 `main@e4ec7e3`，工作区干净、无 worktree。
> **执行前必须重新核对**：`git status --short --branch`、`git status --porcelain -uall`、`git diff --stat`、`git worktree list`。若基准已变，先报告差异再决定是否沿用本计划。

## 一、任务卡与验收标准

- 来源：`项目实施计划.md` 第五节 P2-3，状态 `[ ]`。
- 内容：保存当前队列、位置和已评分结果。
- **验收标准：中途刷新后恢复到正确位置。**
- 风险等级：**L2**。Review Budget = 1 次独立 Review，实质性修复后最多 1 次条件复审。
- L3 升级触发条件（任务卡原文「若发现跨模式状态或数据恢复边界，重新判断为 L3」）：若实施中发现必须让选择题分支参与持久化、或必须修改 `src/services/storage.ts` 的校验/迁移语义，立即停止并重新判断等级。
  - 本计划的设计**刻意避开**这两点：选择题分支零存储交互，存储协议零改动。按此设计维持 L2。

## 二、已确认的产品决策（用户 2026-09-04 确认，不要重开）

| # | 决策 | 结论 |
|---|---|---|
| D1 | 持久化读写落在哪一层 | 新建 `src/composables/useStudySession.ts` 封装；**不建 Pinia store**（Store 接线属 P5-1，越界会把 L2 抬到 L3） |
| D2 | 刷新后如何恢复 | **静默恢复**到上次的词与进度，不加「继续/重开」弹窗（弹窗 UI 属 P3） |
| D3 | 会话跑完后如何处置存储 | **保留 `status: 'completed'` 的会话**；完成页优先从存储读，顺带修掉「完成页刷新变空态」；下次进入学习页发现 completed 则新建会话 |
| D4 | P2-2 遗留的两项瞬态 | 只处理与恢复相关的那项。「评完立刻返回吞掉跳转」由持久化自然解决（须浏览器实测确认）；**「末词 450ms 无卡瞬态」不在本任务范围**，留给 P3 |

### D5 队列与词库的匹配宽严（实施者不要自行改动）

恢复条件采用**宽松规则**：只要存储里 `queue` 的每个 id 都存在于当前词库，就允许恢复；**不要求** `queue` 深度等于 `buildStudyQueue(library)` 的结果。

- 理由：P2-3 只需保证刷新恢复；当前词库固定 20 词，两种规则行为完全相同。词库版本变更的处置属 P6，`SchedulerMetadata.libraryVersion` 已为此预留。
- 宽松规则保证「每个词都能渲染」，不会出现空卡。
- **P6 交接约束**：换真实词库时必须重新评估此规则，否则用户可能被恢复到过期队列。

## 三、事实基础（已核实，不要重新调查）

### 现有实现精确位置

| 关注点 | 位置 | 现状 |
|---|---|---|
| 会话纯函数 | [study-session.ts](src/utils/study-session.ts) 80 行 | `buildStudyQueue`、`createStudySession`、`currentWordId`、`isSessionFinished`、`rateWord` |
| 会话契约测试 | [study-session.test.ts](src/utils/study-session.test.ts) | 22 项 |
| 存储读写协议 | [storage.ts](src/services/storage.ts) 381 行 | `createDefaultEnvelope`、`loadState`、`saveState`、`getBrowserStorage` |
| 存储协议测试 | [storage.test.ts](src/services/storage.test.ts) 478 行 | 内含 `createMemoryStorage()`（带 `writeCount()` 计数）等 helper，直接复用 |
| 持久化数据形状 | [types/storage.ts:22](src/types/storage.ts:22) | `PersistedData.activeSession: StudySession \| null`，本就为本任务预留 |
| `activeSession` 校验 | [storage.ts:332](src/services/storage.ts:332) `validateSession` | 已覆盖全部 9 个字段 + `results` 键来自 `queue` + `currentIndex ∈ [0, queue.length]` |
| 学习页（内存会话） | [study/index.vue:35](src/pages/study/index.vue:35) | `session = ref(createStudySession(...))`，刷新即回第一词 |
| 完成页（路由 state） | [study/complete.vue:15](src/pages/study/complete.vue:15) | 从 `history.state.session` 解析 JSON，刷新变空态 |
| 跳转传参 | [study/index.vue:147](src/pages/study/index.vue:147) | `router.push({ ..., state: { session: JSON.stringify(...) } })` |

### 硬约束（违反即返工）

1. **必须走 `loadState`/`saveState`**，禁止直写 `localStorage`（`交接文档.md` 第 122 行明列禁止事项）。
2. **不得给业务 store 开 `pinia-plugin-persistedstate`**（`P1-3 存储协议实现策略.md` D7）。该插件已在 [stores/index.ts:7](src/stores/index.ts:7) 注册，仅限 UI 噪声状态。
3. **不改 `STORAGE_KEY`**（`cet6-vocab:state` 已被测试冻结，改名会孤儿化用户数据）。
4. **不改 `src/services/storage.ts`**：校验、迁移链、失败语义均已冻结并有 70 项测试 + 6 项反证覆盖。本任务只是它的调用方。
5. **P1-4 调用方向**（[eslint.config.ts](eslint.config.ts)）：`types/constants → utils → services → stores/composables → components/pages/router`，只许向下。
   - `utils/**` **禁止** import `services`、`vue`、`pinia`、`vant`、`@vue/*`。
   - `composables/**` 可 import `services`、`utils`、`vue`。
   - `pages/**` 可 import 以下任意层。
   - **不要修改 `eslint.config.ts`**，本任务的所有 import 方向都已合法。
6. **值 import 必须带 `.ts` 扩展名**（P1-4 启用了 `allowImportingTsExtensions`，见 [storage.ts:11](src/services/storage.ts:11) 的现有写法）；`import type` 不带扩展名。
7. **时间一律入参注入**（P1-2 时间纪律）：纯函数不许调 `Date.now()`。
8. **选择题分支原样不动**：其评分→`Rating` 映射未冻结（属 P4-3/P4-4），不得让它读写存储。
9. **不新增顶层目录**；`src/composables/` 是 `工程规范.md` 已定死的合法目录（目前为空）。

### 预期的自动生成副作用

`src/composables` 已在 auto-import 的 `dirs` 中（[build/vite/index.ts:51](build/vite/index.ts:51)）。新增 composable 后，`pnpm dev` 或 `pnpm build:pro` 会**自动改写 `src/types/auto-imports.d.ts`**。

- 这是预期结果，应与代码一并 commit。
- **不要手工编辑该文件**（`AGENTS.md` 第 7 条）。
- 页面里建议仍写显式 import（更易读，也让 ESLint 方向守卫可见）。

## 四、方案设计

### 分层策略

所有**决策逻辑**放在 utils 层纯函数（可被 `node --test` 覆盖）；composable 只做「调 storage + 持有 ref」的薄胶水层。

这直接回应 P2-2 的 Review 教训：UI 时序 bug 纯函数测不出，所以要让尽可能多的判断变成纯函数，剩下的薄胶水层用浏览器实测兜。

```text
utils/session-recovery.ts   纯函数：恢复判定 + 信封写入   ← Node 测试主战场
        ↑
composables/useStudySession.ts   胶水：getBrowserStorage + loadState/saveState + ref
        ↑
pages/study/index.vue    pages/study/complete.vue
```

### 新增：`src/utils/session-recovery.ts`

允许 import：`types/*`（type-only）、`./study-session.ts`。禁止 import `services`、`vue`。

```ts
export type SessionRecoveryPlan =
  | { persist: false, session: StudySession, restored: false }
  | { persist: true, session: StudySession, restored: boolean, envelope: PersistedEnvelope }

export function planSessionRecovery(input: {
  loadResult: LoadResult
  library: readonly Word[]
  mode: StudyMode
  sessionId: string
  now: Timestamp
}): SessionRecoveryPlan

export function withActiveSession(
  envelope: PersistedEnvelope,
  session: StudySession,
  savedAt: Timestamp,
): PersistedEnvelope
```

**`planSessionRecovery` 判定规则（按顺序）：**

1. `loadResult.status === 'error'` → `{ persist: false, session: 新建会话, restored: false }`
   - **禁止写盘**是这条的全部要点：`corrupted` / `future-version` / `migration-failed` / `invalid` / `unavailable` 任一情况下写盘都会破坏磁盘上的既有数据，直接违反 P1-3 不变量「任何失败路径不破坏磁盘已有数据」「未来版本拒绝加载且不覆盖」。用户仍能正常学习，只是本次不持久化。
2. `status` 为 `fresh` 或 `loaded` → 取 `envelope.data.activeSession`，用下述条件判断能否恢复：
   - `activeSession !== null`
   - `activeSession.mode === input.mode`
   - `activeSession.status === 'active'`
   - `!isSessionFinished(activeSession)` —— 显式检查，不要省。存储校验允许 `status: 'active'` 与 `currentIndex === queue.length` 并存（[storage.ts:349](src/services/storage.ts:349) 只限制上界），这是理论上可达的非法组合。
   - `activeSession.queue` 的每个 id 都在 `input.library` 中（D5 宽松规则）
   - 全部满足 → `{ persist: true, session: activeSession, restored: true, envelope }`（信封原样返回，无需立刻写盘）
   - 任一不满足 → 新建会话，`{ persist: true, session: 新会话, restored: false, envelope: withActiveSession(envelope, 新会话, now) }`

新建会话 = `createStudySession({ id: input.sessionId, mode: input.mode, queue: buildStudyQueue(input.library), startedAt: input.now })`。

**`withActiveSession` 契约：**

- 不可变：返回新对象，入参 `envelope` 及其嵌套对象保持原样。
- 只改两处：`data.activeSession` 与 `savedAt`。`settings`、`progress`、`scheduler`、`schemaVersion`、`appVersion` 原样透传。
  - 尤其**不要碰 `scheduler`**：P1-3 已有 round-trip 测试锁定「读取保留旧 scheduler 元数据」，刷新它会让那条测试变红。
- 产物必须能通过 `loadState` 的校验（round-trip 测试锁定，见第六节）。

### 新增：`src/composables/useStudySession.ts`

```ts
/** 只读取存储里的会话，不新建、不写盘。完成页用。 */
export function readPersistedSession(mode: StudyMode): StudySession | null

/** 学习页用：持有会话 ref，负责恢复、评分落盘。 */
export function useStudySession(): {
  session: Ref<StudySession | null>
  isPersisted: Ref<boolean>
  startOrRestore: (mode: StudyMode) => void
  rate: (wordId: WordId, rating: Rating) => void
}
```

行为：

- `startOrRestore(mode)`：
  1. `getBrowserStorage()`；返回 `null`（隐私模式等）→ 内存新建会话、`isPersisted = false`、直接结束。
  2. `loadState(storage)` → `planSessionRecovery({...})`，`sessionId` 用 `study-${Date.now()}`、`now` 用 `Date.now()`（时间只在这一层取）。
  3. `plan.persist === false` → 只设 `session`，`isPersisted = false`，**不调 `saveState`**。
  4. `plan.persist === true` → 缓存 `plan.envelope` 到闭包变量；`restored === false` 时调一次 `saveState`（把新会话落盘）；`isPersisted = true`。
- `rate(wordId, rating)`：
  1. `rateWord(session, wordId, rating, Date.now())` → 更新 ref。
  2. `isPersisted` 为真时：`next = withActiveSession(缓存信封, 新会话, Date.now())`；`saveState(storage, next)`；成功则把 `next` 写回缓存信封。
  3. `saveState` 返回 `error`（`quota` / `unavailable` / `serialize`）→ 把 `isPersisted` 置 `false`，**不抛错**、不阻塞学习流程。内存会话继续可用。
- `readPersistedSession(mode)`：`getBrowserStorage()` → `loadState` → `status === 'loaded'` 且 `activeSession?.mode === mode` 时返回它，否则 `null`。**任何情况下都不写盘。**

### 修改：`src/pages/study/index.vue`

- 删掉第 35-40 行的 `ref(createStudySession(...))` 直接初始化，改用 composable。
- `session` 变成可能为 `null`（挂载前），模板里 `v-if="!isQuiz && currentWord"` 已有防御，但 `queueSegments` / `progressTotal` / `progressIndex` 等 computed 需要补 null 兜底。
- `onMounted(() => { if (!isQuiz.value) startOrRestore('flashcard') })` —— **只在翻卡分支调用**，这是「选择题零存储交互」的落点。
- `chooseRating` 改调 composable 的 `rate()`。
- 跳完成页时**保留** `state: { session: JSON.stringify(...) }`：它作为无 `localStorage` 环境下完成页的兜底（见下）。
- `isRevealed` / `selectedRating` / 计时器逻辑不动。
- **恢复时必须重置 `isRevealed = false`、`selectedRating = undefined`**，否则恢复后可能直接停在「已翻开且已选档」的状态。
- 恢复时若 `isSessionFinished(session)` 为真（理论上被上面的判定挡掉，但纵深防御）→ 不渲染卡片，直接 `router.replace` 到完成页。

### 修改：`src/pages/study/complete.vue`

数据源改为三级：**存储 → 路由 state → 空态**。

- 新增第一优先级 `readPersistedSession('flashcard')`。
- 保留现有 `history.state` 解析作为第二级（覆盖无 `localStorage` 的降级路径，改动仅几行，不删除现有能力）。
- 空态文案与 `hasRealResult` 逻辑不动。
- `replay()` 不动：它 push 到 `/study`，学习页会发现存储里是 `completed` 会话 → 新建并覆盖，「再学一遍」自然生效。

## 五、文件清单

**新增（3）**

1. `src/utils/session-recovery.ts`
2. `src/utils/session-recovery.test.ts`
3. `src/composables/useStudySession.ts`

**修改（3）**

4. `src/pages/study/index.vue`
5. `src/pages/study/complete.vue`
6. `src/services/storage.test.ts`（**仅追加** round-trip 测试，不改任何现有断言）

**自动生成（1）**

7. `src/types/auto-imports.d.ts`（构建产物，随 commit，不手改）

**明确不许碰**

`src/services/storage.ts`、`src/utils/study-session.ts`、`src/types/**`（除自动生成）、`src/constants/**`、`eslint.config.ts`、`tsconfig.json`、`src/stores/**`、学习页选择题分支、首页、`src/pages/progress/*`、`src/pages/settings/*`、任何样式文件。

> 若实施中认为必须改上述任一文件，**停下来报告**，不要自行扩大范围。

## 六、测试清单

### `src/utils/session-recovery.test.ts`（新文件，纯 Node 测试）

`planSessionRecovery`：

1. `status: 'error'`（逐个覆盖 5 种 `LoadFailureReason`）→ `persist === false`，且返回的是全新会话（`currentIndex === 0`、`results` 为空）。
2. `status: 'fresh'` → 新建会话，`restored === false`，`envelope.data.activeSession` 是该新会话。
3. `loaded` + 可恢复会话（mode 匹配、active、进行到中途、queue ⊆ 词库）→ `restored === true`，返回的 `session` **深度等于**存储里那个（位置和已评分结果都不丢）。
4. `loaded` + `activeSession.mode` 不匹配 → `restored === false`（跨模式不复用）。
5. `loaded` + `status: 'completed'` → `restored === false`（完成后重开）。
6. `loaded` + `activeSession: null` → `restored === false`。
7. `loaded` + `status: 'active'` 但 `currentIndex === queue.length` → `restored === false`（非法组合防御）。
8. `loaded` + queue 含词库外的 id → `restored === false`。
9. 纯函数性：调用两次结果一致；入参 `envelope`、`library` 未被修改。

`withActiveSession`：

10. 不可变：入参 `envelope` 深度未变。
11. 只改 `data.activeSession` 与 `savedAt`；`settings`、`progress`、`scheduler`、`schemaVersion`、`appVersion` 深度相等。
12. `savedAt` 取入参值（不是 `Date.now()`）。

### `src/services/storage.test.ts`（追加，不改现有断言）

13. **Round-trip**：`createDefaultEnvelope()` → `withActiveSession(envelope, 会话, NOW)` → `saveState` → `loadState` → `status === 'loaded'`，且 `envelope.data.activeSession` 与写入的会话深度相等。
    - 这一条是全计划最关键的测试：它证明纯函数产出的信封**真的能通过已冻结的 381 行校验**，而不是「类型对但运行时被拒」。
14. **失败路径零写入**：`loadState` 返回 error 时，`planSessionRecovery` 的产物走不到 `saveState`。用 `createMemoryStorage()` 的 `writeCount()` 断言写入次数为 0。
    - 复用文件顶部已有的 helper，不要新写假 storage。

> 追加位置：放在文件末尾新的 `describe` 块内，与现有 70 项互不干扰。

## 七、反证实验（必做，交接文档明列的教训）

每项先改坏实现，跑 `pnpm test`，确认**指定的测试变红**，然后还原。记录「改了什么 / 哪几项变红 / 是否符合预期」。

| # | 破坏方式 | 预期变红 |
|---|---|---|
| E1 | 去掉 `mode === input.mode` 检查 | 测试 4 |
| E2 | 去掉 `queue ⊆ library` 检查 | 测试 8 |
| E3 | 去掉 `!isSessionFinished` 检查 | 测试 7 |
| E4 | `withActiveSession` 改成原地 mutate 入参 | 测试 10、11 |
| E5 | `withActiveSession` 顺手刷新 `scheduler` 元数据 | 测试 11 + storage.test.ts 现有的 scheduler round-trip 测试 |
| E6 | `error` 路径也返回 `persist: true` | 测试 1、14 |
| E7 | `withActiveSession` 漏写 `savedAt` | 测试 12、13（`savedAt` 校验失败 → `loadState` 返回 invalid） |

若某项**未变红**，不要跳过：说明该测试没锁住它声称锁住的契约，必须补测试或说明为何是有效实验（参考 P2-1 的第 3 项反证记录方式）。

## 八、浏览器验证清单（必做，不能只看 Node 测试绿）

P2-2 的 Critical off-by-one 就是纯函数测试全绿但 UI 时序错的典型。以下必须真机/浏览器实测：

1. **核心验收**：学到第 7 词，刷新 → 回到第 7 词、进度显示 `7 / 20`、队列条前 6 段按评分着色。
2. 恢复后继续评分到第 20 词 → 正常跳完成页，统计与实际点击一致，`status === 'completed'`。
3. **完成页刷新** → 统计仍在（D3 修掉的空态问题），不再是空态。
4. **评完立刻返回**（D4）：评一个词后立刻点返回，再进入 → 停在正确的下一词，不重复计分、不跳词。
5. 完成页点「再学一遍」→ 新会话从第 1 词开始，进度 `1 / 20`，队列条全 pending。
6. **选择题回归**：`?mode=multiple-choice` 全程与 V0 一致（abandon 1/6、四选项、正确/错误反馈）；期间 `localStorage` 的 `cet6-vocab:state` **不被写入**（DevTools 观察 `activeSession` 保持原值）。
7. **模式来回切换**：翻卡学到第 5 词 → 切 `?mode=multiple-choice` → 切回 → 会话未被污染。
8. **无 localStorage 降级**：DevTools 阻断 storage（或隐私窗口）→ 仍能完整学完 20 词，只是刷新回第 1 词、完成页靠路由 state；控制台无未捕获异常。
9. **损坏数据不被覆盖**：手工把 `cet6-vocab:state` 改成 `{"schemaVersion":999}` → 进入学习页能正常学习，且刷新后 DevTools 里该值**仍是 `999` 未被覆盖**（P1-3 不变量的现场证明）。
10. 375px 视口 + 控制台零错误。

## 九、验收门禁

```bash
pnpm check
```

必须退出码 `0`（TypeScript + ESLint + Node 测试 + production build）。

- 当前基线 115/115；本计划新增约 14 项，预期 **129/129 左右**（实际数以运行结果为准，不要照抄本数字）。
- 另需：
  - `git diff --stat` 审计——改动文件必须恰好是第五节的 6 个 + 自动生成的 `auto-imports.d.ts`，零越界。
  - `git diff --check`（空白错误）。
  - 确认 `dist/assets/` 仍**不含 `ts-fsrs`**（本任务不接线 FSRS；若出现说明误引入了依赖）。

## 十、实施步骤（每步都有验证，不要跳步合并）

1. **纯函数先行**：写 `session-recovery.ts` + 测试 → `pnpm test` 全绿 → 跑完 E1~E7 七项反证 → 还原。
   - 验证：测试绿 + 七项反证记录完整。此时零 UI 改动，风险最低。
2. **追加 round-trip**：改 `storage.test.ts`（仅追加）→ `pnpm test` 全绿。
   - 验证：测试 13、14 通过，现有 70 项无一变红。
3. **写 composable**：`useStudySession.ts` → `pnpm typecheck && pnpm lint`。
   - 验证：类型与方向守卫都过。ESLint 若报 `no-restricted-imports`，说明 import 方向写错了，**不要改 eslint.config.ts**，改 import。
4. **接学习页**：改 `study/index.vue` → `pnpm dev` → 浏览器验证 1、2、4、5、6、7、10。
   - 验证：核心验收（刷新回到正确位置）必须先在这里成立。
5. **接完成页**：改 `study/complete.vue` → 浏览器验证 3。
6. **降级与损坏路径**：浏览器验证 8、9。
7. **完整门禁**：`pnpm check` + diff 审计 + `git diff --check` + `dist` 无 `ts-fsrs`。
8. **独立 Review**：L2 Budget = 1 次。Review 必须独立于实施者，重点看第十一节。
9. **修复与条件复审**：若 Review 提出实质问题，修复后最多 1 次聚焦复审。
10. **finalization**：见第十二节。

## 十一、Review 重点（给独立 Review 的提示）

按 P2-2 的教训，优先攻击这些方向：

1. **测试是否真锁住了它声称锁住的契约**（P1-2 Review 的 6 项 Important 全出在这里）。逐条问：把实现改坏，这条测试会红吗？
2. **`withActiveSession` 是否真的没碰 `scheduler` / `progress` / `settings`**——这是丢用户数据的最短路径。
3. **失败路径是否真的零写入**。特别是 `future-version`：写盘就是数据毁坏，是 Critical。
4. **UI 时序**：恢复后 `isRevealed` / `selectedRating` 是否被正确重置？恢复到末词时会不会出现「卡片不显示但也不跳转」？P2-2 的 Critical 就是这一类。
5. **边界索引**：`progressIndex` 的 `Math.min(...)` 在 `queue.length` 与恢复位置组合下是否仍正确（P2-2 修 off-by-one 时引入的钳制）。
6. **选择题是否真的零存储交互**——包括 `?mode` 中途切换的路径。
7. 是否有越界改动（对照第五节的「明确不许碰」）。

## 十二、Finalization 清单

1. `项目实施计划.md` 第五节 P2-3：`[ ]` → `[x]`，补「完成证据」与「当前结论」小节，格式对齐 P2-1 / P2-2 两节。
2. `交接文档.md`：
   - 第 2 节加「G. P2-3 最小会话恢复已完成」小节。
   - 第 3 节「已验证基线」追加本次结果（测试数、反证、Review 结论）。
   - 第 4 节下一步改为 **P2-4 垂直闭环审查**，并写清 P2-3 遗留的已知边界作为其输入。
   - 第 5 节风险清单：移除已被本任务解决的「会话仅存内存 / 刷新回第一词 / 评完立刻返回吞掉跳转」，保留「末词 450ms 无卡瞬态」（D4 明确不做）并标注归属 P3。
   - 第 8 节文档索引加入本文件。
3. 本文件：执行完成后在文末追加「执行记录」小节（实际改动、实测结果、与计划的偏差及原因）。计划正文不要事后改写成完成态。
4. commit 拆分沿用既有惯例：`feat(study): ...` 一个实现 commit + `docs: ...` 一个文档 commit。
5. 推送：本机需经 `127.0.0.1:10809` HTTP 代理（`git -c http.proxy=...`，**不要改动 git config**）。推送前先向用户确认。

## 十三、本任务明确不做

- 不接 FSRS 调度（P4-1）。`progress` 字段本任务只透传，不写入。
- 不做 Settings / Progress Service 接线（P5-1）。
- 不做真实迁移链（P5-2）、不做 JSON 导出导入（P5-3）、不做异常恢复 UI（P5-4）。
- 不做选择题持久化（评分映射未冻结，属 P4-3/P4-4）。
- 不修「末词 450ms 无卡瞬态」（D4）。
- 不修「首页 6 词文案与 20 词不一致」（属 P3-3）。
- 不做视觉打磨（属 P3）。
- 不改存储协议、不改 ESLint 配置、不新建 Pinia store。

## 十四、执行记录（2026-09-05 收口时追加；正文不事后改写）

### 实际改动

与第五节清单完全一致：新增 `src/utils/session-recovery.ts`、`src/utils/session-recovery.test.ts`、`src/composables/useStudySession.ts`；修改 `src/pages/study/index.vue`、`src/pages/study/complete.vue`、`src/services/storage.test.ts`（仅末尾追加 describe 块）；自动生成 `src/types/auto-imports.d.ts` 随 commit。另修正 `.claude/launch.json` 的 dev 端口（5173→3000，与 vite.config.ts 实际端口对齐，属工具配置非产品代码）。步骤按第十节顺序执行，无跳步合并。

### 测试与反证

- Node 测试最终 130/130（基线 115 + session-recovery 13 + storage 集成 2），其中 13 = 计划 12 项 + Review 条件 I-1 补的 1 项（「completed 且索引居中」组合的独立锁定，E8 反证：删 `status === 'active'` 检查恰该用例变红）。
- 反证 E1–E7 实际变红情况与计划第七节预期有两处出入，均为计划预期偏差而非测试缺陷：E4 原地 mutate 除测试 10、11 外还红测试 12（入参 `envelope.savedAt` 断言同被破坏，方向一致）；E5 只红测试 11 与**新增**的测试 13——计划预期里「storage.test.ts **现有的** scheduler round-trip 测试」不受影响，因为该测试走 saveState/loadState 读取路径，不经过 `withActiveSession`（写入侧）；为补上该锁点，测试 13 特意用带旧 scheduler 元数据的信封并断言 round-trip 后 scheduler 原样保留，E5 复验即双红。E7 漏写 savedAt（spread 继承形态）红测试 12、13——测试 13 红的原因是显式断言 `updated.savedAt === NOW`，而非计划预想的「校验失败返回 invalid」（默认信封 savedAt=0 本身是合法时间戳，round-trip 不会拒收）。
- E6 复验在测试 14 落地后补做（error 路径返回 persist:true → 测试 1、14 双红）。

### 浏览器验证

第八节 10 项全过。关键证据：第 7 词刷新恢复（advocate→…→feasible 评 6 词后刷新，回 hypothesis、7/20、前 6 段按 know/vague/unknown 着色、卡片未翻开）；恢复后继续评完 20 词跳完成页统计 9/6/5 与点击一致；完成页刷新统计仍在（8/7/5）；评完立刻返回再进入停第 4 词 4/20 无重复计分；选择题全程 `localStorage` 零写入且完成页保持 V0 固定 Mock；翻卡↔选择题来回切换会话保位（index 3 不变）；375px 无溢出零控制台错误；`localStorage` 阻断（defineProperty 抛 SecurityError）下 20 词全流程、完成页靠路由 state；`{"schemaVersion":999}` 学习正常且刷新后原值未被覆盖。验证 9 第一轮曾因测试方法（pushState 同路径导航未重挂载组件，页面复用 replay 时缓存的信封正常落盘覆盖了手工篡改值）得到「999 被覆盖」的假阴性——整页 reload 重测后确认为环境操作问题而非产品缺陷，组件存活期间的外部篡改不在本任务范围。

### 与计划的偏差及理由（三处，均经独立 Review 判定合理）

1. **学习页用 `watch(isQuiz, immediate)` 替代计划的 `onMounted`**：setup 期同步恢复消除首帧空窗；修复计划方案的真实回归——首进 `?mode=multiple-choice` 后 onMounted 的 `if (!isQuiz)` 不触发，之后切回 flashcard 组件复用不重挂载、session 恒 null 导致空白页。已有会话时不重启，保持「?mode 切换不重置进度」语义。
2. **完成页第一级只接受 `status === 'completed'`**：D3 的意图即完成页读 completed 会话；若接受 active 会话，saveState 配额降级后「存储 active + 路由 state completed」组合会显示残缺统计（部分 results + 大量 pending 段落）却冠以「本组完成」。
3. **bundle 含 ts-fsrs 片段（计划第九节门禁未满足）**：`useStudySession` chunk 含 `generatorParameters` 与 Rating 枚举，来源是「必须走 loadState」+ `storage.ts → utils/fsrs.ts`（P1-3 冻结值依赖）的结构性后果，非误引入；调度本体未进 bundle。消除它需改 `storage.ts`（本计划明令不许碰），故记录偏差不改。`交接文档.md` 第 5 节 bundle 基线已随之改写。

### Review 结论

独立 L2 Feature Review（独立 agent）：首轮 **APPROVED WITH CONDITIONS**（Critical 0、Important 2、Minor 2）——I-1 status 闸门测试独立锁定（已修复 + E8 反证 + 聚焦复审 APPROVED）、I-2 交接文档 bundle 基线改写（finalization 已执行）；M-1 `advanceTimer` 跨模式泄漏（P2-2 遗留反馈期机制，归 P3，已写入交接文档 P2-4 起点事实）、M-2 反证预期表两处出入（本节已记录）。聚焦复审最终结论 **APPROVED**。

### 最终门禁

`pnpm check` 退出码 0（TypeScript、ESLint、130/130 Node 测试、production build）；`git diff --check` 通过；diff 审计恰 6 个文件 + 自动生成声明，零越界。
