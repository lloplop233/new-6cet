# 任务分层与模型选择指南

> 适用项目：六级词汇背记工具
>
> 目标：在模型和 Agent 工具持续变化时，仍由任务本身稳定决定执行流程，并让结果可验证、可交接。
>
> 本文只规定任务路由、模型选择与协作方式；具体开发顺序见 `项目实施计划.md`。

## 一、唯一主路由：任务风险 L0-L3

先判断任务本身，再选择足够完成任务的模型。模型名称、价格、供应商和当前能力排名都不决定 Workflow。

快速判断只看以下问题：

1. 修改是否局部且边界明确。
2. 是否跨模块、改变数据或状态模型。
3. 是否改变公共接口、存储协议或迁移行为。
4. 错误是否可能污染架构、用户数据、学习进度或安全边界。
5. 是否需要在多个合理方案中做复杂取舍。

边界模糊时选择相邻较高一级，不使用文件数、代码行数或加权分数公式。

| 风险等级 | 典型任务 | Workflow | Review Budget |
|---|---|---|---|
| L0 机械/极低风险 | 文案、图标、token 替换、明确样式、小范围机械修改 | direct implementation → minimal verification → stop | 0 |
| L1 局部普通任务 | 单组件、简单交互、props/emit、局部状态、定位明确的小型 bug | short analysis → implementation → targeted verification → inspect diff → stop | 默认 0 |
| L2 Feature/中等复杂任务 | 完整页面、多组件联动、中等状态、一组相关任务、有业务逻辑的功能 | 必要的简短规划 → 必要时拆分 → implementation → automated verification → feature/batch checkpoint | 每个 Feature/Batch 默认 1 次 Review |
| L3 复杂/高风险任务 | 架构、核心状态、数据持久化、公共接口、迁移、安全、大型重构、核心算法、复杂 bug | deep analysis → plan → implementation → comprehensive verification → independent review → conditional rereview | 1 次 Review；修复后最多 1 次条件复审 |

`L0-L3` 同时决定分析深度、验证范围、Checkpoint 和 Review Budget。不得再建立一套由模型档位决定的 Workflow。

## 二、模型选择：最低充分能力

模型只在 Workflow 已由 `L0-L3` 确定后选择。默认使用当前可用、能够可靠完成任务且成本或延迟更低的模型。

选择时只回答三个问题：

1. 候选模型能否覆盖本任务所需的上下文、文件范围和工具。
2. 候选模型能否可靠完成本任务所需的推理和实现。
3. 是否已经出现失败、不确定性或新的风险证据。

最低能力使用自然语言描述，不建立第二套能力等级代码：

- 局部明确任务：能可靠遵循约束并完成机械或局部实现。
- 中等复杂 Feature：能理解多文件上下文、业务状态和普通方案取舍。
- 高风险任务：能处理架构、复杂状态、跨模块数据流、公共接口和不可逆风险。

同一模型可以在不同时间或不同任务类型上表现不同，因此：

- 不在项目规则中维护具体模型排行榜。
- 不把具体厂商、型号、版本映射为固定职责。
- 不规定某个模型必须规划、实现或 Review。
- 不因为执行模型成本较低就自动增加 Review。
- 认为模型能力不足时，升级执行模型，不让更强模型把已完成工作重新做一遍。

具体模型名称只可作为运行时选择、实际执行记录或历史署名，不得成为长期 Workflow 规则。

## 三、任务分层参考

### L0：机械/极低风险

- 替换文案、favicon、图标引用或已确定 token。
- 修复 import、命名、换行和未使用变量。
- 更新已经验证的任务状态或文件清单。
- 运行明确的检查命令并整理结果。

验收应可机械完成。必要验证通过且 Acceptance Criteria 满足后直接停止，不调用独立 reviewer。

### L1：局部普通任务

- 按已定规格实现单一组件。
- 为纯函数补常规测试。
- 完成局部 props、emit、交互或状态修改。
- 修复定位明确、影响范围局部的 bug。
- 实现接口已经冻结的单一 Store 或 Service 局部逻辑。

完成 targeted verification 并检查 diff 后停止。若发现跨模块影响、公共接口变化或隐藏复杂度，重新判断为 L2/L3。

### L2：Feature/中等复杂任务

- 实现完整页面或一组关联组件。
- 完成多组件联动和中等复杂状态。
- 实现浏览器 TTS、词库接入等有明确边界的 Feature。
- 完成一组共享同一验收目标的相关任务。
- 修复需要理解多个文件但不改变核心架构的 bug。

只有确实需要时才拆分任务。Review 按 Feature/Batch Checkpoint 进行，不为每张子任务单独增加 Review。

### L3：复杂/高风险任务

- 定义领域模型、核心状态模型或公共接口。
- 选择 FSRS 实现、冻结算法边界或修改核心调度逻辑。
- 设计存储协议、迁移、备份恢复和数据覆盖保护。
- 设计跨模块数据流、双模式状态机、PWA 更新和恢复策略。
- 处理安全问题、大型重构或难以定位的复杂 bug。
- 修改 `设计系统.md`、`工程规范.md` 或核心技术选型。

L3 默认使用具备高级复杂推理能力的模型，并按 Review Budget 做一次独立 Review。独立性来自独立上下文和审查职责，不来自特定厂商、型号或更高价格。

## 四、Agent 工具按任务需要使用

任务决定 Workflow，模型提供推理与执行能力，Agent 工具提供读取、编辑、测试、浏览器或研究等操作能力。三者不要混为一谈。

### 1. 探索 Agent

用途：读取目录、搜索引用、梳理依赖和现状。默认只读。

### 2. 编码 Agent

用途：编辑文件、运行检查、修复问题。必须明确允许修改的文件、禁止范围和验收命令。

### 3. 测试 Agent

用途：设计测试矩阵、补测试、运行回归。重点用于 FSRS、队列、存储迁移、备份导入和会话恢复。

### 4. 浏览器与视觉 Agent

用途：运行页面、切换视口、截图、检查交互和布局。重点用于核心首页、学习流程、字体、IPA 和 PWA。

### 5. 审查 Agent

用途：在 Review Budget 允许时独立检查已有改动，重点检查行为回归、遗漏边界、无关改动、缺失测试和规范违规。

### 6. 研究 Agent

用途：需要当前资料时查询官方文档和版本信息，例如 FSRS、浏览器能力、PWA、Vant 或依赖兼容性。不能替代本地代码阅读。

额外 Agent 只在任务确实需要且工作可以独立时使用。任务风险、模型身份或“保险”本身都不能成为增加 Agent 的理由。

## 五、Fast Path、Checkpoint 与 Review

### Fast Path

`L0/L1` 满足以下条件后立即停止：

- 必要的自动化或 targeted verification 通过。
- Acceptance Criteria 满足。
- diff 范围与任务一致。
- 没有新风险或实质性不确定性。

禁止仅因实现模型较弱、较便宜或不是旗舰模型而追加复核。

### Checkpoint Review

- L2 默认每个 Feature/Batch 进行 1 次 Review，不为每张子任务重复 Review。
- L3 进行 1 次独立 Review。
- Checkpoint 由功能边界和风险决定，不由模型身份、任务数量或交接次数决定。

### Conditional Rereview

L3 首次 Review 发现实质性问题并完成修复后，最多追加 1 次聚焦复审。没有实质性发现、只改文案格式或验证已经充分时，不触发复审。

## 六、失败升级，而不是成功复核

出现以下任一证据时，暂停继续猜测，重新判断任务风险并选择更充分的模型：

- 当前模型无法理解任务、约束或关键上下文。
- 同一问题连续出现两次实质性失败。
- targeted tests、类型检查或关键验收持续无法通过。
- 修改从局部扩大为跨模块影响。
- 需要改变公共接口、目录职责、数据或状态模型。
- 涉及用户进度覆盖、迁移、恢复或安全边界。
- 发现文档和代码冲突、难以复现的状态问题或隐藏复杂度。
- 需要在多个合理方案中做长期取舍。
- 当前模型开始增加未要求的抽象或功能。

升级流程：

```text
失败 / 不确定 / 风险上升
→ 保留现有证据
→ 必要时调整 L0-L3
→ 选择更充分的执行模型
→ 继续实现与验证
```

成功流程：

```text
实现成功
→ 验证通过
→ Acceptance Criteria 满足
→ Stop
```

模型升级不能自动增加 Review Budget。

## 七、并行执行规则

可以并行：

- 只读探索与规范扫描。
- 不同文件、无共享接口的展示组件。
- 测试用例设计与不依赖测试结果的文档准备。
- 图标资源处理与业务纯函数开发。

不要并行：

- 领域类型与依赖这些类型的 Store/Service。
- 存储协议与迁移实现。
- 同一页面结构和样式的多路修改。
- FSRS 调度与学习会话状态机。
- 两个 Agent 同时修改 `tokens.less`、`var.less` 或 `AGENTS.md`。

并行任务必须先冻结共享接口。是否并行由任务依赖决定，不由可用模型数量决定。

## 八、任务卡模板

```markdown
# 任务
[一句话目标]

## 风险与流程
- 风险等级：L0 / L1 / L2 / L3
- 最低能力需求：[自然语言描述，不写具体模型]
- Review Budget：[按风险等级填写]

## 开始前阅读
- `AGENTS.md`
- `交接文档.md`
- [与任务直接相关的文档和文件]

## 允许修改
- [明确文件或目录]

## 禁止修改
- [不在范围内的内容]

## 已确定决策
- [不可重新讨论的产品、设计、架构决定]

## 成功标准
- [可观察行为]
- [测试或检查命令]

## 交付要求
- 说明修改文件
- 说明验证结果
- 说明未验证项和风险
```

## 九、任务交接模板

```markdown
## 已完成
- [实际完成内容]

## 修改文件
- `[path]`：[改动目的]

## 验证
- `[command]`：通过/失败

## 关键决策
- [为什么这样实现]

## 未完成或风险
- [下一步必须处理的事项]

## 下一任务与风险
- 下一任务：[任务编号或目标]
- 风险等级：L0 / L1 / L2 / L3
- 最低能力需求：[自然语言描述]
- 必要工具与验证：[按任务填写]
```

## 十、完成与验证责任

- L0/L1：验证通过且 Acceptance Criteria 满足后可以直接宣布完成。
- L2：在 Feature/Batch Checkpoint Review 和必要验证通过后完成。
- L3：在全面验证和独立 Review 通过后完成；只有实质性修复才触发条件复审。
- FSRS、本地数据升级与恢复、备份导入、双模式计分、PWA 更新、真实词库版权边界和真机核心流程都按实际风险执行，不由模型名称决定责任。
- 本地代码、项目文档、测试、构建和真实浏览器行为是最终证据，模型判断不能替代验证。

## 十一、长期规则与运行时选择边界

- 新执行者开始前先读 `AGENTS.md`、`交接文档.md` 和本文件。
- 具体任务从 `项目实施计划.md` 领取，不从聊天记录猜测当前阶段。
- 任何模型或 Agent 都必须遵守最小修改原则，不能自行扩展产品范围。
- 任何模型或 Agent 都不能直接改写 `设计系统.md`、`工程规范.md` 或存储协议，除非任务明确授权且有用户确认。
- 不同模型或 Agent 意见不一致时，回到可验证事实和 Acceptance Criteria，不按模型名或价格决定结论。
- 当前哪个模型更强、更快或更便宜属于运行时资源选择，不写入长期工程知识。
