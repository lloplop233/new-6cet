# 模型与 Agent 协作指南

> 适用项目：六级词汇背记工具  
> 目标：在频繁切换模型和 Agent 工具时，保持任务分配合理、上下文连续、结果可验证。  
> 本文只规定协作方式；具体开发顺序见 `项目实施计划.md`。

## 一、先看结论

不要按“代码多少”判断模型，而要按以下四项判断：

1. 任务是否需要理解多个文件和历史决策。
2. 错误结果是否可能污染数据、架构或用户进度。
3. 是否存在多种合理方案，需要做取舍。
4. 验收是否只能依靠综合判断，而不是机械检查。

满足的项越多，越应该使用强推理模型。

### 模型分级

| 级别 | 定位 | 适合任务 | 不适合任务 |
|---|---|---|---|
| L1 轻量模型 | 快速、低成本、机械执行 | 单文件小改、格式整理、固定模式组件、补类型、运行检查、生成清单 | 架构设计、跨模块调试、数据迁移、复杂交互、最终审查 |
| L2 标准模型 | 日常开发主力 | 边界明确的功能、2～5 个文件联动、常规组件、Store、Service、测试 | 核心领域建模、复杂状态机、疑难故障、重大视觉决策 |
| L3 强推理模型 | 负责高风险决策和收口 | 架构、领域模型、FSRS、存储协议、数据迁移、复杂调试、全局 UI、发布审查 | 大量重复机械修改 |

模型名称会变化，选择时先按能力级别判断，再映射到本项目当前可用的三个模型。模型的实际效果还会受平台、上下文长度、推理档位、工具权限和提示词影响，因此以下是项目分工基线，不是绝对性能排名。

## 二、当前三模型映射

### 推荐总表

| 模型 | 项目角色 | 最适合 | 主要优点 | 主要风险 |
|---|---|---|---|---|
| `DeepSeek V4 Flash` | 执行与批处理模型 | 大量局部编码、测试、扫描、转换、文档整理 | 成本和吞吐友好，支持长上下文、工具调用和结构化输出 | 不适合独自做关键架构取舍；复杂任务需要更严格的验收 |
| `GPT-5.6 Sol` | 主架构与收口模型 | 复杂编码、跨模块调试、产品决策、最终整合 | 对复杂代码、工具使用、研究和多步骤任务更强，适合处理歧义 | 成本和耗时通常更高；不应浪费在重复机械任务上 |
| `Claude Opus 5` | 独立审查与复杂方案模型 | 长上下文审查、Agent 编程、产品体验、风险复核 | 适合复杂 Agent 编程、企业级工作和长文档整体理解 | 不应让它在没有验收标准时自由扩大范围；最终仍需本地检查 |

### 能力级别映射

| 能力级别 | 默认模型 | 备选模型 | 使用说明 |
|---|---|---|---|
| L1 轻量执行 | `DeepSeek V4 Flash` | `GPT-5.6 Sol` 低推理档 | 只做边界明确、可机械验收的工作 |
| L2 日常开发 | `DeepSeek V4 Flash` | `GPT-5.6 Sol` 低/中推理档 | 适合独立功能和局部跨文件修改 |
| L3 强推理 | `GPT-5.6 Sol` | `Claude Opus 5` | 负责架构、算法、数据安全和复杂状态问题 |
| 独立复核 | `Claude Opus 5` | `GPT-5.6 Sol` | 不让实现者单独为高风险结果背书 |

### 本项目的默认调度

推荐采用“`GPT-5.6 Sol` 定方向 → `DeepSeek V4 Flash` 执行 → `Claude Opus 5` 复核”的主链路：

1. `GPT-5.6 Sol` 冻结领域模型、接口、状态边界和验收标准。
2. `DeepSeek V4 Flash` 承担拆分后的局部实现、测试、扫描和文档同步。
3. `Claude Opus 5` 对复杂功能做独立审查，重点寻找遗漏、状态冲突和用户体验问题。
4. 本地命令和真实浏览器结果是最终证据，模型判断不能替代验证。

遇到长文档、全仓库审查或跨阶段交接时，可以让 `Claude Opus 5` 先做独立分析；遇到实现方案冲突时，由 `GPT-5.6 Sol` 负责最终取舍。

## 三、任务分配矩阵

### 适合 L1 轻量模型

这些任务应有明确输入、局部范围和机械验收：

| 工作 | 推荐 Agent 工具 | 必须提供的输入 | 验收方式 |
|---|---|---|---|
| 文件清单、目录盘点 | 只读探索 Agent | 项目根目录、排除目录 | 文件数量和目录树 |
| 修复格式与 Lint 小问题 | 编码 Agent | 指定文件、禁止改业务逻辑 | `pnpm lint` |
| 补充简单 TypeScript 类型 | 编码 Agent | 已确定的数据结构 | `pnpm typecheck` |
| 按既定规格创建单一展示组件 | 编码 Agent | props、样式 token、参考组件 | 组件检查和截图 |
| 替换 favicon、文案、图标引用 | 编码 Agent | 明确资源和目标文件 | 构建与人工目检 |
| 为纯函数补常规单元测试 | 测试 Agent | 函数行为和边界表 | 指定测试通过 |
| 执行规范扫描、产出报告 | 审查 Agent | 检查命令和规则 | 输出可复现 |
| 更新交接文档中的已完成状态 | 文档 Agent | 已验证结果 | 与代码状态一致 |

典型例子：

- 创建 `Eyebrow.vue`、`Tag.vue` 这类规格已经锁定的组件。
- 把页面里的重复结构替换为已经存在的组件。
- 修正 import、命名、换行、未使用变量。
- 运行类型检查、Lint、构建并整理错误。

### 适合 L2 标准模型

这些任务需要理解局部业务，但边界仍然清楚：

| 工作 | 推荐 Agent 工具 | 需要关注 |
|---|---|---|
| 实现单个 Store 或 Service | 编码 Agent | 职责边界、持久化副作用、类型契约 |
| 实现一组关联组件 | 编码 Agent + 浏览器 Agent | 状态完整、复用规则、移动端适配 |
| 完成一个独立页面 | 编码 Agent + 浏览器 Agent | 页面流程、空状态、错误状态 |
| 实现 JSON 导出 | 编码 Agent + 测试 Agent | 文件格式、版本号、异常处理 |
| 接入 TTS | 编码 Agent + 真机/浏览器 Agent | 浏览器兼容、停止与重复播放 |
| 接入已清洗的词库 | 编码 Agent + 数据检查 Agent | 字段映射、懒加载、版权边界 |
| 修复定位明确的跨文件 Bug | 编码 Agent | 复现步骤、最小修复、回归测试 |

L2 任务应该由 L3 模型先确定接口时再执行，尤其是 Store、Service 和持久化相关工作。

### 必须优先使用 L3 强推理模型

| 工作 | 原因 | 推荐 Agent 工具 |
|---|---|---|
| 定义 `Word`、`ReviewState`、`StudySession` 等领域模型 | 会影响所有后续模块 | 架构/编码 Agent |
| 决定 FSRS 库、评分映射和调度边界 | 算法错误会直接破坏复习效果 | 强推理编码 Agent + 测试 Agent |
| 设计本地存储结构、版本和迁移 | 错误可能导致用户进度丢失 | 架构 Agent + 审查 Agent |
| 设计双模式共享状态机 | 容易出现重复计分、跳题和恢复错误 | 强推理编码 Agent |
| 复杂故障根因分析 | 需要跨配置、运行时和依赖定位 | 调试 Agent |
| 首页整体信息架构和核心交互 | 是产品主要体验，不是单一组件 | UI 设计 Agent + 浏览器 Agent |
| PWA 更新、离线和恢复策略 | 涉及缓存、数据安全和浏览器生命周期 | 强推理编码 Agent |
| 发布前全局审查 | 需要综合业务、工程、性能和无障碍 | 审查 Agent + 浏览器 Agent |
| 修改 `设计系统.md`、`工程规范.md` 或核心技术选型 | 会改变全项目约束 | 强推理模型，需用户确认 |

## 四、Agent 工具怎么分工

模型决定“思考能力”，Agent 工具决定“能做什么”。两者不要混为一谈。

### 1. 探索 Agent

用途：读取目录、搜索引用、梳理依赖和现状。默认只读。

适合：每个任务开始前建立事实基础。  
不适合：没有授权时直接修改文件。

### 2. 编码 Agent

用途：编辑文件、运行检查、修复问题。

要求：必须明确允许修改的文件、禁止触碰的范围和验收命令。

### 3. 测试 Agent

用途：设计测试矩阵、补测试、运行回归。

重点用于：FSRS、队列生成、存储迁移、备份导入、会话恢复。

### 4. 浏览器与视觉 Agent

用途：运行页面、切换视口、截图、检查交互和布局。

重点用于：核心首页、翻卡、选择题、底部操作区、字体与 IPA、PWA。

### 5. 审查 Agent

用途：独立检查已有改动，不负责为自己的实现背书。

重点检查：行为回归、遗漏边界、无关改动、缺失测试、规范违规。

### 6. 研究 Agent

用途：需要当前资料时查询官方文档和版本信息。

仅在这些情况使用：

- FSRS 候选库和 API 发生变化。
- 浏览器 TTS、PWA 或 Vant 行为需要核实。
- 依赖升级或兼容性判断需要近期数据。

不要让研究 Agent 替代本地代码阅读。

## 五、推荐的协作流水线

对于高风险功能，使用以下四段式：

1. **L3 设计**：定义目标、接口、数据结构、边界和验收标准。
2. **L1/L2 实现**：按拆分后的任务卡完成局部编码。
3. **L1 机械验证**：运行类型检查、Lint、单元测试和构建。
4. **L3 独立审查**：检查是否满足产品目标及是否存在系统性风险。

对于低风险小改，可以缩短为：

1. L1 实现。
2. L1 验证。
3. 用户或 L2 快速复核。

## 六、什么时候必须升级模型

执行中遇到以下任一情况，停止让轻量模型继续猜测，升级到 L3：

- 同一问题连续修复两次仍失败。
- 需要修改超过 5 个业务文件。
- 需要改变已有接口、目录或数据结构。
- 发现文档和代码互相冲突。
- 涉及用户进度覆盖、迁移或恢复。
- 出现难以稳定复现的状态问题。
- 需要在多个合理方案中做长期取舍。
- 轻量模型开始增加未要求的抽象或功能。

## 七、并行执行规则

可以并行：

- 只读探索与规范扫描。
- 不同文件、无共享接口的展示组件。
- 测试用例设计与文档更新。
- 图标资源处理与业务纯函数开发。

不要并行：

- 领域类型与依赖这些类型的 Store/Service。
- 存储协议与迁移实现。
- 同一页面结构和样式的多路修改。
- FSRS 调度与学习会话状态机。
- 两个 Agent 同时修改 `tokens.less`、`var.less` 或 `AGENTS.md`。

并行任务必须先冻结共享接口，再分发工作。

## 八、任务卡模板

每次交给新模型时，尽量提供以下内容：

```markdown
# 任务
[一句话目标]

## 开始前阅读
- `AGENTS.md`
- `交接文档.md`
- [与任务直接相关的文档和文件]

## 允许修改
- [明确文件或目录]

## 禁止修改
- [不在范围内的内容]

## 已确定决策
- [不可重新讨论的产品、设计、架构决定]

## 成功标准
- [可观察行为]
- [测试或检查命令]

## 交付要求
- 说明修改文件
- 说明验证结果
- 说明未验证项和风险
```

## 九、模型交接模板

一个模型完成后，必须留下可被下一个模型直接消费的交接信息：

```markdown
## 已完成
- [实际完成内容]

## 修改文件
- `[path]`：[改动目的]

## 验证
- `[command]`：通过/失败

## 关键决策
- [为什么这样实现]

## 未完成或风险
- [下一步必须处理的事项]

## 建议下一模型
- L1 / L2 / L3
- 原因：[任务风险和上下文复杂度]
```

## 十、提交前责任划分

轻量模型可以执行检查，但不能单独宣布高风险功能“已完成”。以下任务必须由 L3 或独立审查 Agent 最终确认：

- FSRS 正确性。
- 本地数据升级与恢复。
- 备份导入不会静默覆盖数据。
- 双模式不会重复计分。
- PWA 更新不会破坏当前学习状态。
- 真实词库字段和版权边界正确。
- 核心页面在真实手机上可用。

## 十一、本项目的默认选择

- 日常默认使用 `DeepSeek V4 Flash` 执行 L1/L2 任务。
- 涉及架构、状态、算法、数据安全和全局体验时使用 `GPT-5.6 Sol`。
- 长上下文独立审查、交接复核和复杂 Agent 编程优先使用 `Claude Opus 5`。
- 单文件机械任务使用 `DeepSeek V4 Flash`，不要占用强模型。
- 新模型接手前先读 `AGENTS.md`、`交接文档.md` 和本文件。
- 具体任务从 `项目实施计划.md` 领取，不从聊天记录猜测当前阶段。

## 十二、模型使用边界

- `DeepSeek V4 Flash` 的输出必须经过本地类型检查、Lint、测试或构建；不能因为代码量大就跳过验收。
- `GPT-5.6 Sol` 负责高风险取舍，但也必须遵守最小修改原则，不能自行扩展产品范围。
- `Claude Opus 5` 适合发现问题，不应默认拥有修改全部文件的权限；审查任务优先只读。
- 三个模型都不能直接改写 `设计系统.md`、`工程规范.md` 或存储协议，除非任务明确授权且有用户确认。
- 如果三个模型意见不一致，优先回到可验证事实：现有代码、项目文档、测试结果和真实浏览器行为。

## 十三、资料来源

- [OpenAI Codex Models](https://developers.openai.com/codex/models/)：`GPT-5.6 Sol` 的官方定位和推理档位说明。
- [DeepSeek Models & Pricing](https://api-docs.deepseek.com/quick_start/pricing)：`DeepSeek V4 Flash` 的模型能力、上下文和工具支持说明。
- [Anthropic Models Overview](https://docs.anthropic.com/en/docs/about-claude/models/overview)：`Claude Opus 5` 的官方定位和能力说明。
