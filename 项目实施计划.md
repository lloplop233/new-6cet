# 项目实施计划

> 本计划将项目拆成具有独立验收标准的任务阶段。
>
> 风险等级、Workflow、Review Budget 和模型选择规则见 `模型与Agent协作指南.md`。
>
> 状态标记：`[ ]` 未开始、`[~]` 进行中、`[x]` 已验证完成、`[!]` 阻塞。

## 任务路由速查

- L0：机械或极低风险任务，直接实现和最小验证，Review Budget 为 0。
- L1：局部普通任务，短分析、定向验证和 diff 检查，Review Budget 默认 0。
- L2：Feature 或中等复杂任务，按需规划和拆分，每个 Feature/Batch 默认 1 次 Checkpoint Review。
- L3：复杂或高风险任务，深入分析、全面验证和 1 次独立 Review；实质性修复后最多 1 次条件复审。
- 模型在风险和 Workflow 确定后按最低充分能力运行时选择；具体型号不进入任务定义，也不能改变 Review Budget。
- 任何模型都不能替代本地命令、浏览器和真机验证。

## 一、总目标

交付一个移动端 H5 六级词汇背记工具，支持：

- 翻卡自评与选择题辨义双模式。
- FSRS 三档评分调度。
- 本地持久化和 JSON 备份恢复。
- 浏览器 TTS。
- 5651 词词库按词频使用。
- PWA 安装和离线使用。

不包含后端、账号、云同步、社交、排行榜和其他考试模块。

## 二、阶段依赖

```text
P0 工程基线
  -> P1 领域模型与存储协议
  -> P2 模拟数据垂直闭环
  -> P3 核心组件与正式界面
  -> P4 FSRS 与双模式
  -> P5 持久化、备份与恢复
  -> P6 真实词库
  -> P7 字体、TTS 与 PWA
  -> P8 全面验收与发布准备
```

任何阶段不得用“页面看起来完成”替代前置阶段的行为验收。

## 三、P0 工程基线

目标：所有后续模型都从可检查、可构建、可回退的基线开始。

### P0-1 修复类型检查阻断

- 状态：`[x]`
- 风险等级：L0；若发现跨文件影响则重新判断为 L1/L2。
- 推荐工具：编码 Agent。
- 任务：处理 `src/router/index.ts` 引用缺失的 `./types`。
- 允许范围：路由类型和相关 import。
- 验收：`pnpm typecheck` 通过。

### P0-2 收敛 Lint 范围和规则

- 状态：`[x]`
- 风险等级：L1。
- 推荐工具：编码 Agent。
- 任务：区分源码错误与文档格式噪声，明确 `tokens.less` 的格式规则。
- 约束：不要用全局自动修复改写设计文档内容。
- 验收：`pnpm lint` 输出可行动，源码无错误。

### P0-3 初始化 Git 基线

- 状态：`[x]`
- 风险等级：L1。
- 推荐工具：Git Agent 或编码 Agent。
- 任务：检查 `.gitignore`，执行 `git init`，建立首个基线 commit。
- 前提：必须由用户明确要求提交。
- 验收：工作区状态明确，`node_modules`、`dist` 不入库。

### P0-4 建立质量命令

- 状态：`[x]`
- 风险等级：L1。
- 推荐工具：编码 Agent。
- 任务：确保类型检查、Lint、测试和生产构建有稳定命令。
- 验收：命令名称写入 `package.json` 和交接文档。

## 四、P1 领域模型与存储协议

目标：先定义业务真相，再写页面。

### P1-1 定义领域类型

- 状态：`[x]`
- 风险等级：L3。
- 推荐工具：架构/编码 Agent。
- 产物建议：`src/types/word.ts`、`review.ts`、`study.ts`、`settings.ts`、`backup.ts`。
- 至少定义：`Word`、`ReviewState`、`Rating`、`StudyMode`、`StudySession`、`Settings`、`BackupEnvelope`。
- 验收：字段语义、可选性、时间单位和唯一标识明确。
- 验证说明：分项类型检查、Lint、测试、生产构建和工程规范扫描已通过；宿主供应链联网校验阻断了 `pnpm check` 取得整体退出码，详见 `P1交接文档.md`。

### P1-2 选择 FSRS 实现策略

- 状态：`[ ]`
- 风险等级：L3。
- 推荐工具：研究 Agent + 编码 Agent。
- 原则：优先成熟库，不手写核心算法。
- 需要确认：三档评分到 FSRS 等级的映射、时间处理、初始化参数和序列化字段。
- 验收：形成短决策记录和最小验证用例。

### P1-3 设计存储协议

- 状态：`[ ]`
- 风险等级：L3。
- 推荐工具：架构/编码 Agent；独立 Review 按 L3 Review Budget 执行。
- 内容：存储键、`schemaVersion`、默认值、迁移、损坏恢复、写入失败处理。
- 约束：词库本体不重复存进 `localStorage`。
- 验收：能够解释升级、降级和异常数据会发生什么。

### P1-4 冻结模块接口

- 状态：`[ ]`
- 风险等级：L3。
- 推荐工具：架构 Agent。
- 内容：明确 `utils`、`services`、`stores`、`composables` 的调用方向。
- 验收：后续 L1/L2 任务无需重新设计接口即可实施。

## 五、P2 模拟数据垂直闭环

目标：用 20 个模拟词打通一次完整学习，不追求最终视觉。

### P2-1 模拟词库与队列生成

- 状态：`[ ]`
- 风险等级：L1。
- 推荐工具：编码 Agent + 测试 Agent。
- 内容：固定模拟词、今日队列、当前位置和结束状态。
- 验收：队列纯函数测试通过，无重复和越界。

### P2-2 翻卡自评最小流程

- 状态：`[ ]`
- 风险等级：L2。
- 推荐工具：编码 Agent + 浏览器 Agent。
- 流程：展示单词 -> 翻开释义 -> 三档评分 -> 下一词 -> 完成总结。
- 验收：刷新前后不会出现重复计分或跳词。

### P2-3 最小会话恢复

- 状态：`[ ]`
- 风险等级：L2；若发现跨模式状态或数据恢复边界，重新判断为 L3。
- 推荐工具：编码 Agent + 测试 Agent。
- 内容：保存当前队列、位置和已评分结果。
- 验收：中途刷新后恢复到正确位置。

### P2-4 垂直闭环审查

- 状态：`[ ]`
- 风险等级：L2 Feature/Batch Checkpoint。
- 推荐工具：独立审查 Agent。
- 验收：确认状态边界可支持后续选择题和 FSRS，不需要推翻重写。

## 六、P3 核心组件与正式界面

目标：在已验证的行为上落实设计系统。

### P3-1 通用 UI 组件

- 状态：`[ ]`
- 风险等级：L1；无共享接口的组件可并行，但不能并行修改共享 token。
- 推荐工具：编码 Agent。
- 组件：`Eyebrow.vue`、`StatCard.vue`、`StatNumber.vue`、`Tag.vue`、`SegmentToggle.vue`。
- 验收：props 不超过 6 个，无字面样式值，组件内部不写 `margin`。

### P3-2 词汇业务组件

- 状态：`[ ]`
- 风险等级：L2。
- 推荐工具：编码 Agent + 浏览器 Agent。
- 组件：`RateButtons.vue`、`QueueBar.vue`，以及经实际重复证明需要的词卡组件。
- 验收：评分状态、禁用状态和触控尺寸完整。

### P3-3 首页正式布局

- 状态：`[ ]`
- 风险等级：L2；若需要改变全局设计方向或公共状态接口，重新判断为 L3。
- 推荐工具：编码 Agent + 浏览器/视觉 Agent。
- 内容：首页统计、模式切换、词卡、队列条和拇指区操作。
- 约束：保持 `设计系统.md` 已锁定方向，不重新设计品牌色和签名元素。
- 验收：至少检查 320、375、430、600px 视口及真实手机。

### P3-4 状态与无障碍补齐

- 状态：`[ ]`
- 风险等级：L2。
- 推荐工具：浏览器 Agent。
- 内容：空状态、加载、禁用、错误、键盘焦点、ARIA、减少动效。
- 验收：核心流程不依赖颜色传递唯一信息。

## 七、P4 FSRS 与双模式

目标：接入真实调度逻辑，并让两种学习模式共享统一计分核心。

### P4-1 FSRS 适配层

- 状态：`[ ]`
- 风险等级：L3。
- 推荐工具：编码 Agent + 测试 Agent。
- 内容：隔离第三方库 API，提供项目内部稳定接口。
- 验收：固定时间输入能产生可重复结果；边界测试通过。

### P4-2 三档评分映射

- 状态：`[ ]`
- 风险等级：L1；只实现 P1 已冻结映射，若要改变映射决策则重新判断为 L3。
- 内容：认识 -> Good，模糊 -> Hard，不熟 -> Again。
- 验收：映射只存在一个来源，UI 不直接调用第三方库。

### P4-3 选择题辨义模式

- 状态：`[ ]`
- 风险等级：L2；若需要重新设计公共评分或状态边界则重新判断为 L3。
- 推荐工具：编码 Agent + 测试 Agent + 浏览器 Agent。
- 内容：题目、选项、正确/错误反馈、评分映射、下一题。
- 验收：同一单词一次会话只计分一次，答案分布无明显位置偏差。

### P4-4 双模式状态机审查

- 状态：`[ ]`
- 风险等级：L3。
- 推荐工具：独立审查 Agent。
- 重点：模式切换、中途退出、快速连点、恢复和完成边界。

## 八、P5 持久化、备份与恢复

目标：用户进度可持续、可迁移、可恢复。

### P5-1 Settings 与 Progress Service

- 状态：`[ ]`
- 风险等级：L3；接口必须来自 P1。
- 推荐工具：编码 Agent + 测试 Agent。
- 验收：读取、写入、默认值和失败分支均有测试。

### P5-2 数据迁移框架

- 状态：`[ ]`
- 风险等级：L3。
- 推荐工具：编码 Agent + 测试 Agent。
- 验收：至少有一条模拟旧版本到新版本的迁移测试。

### P5-3 JSON 导出与导入

- 状态：`[ ]`
- 风险等级：L3；Review 按 L3 Review Budget 执行，不拆成模型档位接力。
- 内容：导出版本、时间、设置和进度；导入前校验并备份旧数据。
- 验收：错误文件不会覆盖现有数据；成功导入结果可复现。

### P5-4 异常恢复

- 状态：`[ ]`
- 风险等级：L3。
- 内容：JSON 损坏、字段缺失、存储满、写入中断。
- 验收：用户得到明确提示，且存在保守恢复路径。

## 九、P6 真实词库

目标：在不改变业务接口的前提下接入旧项目词库。

### P6-1 词库审查和字段映射

- 状态：`[ ]`
- 风险等级：L3。
- 推荐工具：数据检查 Agent。
- 约束：旧项目只读；只处理允许使用的词、音标和释义。
- 验收：字段定义、数量、重复项、缺失项和版权边界有记录。

### P6-2 词库转换与校验

- 状态：`[ ]`
- 风险等级：L2。
- 推荐工具：编码 Agent + 数据检查 Agent。
- 验收：总数、唯一词数、字段缺失、排序和抽样检查均通过。

### P6-3 懒加载与体积控制

- 状态：`[ ]`
- 风险等级：L2。
- 推荐工具：性能 Agent + 编码 Agent。
- 验收：词库不阻塞首屏；记录构建产物和加载时间。

## 十、P7 字体、TTS 与 PWA

目标：完成移动端实际使用所需的浏览器能力。

### P7-1 字体接入与 IPA 验证

- 状态：`[ ]`
- 风险等级：L2。
- 推荐工具：编码 Agent + 浏览器/视觉 Agent。
- 内容：衬线、无衬线、等宽字体；必要时给 IPA 单独字体。
- 验收：字体加载失败有回退，IPA 抽样字形完整，无明显布局跳动。

### P7-2 TTS Service

- 状态：`[ ]`
- 风险等级：L2。
- 推荐工具：编码 Agent + 真机浏览器 Agent。
- 验收：播放、停止、切词取消、无可用语音和不支持状态均正确。

### P7-3 favicon 与 PWA 图标

- 状态：`[ ]`
- 风险等级：L0。
- 推荐工具：图像/编码 Agent。
- 验收：青墨配色、maskable 安全区、安装后图标显示正确。

### P7-4 离线与更新策略

- 状态：`[ ]`
- 风险等级：L3。
- 推荐工具：编码 Agent + 浏览器 Agent。
- 验收：首次在线后可离线打开；更新不会静默丢失当前会话。

## 十一、P8 全面验收与发布准备

目标：证明产品可用，而不是仅证明代码能编译。

### P8-1 自动化质量门禁

- 状态：`[ ]`
- 风险等级：L0。
- 验收：`typecheck`、Lint、测试、生产构建全部通过。

### P8-2 核心流程端到端检查

- 状态：`[ ]`
- 风险等级：L2。
- 推荐工具：浏览器 Agent。
- 场景：首次使用、翻卡、选择题、刷新恢复、导出、导入、离线启动。

### P8-3 性能和无障碍审查

- 状态：`[ ]`
- 风险等级：L2 Feature/Batch Checkpoint。
- 推荐工具：审查 Agent + 浏览器 Agent。
- 验收：触控尺寸、对比度、焦点、长文本、减少动效、首屏和包体预算。

### P8-4 真机验收

- 状态：`[ ]`
- 风险等级：L2；用户参与确认。
- 场景：iOS Safari、Android Chrome、PWA 安装、后台恢复、音频播放。

### P8-5 最终交接

- 状态：`[ ]`
- 风险等级：L1；只基于已验证事实更新文档，不因执行模型追加复核。
- 内容：更新 `交接文档.md`，记录版本、命令、已知限制和后续事项。

## 十二、每次领取任务的规则

1. 只领取一个具有独立验收标准的任务卡。
2. 开始前阅读 `AGENTS.md`、`交接文档.md`、`模型与Agent协作指南.md`。
3. 高风险任务按 L3 Workflow 先冻结接口和验收标准，再实施；不按模型档位拆成固定接力链。
4. 完成后立即更新状态，但只有验证通过才能标记 `[x]`。
5. 发现前置设计错误时回退到上一阶段，不在下游打补丁掩盖。
6. 每次交付使用 `模型与Agent协作指南.md` 中的交接模板。

## 十三、当前推荐起点

P0-1～P0-4、P1-1 已验证完成。当前顺序是：

1. 从风险等级 L3 的 P1-2 开始逐张领取任务卡，依次冻结 FSRS、存储和模块接口；运行时选择满足任务最低能力要求的模型。
