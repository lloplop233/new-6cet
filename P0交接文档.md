# P0 工程基线 — 模型交接文档

> 初始交接人：DeepSeek V4 Flash（L1/L2 执行模型）
> P0-3、P0-4 补充：Codex（GPT-5.6 Sol）
> 最后更新：2026-08-30
> 覆盖任务：`项目实施计划.md` 的 P0-1～P0-4
> 接手阅读顺序：`AGENTS.md` → `交接文档.md` → 本文 → `项目实施计划.md`

## 一、任务卡与验收

| 任务 | 验收标准 | 结果 |
|---|---|---|
| P0-1 修复类型检查阻断 | `pnpm typecheck` 通过 | ✅ 通过 |
| P0-2 收敛 Lint 范围和规则 | `pnpm lint` 输出可行动、源码无错误 | ✅ 通过 |
| P0-3 初始化 Git 基线 | 忽略构建产物，建立首个基线 commit | ✅ 通过 |
| P0-4 建立质量命令 | 类型检查、Lint、测试、生产构建有稳定命令 | ✅ 通过 |

## 二、已完成

- P0-1：`pnpm typecheck` 通过，类型检查阻断已解除。
- P0-2：`pnpm lint` 通过（0 error / 0 warning）；文档格式噪声与源码错误已分离。
- P0-3：确认 `node_modules`、`dist` 和本地 `.impeccable/` 报告不入库，在 `main` 建立首个基线 commit。
- P0-4：新增 Node 内置测试运行器、3 个 `round` 测试和 `pnpm check` 聚合质量门禁。
- `pnpm build:dev` 和 `pnpm build:pro` 均已通过，产物生成于 `dist/`。

## 三、修改文件

- `src/router/index.ts`：删除 `import type { EnhancedRouteLocation } from './types'` 及 `beforeEach` 回调上的该类型标注，`to` 改由 `vue-router` 守卫签名推断。
- `eslint.config.ts`：
  - 工厂函数顶层选项新增 `ignores: ['**/*.md']`，设计文档不再参与 lint / 自动格式化；
  - 新增独立配置块，对 `src/styles/tokens.less`、`src/styles/var.less` 关闭 `format/prettier`；
  - 仅对测试文件关闭强制使用 Vitest 的 `test/no-import-node-test` 规则。
- `package.json`：`lint:fix` 自动调整 key 顺序，并新增 `test` 和 `check` 命令；未新增依赖。
- `src/pages/tokens/index.vue`：`lint:fix` 自动修复格式，并移除 `size="20px"` 和 `letter-spacing: 0.08em` 两处字面样式值。
- `src/styles/tokens.less`、`src/styles/var.less`：**未改动**（时间戳保持原状，符合预期）。
- `.gitignore`：忽略本地 `.impeccable/` 审查产物。
- `src/utils/round.test.mjs`：新增 Node 内置测试运行器冒烟测试，覆盖默认精度、小数精度和负精度。
- `项目实施计划.md`：将已验证的 P0-1～P0-4 标记为 `[x]`，同步当前推荐起点。
- `交接文档.md`：同步 P0 状态、质量命令和正确的后续阶段顺序。

## 四、验证

| 命令 | 结果 |
|---|---|
| `pnpm typecheck` | 通过 |
| `pnpm lint` | 通过，0 问题 |
| `pnpm test` | 通过，3/3 测试通过 |
| `pnpm build:dev` | 通过 |
| `pnpm build:pro` | 通过 |
| `pnpm check` | 通过，依次完成类型检查、Lint、测试和生产构建 |
| 工程规范机械扫描 | 通过：Vant 变量、`.van-*`、页面/组件 hex、`src/api` 均无违规 |

## 五、关键决策

1. **不新建 `src/router/types.ts`**：工程规范 1.1 规定 `router/` 目录只放路由实例与守卫、不放类型；且 `src/types/vue-router.d.ts` 已增强 `RouteMeta.keepAlive`，`EnhancedRouteLocation` 原标注本就冗余，删掉即可。
2. **antfu v9 的配置返回对象是 thenable，不能 `...spread`**：曾尝试 `export default [{ ignores }, ...antfu(...)]` 报 "not iterable"；正确写法是用工厂函数顶层 `ignores` 选项。
3. **`tokens.less` / `var.less` 不参与 prettier**：两文件是手写设计数值源与 Vant 映射层，注释对齐、大写 hex 与 `设计系统.md` 一致；若被 prettier 格式化会变成小写 hex 并打散对齐，故单独关闭 `format/prettier`（其余 lint 规则仍生效）。
4. **排除 `*.md` 后才跑 `lint:fix`**：遵守 P0-2「不要用全局自动修复改写设计文档内容」的约束。
5. **P0 测试使用 Node 内置运行器**：项目最低 Node 版本为 22.23，能够直接运行可擦除类型的 TypeScript；当前纯函数测试无需额外测试依赖。后续若需要 Vue 组件或浏览器环境测试，再按具体任务评估 Vitest，不在 P0 提前扩展。
6. **测试文件使用 `.test.mjs`**：运行时可显式导入 `round.ts`，同时避免为测试修改全局 TypeScript import 规则。

## 六、未完成或风险

- 当前未配置 Git 远端，本次只建立本地基线 commit，未执行 push。
- `pnpm build:pro` 仍会输出既有的 `.env.production` 中 `NODE_ENV=production` 提示，以及 PostCSS 插件未传 `from` 的警告；构建成功，本任务未扩大范围处理这两项。
- 副作用：`*.md` 不参与 Lint / 格式检查，文档格式靠人工维护，与「不自动改写设计文档」的约束一致。

## 七、历史后续记录

- 当时的下一任务是风险等级 L3 的 P1-1；该任务现已完成。当前任务路由以 `AGENTS.md`、`模型与Agent协作指南.md` 和 `项目实施计划.md` 为准。
