# 工程规范 — 六级词汇工具

日期：2026-08-26
适用范围：本项目 `src/` 下全部源码。
效力：规范优先于个人习惯。与本文冲突的写法即为待修项，不需要再讨论"这样写行不行"。

---

## 零、先澄清一个词：本项目没有"接口"

你说的四类里有"接口"。这个词在通常项目里指 HTTP API（前端调后端），但**我们这个项目没有后端** — 纯前端单机，词库是本地文件，进度存在浏览器里。

所以 `src/api/` 这个目录不建（模板自带的那个要删）。你说的"接口"在我们这里对应的是**数据访问层**：所有和外部世界打交道的代码。它归入 `src/services/`，第 1.2 节有定义。

如果你说的"接口"其实指 TypeScript 的 `interface`（类型定义），那对应的是 `src/types/`，见第 1.4 节。两个都定了，不管你指哪个都有归处。

---

## 一、文件归位

### 1.1 目录树（定死，不许新增顶层目录）

```
src/
├── pages/          页面 — 一个路由一个目录，文件路由自动生成
├── components/
│   ├── layout/     外壳件：只出现在 App.vue 或路由外层
│   ├── ui/         通用件：无业务语义，换个项目也能用
│   └── word/       业务件：名字里带产品名词的
├── composables/    组合式函数 — 有响应式状态
├── services/       数据访问层 — 碰外部世界的
├── stores/         Pinia 全局状态
├── utils/          纯函数 — 无状态、不碰外部世界
├── types/          跨模块共享的类型
├── styles/         全局样式与设计 token
├── constants/      常量
├── router/         路由实例与全局守卫（框架必需，只有一个 index.ts）
└── assets/         参与打包的静态资源（图片、字体）
```

新增顶层目录需要你点头。子目录同理：`components/` 下就这三个，不许出现第四个。

最后两个目录（`router/` `assets/`）是骨架自带且不可省的，初稿写树时漏了，这里补齐 — 不是新增。`router/` 下只放路由实例和守卫，不放类型、不放页面清单；页面标题之类的常量属于 `constants/`。

### 1.2 每个目录收什么

判定标准全部写成"能一眼看出对错"的形式，不用"应该""建议"这类词。

| 目录 | 收什么 | 判定标准（机械可判） | 举例 |
|---|---|---|---|
| `pages/` | 页面 | 出现在路由表里 | `pages/study/index.vue` |
| `components/layout/` | 外壳件 | 只被 `App.vue` 或路由外层用 | `TabBar.vue` `NavBar.vue` |
| `components/ui/` | 通用件 | 名字里**没有**产品名词，删掉本项目也能用 | `Eyebrow.vue` `StatCard.vue` |
| `components/word/` | 业务件 | 名字里**有**产品名词（词/卡/评分/释义/例句/队列） | `WordCard.vue` `RateButtons.vue` |
| `composables/` | 组合式函数 | 文件里 `import` 了 `vue` | `useQueue.ts` |
| `services/` | 数据访问 | 碰浏览器 API、本地文件或存储 | `words.ts` `progress.ts` |
| `stores/` | 全局状态 | 跨页面共享且要持久化 | `settings.ts` |
| `utils/` | 纯函数 | 同样输入必得同样输出，没有副作用 | `shrinkFontSize.ts` |
| `types/` | 共享类型 | 被两个以上文件 `import` | `word.ts` |
| `constants/` | 常量 | 全大写命名的固定值 | `RATING.KNOW` |

### 1.3 三条最容易搞错的边界

这三条是实际会反复纠结的地方，各给一个一句话就能判的标准。

**`utils/` 还是 `composables/`** — 看文件里有没有 `import { ref, computed, watch, onMounted } from 'vue'`。有，放 `composables/`；没有，放 `utils/`。不看功能，只看这一行。

**`utils/` 还是 `services/`** — 看它有没有碰外部世界：`localStorage`、`fetch`、动态 `import()` 词库分片、`speechSynthesis`、`Date.now()`。碰了任何一个就是 `services/`。判据是"同样的输入会不会得到不同的结果"：会，就不是纯函数。

**`ui/` 还是 `word/`** — 看组件名。含"词、卡、评分、释义、例句、队列、音标"这类产品名词的进 `word/`，其余进 `ui/`。`StatCard`（数据卡外壳）是 `ui/`，`WordCard`（词卡）是 `word/`，虽然两个都叫 Card。

### 1.4 命名

| 类型 | 规则 | 例 |
|---|---|---|
| 组件文件 | 大驼峰，名词或名词短语 | `WordCard.vue` |
| 页面目录 | 全小写短横线，目录名即路由 | `pages/study/index.vue` → `/study` |
| composable | `use` + 大驼峰 | `useQueue.ts` |
| service / util | 小驼峰文件名，一个文件一个主题 | `progress.ts` `speech.ts` |
| 类型文件 | 小写，**用 `.ts` 不用 `.d.ts`** | `types/word.ts` |
| 常量 | 全大写下划线，成组的用对象 | `RATING`、`SHARD_SIZE` |

类型文件必须用 `.ts`：`src/types/` 下已经有模板自动生成的 `.d.ts`（`auto-imports` `components` `route-map` `env` 等），手写的用 `.ts` 才能一眼分清哪些是自动生成的、哪些是我们写的。自动生成的那几个永远不要手改。

**只被一个文件用到的类型，写在那个文件里，不进 `src/types/`。** `types/` 的门槛就是 1.2 表里那条"被两个以上文件 `import`"。为一个只有一处使用的类型单开文件，是把一行代码换成一个文件加一条 import。

### 1.5 新文件落地流程

写新文件之前按顺序回答三个问题，第一个能回答"是"的就是它的归处：

1. 它有 DOM 结构吗 → `components/` 的三个子目录之一（按 1.3 判）
2. 它 `import` 了 `vue` 吗 → `composables/`
3. 它碰外部世界吗 → `services/`；都不是 → `utils/`

回答不了，说明这个文件承担了两件事，**先拆开再放**。一个文件承担两种职责就是它没有归处的原因，不是规范不够细。

---

## 二、组件复用

### 2.1 第二次出现就封装

常见的说法是"三次法则"（同样的东西出现三次才抽）。**本项目改成第二次就抽**，理由是我们的页面总数不到十个，同一段结构真出现两次，第三次几乎必然会来；而且这个项目只有你一个人维护三个月以上，复制粘贴的副本会在你忘记它存在之后才暴露问题。

判定"同一个东西"看两条，两条都满足才算重复：

1. **DOM 结构相同** — 标签层级和数量一致，只有文字或数值不同
2. **语义相同** — 它们在产品里说的是同一件事

只满足第 1 条的不算。比如"三档评分按钮"和"选择题的四个选项"都是"一排带边框的可点区块"，结构像，但一个是自评、一个是答题，语义不同，未来会各自分叉 — 这种**不许合并**，见 2.3。

### 2.2 抽成什么

重复的东西不是一律抽成组件。按重复的是什么来定：

| 重复的是 | 抽成 | 放哪 |
|---|---|---|
| DOM 结构 + 样式 | 组件 | `components/` |
| 只有逻辑，带响应式状态 | composable | `composables/` |
| 只有计算，无状态 | 纯函数 | `utils/` |
| **只有样式，结构不同** | CSS 类或 token | `styles/` |

最后一行最容易做错：看到两处颜色字号一样就抽个组件出来包住，那是用组件解决样式问题。样式重复的正确解法是 token（第三节要做的事）或一个 CSS 类，**不是组件**。

### 2.3 什么时候宁可重复也不封装

三种情况明确不许抽：

1. **结构像但语义不同** — 评分按钮 vs 选择题选项。合并成一个"通用按钮块"组件，然后靠 props 分支出两套行为，是把两个简单东西换成一个复杂东西。
2. **只出现一次，但"以后可能会复用"** — 不抽。等第二次真的出现再抽，那时你才知道哪部分该做成参数。
3. **抽完之后要靠 `if` 分出两套完全不同的外观** — 说明它们不是一个东西。判据：如果组件里出现"当 `type === 'a'` 时用这套结构，否则用另一套结构"，拆回两个组件。

### 2.4 组件自身的四条硬约束

**不许出现字面样式值。** 颜色、字号、圆角、间距一律用 token。组件里出现 `#0E8C7F`、`16px`、`border-radius: 12px` 即违规，要写成 `var(--know)`、`var(--fs-body)`、`var(--r-md)`。这条和技术选型定的"禁止写 `.van-xxx`"是同一件事的两面：外观的改动只能发生在 token 层。

**不许知道自己的外边距。** 组件内部只写 `padding`，不写 `margin`。要它和别的东西隔多远，由用它的父级决定。违反这条的组件换个位置就得改代码，等于不可复用。

**props 超过 6 个就是抽错了。** props 变多说明这个组件在替调用方做太多决定。解法是拆成更小的组件，或者把可变的部分改成插槽（`slot`）。

**抽取之后必须删掉原来的副本。** 抽完新组件、旧的复制代码还留在页面里没删，等于把一份重复变成两份。抽取动作的完成标准是"原来的两处都在用新组件"，不是"新组件写好了"。

### 2.5 已经能点名的复用件

从设计系统里直接能看出会重复的东西，先在这里点名，写页面时按这个建，不要等重复发生了再回头抽：

| 元素 | 会出现在 | 抽成 | 归处 |
|---|---|---|---|
| eyebrow 小标题（等宽全大写 11px、字距 0.12em） | 几乎每一屏 | 组件 | `ui/Eyebrow.vue` |
| 大数字 + 小单位（`32px` 等宽 + `12px` 灰字） | 天数、进度、掌握度 | 组件 | `ui/StatNumber.vue` |
| 数据卡外壳（`--r-card` 圆角、20px padding、淡描边） | 统计区多张并列 | 组件 | `ui/StatCard.vue` |
| 标签（真题年份、词性） | 词卡、列表 | 组件 | `ui/Tag.vue` |
| 三档评分按钮（3 个同结构不同档色） | 翻卡页 | 组件 + 档位循环 | `word/RateButtons.vue` |
| 横线段队列条（含 20 段以上聚合） | 翻卡页顶部、统计页 | 组件 | `word/QueueBar.vue` |
| 模式切换胶囊 | 翻卡 / 选择题切换 | 组件 | `ui/SegmentToggle.vue` |

三档评分按钮特别说一句：它是**一个组件渲染三次**，不是三个组件。三档的差异只有档色和文案，全部来自档位数据；如果最后写成了三段近乎一样的模板，就是违反 2.1。设计系统里"三档视觉权重必须相等"这条要求，也只有用同一个组件渲染才能保证不跑偏。

---

## 三、怎么检查有没有违规

规范要能被检查，否则三个月后就是一纸空文。下面几条都能直接跑，输出为空即合规。

只有 `src/styles/var.less` 允许出现 `--van-` 变量：

```bash
grep -rn --include="*.vue" --include="*.ts" -e "--van-" src
```

模式用 `-e` 传，不要写成 `grep -rn -- "--van-" src --include=...`：`--` 会终止选项解析，后面的 `--include` 被当成路径，检查等于没跑。

任何地方都不许出现 `.van-` 类选择器（`postcss.config.ts` 里那份 fixed 元素修正列表是配置不是样式，不在此列）：

```bash
grep -rn "\.van-" src
```

源码里不许出现十六进制颜色字面值。`src/styles/` 是数值的唯一来源，排除在外：

```bash
grep -rniE "#[0-9a-f]{3,8}\b" src --exclude-dir=styles --exclude="*.svg"
```

这一条初稿只查了 `src/components` 和 `src/pages`，漏掉了 `src/App.vue` — 而那里恰好有一处 `theme-color` 的硬编码颜色（已改成运行时读 `--bg`）。检查范围要覆盖整个 `src`，否则规范查不出自己身上的违规。

`src/` 之外和 SVG 里有四处必须写真实颜色值，是**已知例外**，不算违规：

| 位置 | 为什么不能用 token |
|---|---|
| `index.html` 的 `mask-icon` / `msapplication-TileColor` | 首帧之前执行，CSS 还没加载 |
| `build/vite/index.ts` 的 PWA `manifest` | 构建期生成 JSON，没有运行时 |
| `public/*.svg` 图标 | 静态文件，不经过打包 |
| `src/assets/**/*.svg` | 同上，SVG 是资源不是源码 |

改 token 里的品牌色或背景色时，这三处要手动同步。除此之外任何地方出现字面颜色都是待修项。

`src/api/` 不许存在（本项目无后端，见第零节）：

```bash
test -d src/api && echo "违规：src/api 应删除" || echo OK
```

剩下两条只能靠眼看，没有可靠的机械检查方式：组件根元素有没有写 `margin`（2.4 第二条）、有没有该抽没抽的重复结构（2.1）。这两条我在每次改完之后自查并在交付说明里写清结果。




