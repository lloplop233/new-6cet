# 技术选型 — 六级词汇工具

日期：2026-08-26
形态：Vue 3 单页 H5，手机浏览器直接访问，不打包 App
UI 库：**Vant 4，唯一一个，不引第二个**

---

## 一、候选调研（数据取自 npm registry，2026-08-26 实测）

| 库 | 最新版 | 最近发布 | 距今 | Vue 3 支持 |
|---|---|---|---|---|
| **Vant** | 4.10.0 | 2026-06-28 | 约 2 个月 | peerDeps `vue ^3.0.0` |
| Varlet | 3.20.6 | 2026-08-19 | 7 天 | peerDeps `vue ^3.2.0` |
| TDesign Mobile Vue | 1.16.1 | 2026-08-06 | 20 天 | peerDeps `vue ^3.2.6` |
| NutUI | 4.3.15-beta.3 | 2026-04-02 | 约 5 个月 | 未声明 peerDeps |

### 落选理由

**NutUI — 直接排除。** npm 的 `latest` 标签指向的是 **beta 版**（`4.3.15-beta.1`），最后一个稳定版 `4.3.14` 发布于 2025-07-25，也就是 13 个月前。稳定版停更加上 latest 挂在 beta 上，这是维护减速的明确信号，不适合押上一个三个月的备考周期。

**Varlet — 维护最活跃，但风格绑定太死。** 它的定位是"基于 Material Design 2 和 3"的库。MD 的设计语言是强势的：按钮涟漪、阴影浮起、全大写标签、MD3 的形状语义。我们前面定的是纸质编辑感（克制圆角、衬线主角、无阴影）。要把 Varlet 改成那个样子，等于对抗它的设计语言，**最后一定会退化成逐个组件覆盖样式 — 正好违反你提的第二条要求**。所以它活跃度第一也不选。

**TDesign Mobile Vue — token 体系最系统，但还太年轻。** 版本号还在 1.x，组件数量和社区规模都小于 Vant，遇到坑时能查到的中文资料少一个量级。它的 Design Token 分层（全局 token → 组件 token）确实比 Vant 更规范，这是它唯一明显胜过 Vant 的地方。如果你更看重 token 体系的规范性而不是社区规模，这是唯一值得推翻我结论的候选。

### 选 Vant 4 的理由

1. **移动端 H5 最成熟**：四个里唯一进入大版本长期稳定期的（4.x 已多年），另两个还在 1.x / 3.x 迭代期
2. **风格中立**：没有绑定 Material 这类强势设计语言，改主题不需要对抗它
3. **变量结构和我们的设计系统天然同构**（下面第三节有逐项映射）—— 这是实质理由，不是"文档好"这种空话
4. 体积在四个里最小，H5 首屏直接受益
5. 中文文档 + 最大社区，你自己长期维护时查资料成本最低

代价要说清楚：**Vant 的发布节奏是四个里最慢的**（2 个月）。缓解在于 4.10.0 是 minor 版本而非 patch，说明还在加功能不只是修 bug。这一条我认为可以接受，但它是事实上的短板。

---

## 二、主题机制：一个必须绕开的坑

你要求"以后改风格统一走主题配置，不准逐个组件覆盖样式"。Vant 能做到，但**默认用法做不到**，这一点查证过官方文档和 issue，不是推测。

Vant 把变量分成两层：

- **基础变量**：`--van-primary-color`、`--van-text-color`、`--van-radius-md` 这类
- **组件变量**：`--van-button-primary-background`、`--van-slider-bar-height` 这类，它们的默认值引用基础变量

坑在于：`<van-config-provider :theme-vars="...">` 默认 `theme-vars-scope="local"`，变量落在 ConfigProvider 这个元素上，而**组件变量是对"最近祖先"的基础变量求值的**。所以在 local scope 下覆盖基础变量不会向下传播 — 官方文档把这种写法明确列为"不能用"。

佐证：[youzan/vant#9080](https://github.com/youzan/vant/issues/9080) 报的就是这个 — 改了 `--van-blue`，DevTools 显示覆盖生效，但引用它的 `--van-primary-color` 仍是默认值。该 issue 已关闭并打上 `document` 标签，说明这是设计行为而不是 bug。

还有一条相关的：Vant 的默认值声明在 `:root` 上，普通 CSS 覆盖可能因选择器特异性打不过，官方建议写成 `:root:root` 提权。

### 因此定下三条硬规矩

1. **所有基础变量在一个全局样式文件的 `:root` 里定义**，不用 ConfigProvider 的 local scope。需要运行时切换主题（例如以后加深色模式）时，才用 `<van-config-provider theme-vars-scope="global">`。
2. **全项目只有一个文件可以写 `--van-` 开头的变量。** 组件文件、页面文件里出现 `--van-` 一律视为违规。
3. **禁止写 `.van-xxx` 类选择器。** 需要改 Vant 组件外观时，先去找对应的组件变量；组件变量覆盖不到的，说明这个东西不该用 Vant 组件做 — 自己写一个，用我们自己的 token。

第 3 条是这套纪律的关键：它把"覆盖样式"的冲动转成一个二选一 — 要么走变量，要么这个组件不用 Vant。不留中间地带。

### 我们自己写的部分和 Vant 的边界

词卡、横线段队列条、三档评分按钮这些是产品的核心，**不用 Vant 组件做**，用我们自己的 token 写。Vant 负责的是通用外壳：Popup、Toast、Dialog、Switch、Slider（词量可调）、Tabbar、Cell（设置项）、ActionSheet。

这条边界让第 3 条规矩几乎不会被触发 — 需要精细视觉控制的地方本来就不经过 Vant。

**图标同样只用一个来源。** 优先 Vant 内置的 `van-icon`；它的图标不合调性时，直接内联手写 SVG（内联 SVG 不算引入第二个库）。不引 lucide、不引 iconify、不引任何图标包。

---

## 三、主题对齐：Vant 变量 → 我们的设计 token

选 Vant 最实在的理由在这里 — 它的基础变量结构和我们前面定的色彩体系是同构的：**底色两层、文字三层、语义四色**，我们的设计系统正好也是这个形状，一对一能填满，不需要新增语义槽也没有空槽浪费。

### 颜色

| Vant 变量 | 填入 | 对应我们的 token |
|---|---|---|
| `--van-primary-color` | `#0A6B60` | brand-deep（**用深阶不是主色** — 按钮白字要过 AA） |
| `--van-success-color` | `#0E8C7F` | know（认识） |
| `--van-warning-color` | `#B87D22` | vague（模糊） |
| `--van-danger-color` | `#A63D50` | unknown（不熟） |
| `--van-text-color` | `#131E1C` | ink |
| `--van-text-color-2` | `#465250` | ink-2 |
| `--van-text-color-3` | `#5F6B69` | ink-3 |
| `--van-background` | `#F1F2ED` | bg |
| `--van-background-2` | `#FDFDFB` | surface |
| `--van-border-color` | `#DCE0D9` | line |
| `--van-active-color` | `#E8EBE4` | line-soft（点按反馈底） |

**不要去改 `--van-blue` / `--van-red` / `--van-green` 这些原色变量。** 直接给语义变量赋 hex。原因就是第二节那个坑：改原色不会传播到引用它的语义变量。

### 圆角（Vant 只有 4 档，重映射）

| Vant 变量 | 默认 | 改为 |
|---|---|---|
| `--van-radius-sm` | 2px | **4px** |
| `--van-radius-md` | 4px | **8px** |
| `--van-radius-lg` | 8px | **12px** |
| `--van-radius-max` | 999px | 999px |

设计系统里的 14px（数据卡）和 18px（词卡）不映射到 Vant — 那两个是我们自己写的组件，用自己的 `--r-card` / `--r-word`。

### 间距（几乎不用动）

Vant 的 `--van-padding-base/xs/sm/md/lg/xl` = 4/8/12/16/24/32，**六档全部落在我们的 4/8/12/16/20/24/32/40/48/64 里，一个都不用改**。我们多出来的 20px（页面左右留白）用自己的 `--s5`，不经过 Vant。

### 字号（改字号必须同步改行高）

| Vant 变量 | 默认 | 改为 |
|---|---|---|
| `--van-font-size-xs` | 10px | **11px** |
| `--van-font-size-sm` | 12px | **13px** |
| `--van-font-size-md` | 14px | **15px** |
| `--van-font-size-lg` | 16px | 16px |
| `--van-line-height-xs` | 14px | **16px** |
| `--van-line-height-sm` | 18px | **20px** |
| `--van-line-height-md` | 20px | **22px** |
| `--van-line-height-lg` | 22px | **24px** |

Vant 的行高是**固定 px 值**不是倍数，只改字号不改行高会造成组件内文字错位。这一条容易漏。

### 字体

`--van-base-font` 填无衬线栈（Inter + Noto Sans SC）。衬线（单词主角）和等宽（标签、数字）**不进 Vant** — Vant 只有一个字体变量，承载不了三角分工，而那两个角色只出现在我们自己写的组件里。

`--van-price-font` 不用（项目里没有价格场景）。

### 动效（建议值）

`--van-duration-base` 0.3s → **0.24s**，`--van-duration-fast` 0.2s → **0.16s**。编辑感要利落，0.3s 在手机上偏拖。这条是审美判断，不是硬要求。

---

## 四、核实情况

**已核实**：四个库的版本号与历史发布时间（npm registry 实测）、Vant 的 peerDependencies、Vant 基础变量清单与 `theme-vars` 的 scope 行为、issue #9080 的状态与标签。

**未核实**：上面的 Vant 变量清单来自 2026-08-26 抓取的官方 ConfigProvider 文档，抓到的是英文镜像页，**未与 4.10.0 的包内实际 CSS 逐行比对**。装包之后我会用包里的样式文件复核一遍变量名和默认值，有差异再改这张表。另外 Vant 4.10.0 在 Vue 3.5+ 上的兼容表现我没有实测数据。

**没查**：各库的 bundle size 实测、Vant 组件在 iOS Safari 上的具体表现。这两项等装完包和真机打开后才有意义。

---

## 五、下一步

在写页面之前先做两件事，都不涉及产品界面：

1. 建 Vue 3 + Vite + Vant 4 骨架，装包后**用包内 CSS 复核第三节的变量表**
2. 写那一个全局 `:root` 主题文件，然后用一个临时的组件陈列页（不是产品页面）验证：只改这一个文件，Vant 的按钮、Switch、Slider、Cell、Popup 外观是否全部跟着变 — 这就是"统一走主题配置"这条要求的验收标准

第 2 步跑通了才说明这套选型真的成立。跑不通就得回头重新考虑 TDesign。

