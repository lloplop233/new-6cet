# 设计系统 v1 — 六级词汇工具

日期：2026-08-26
状态：**已确认定稿**。四点全部经本人确认（主色 2026-08-26；认识色/衬线/砍环 2026-08-26）。本文不含任何页面代码。

---

## 一、你给的三组参考，其实是三种互斥的策略

### 组 A — Fitness Tracker（图 1、2）

- **底色**：外画布浅冷灰蓝，App 内纯白，卡片白底
- **主色**：紫罗兰（约 `#6C5CE7`），只出现在选中态：选中的星期胶囊、底部激活 tab、"Details" 链接
- **色彩策略**：**多彩语义色**。每个指标一个专属色 — 卡路里红、饮水青、运动橙、体重紫、步数绿
- **签名手法两个**：
  1. 卡片白底 + **极淡的同色系描边**（红卡淡红边、青卡淡青边），色彩只出现在描边和数字上，不做大面积填充
  2. **大号彩色数字 + 紧跟的小号灰色单位** — `806`（大、粗、红）`Cal`（小、灰）
- **圆角**：卡片 16–20px，星期胶囊 14–16px（是圆角矩形，不是全圆），图标容器 12px 带极浅同色底
- **字体**：几何无衬线（Poppins/Circular 一类），数字大号粗体
- **进度环**：粗描边、圆头端点、内圈虚线刻度、中心大数字 + 下方 `0f 7000Steps` 小灰字
- **气质**：明亮、留白多、数据驱动、多彩但被白底压住

### 组 B — LA PLAZA 语言学习（图 3、4）

**这组和你的项目是同一个场景 — 翻卡、连续打卡、答题解锁。**

- **底色**：奶油纸白，外部场景是暖玫瑰粉
- **强调色**：**青绿松石（约 `#00B3A6`）单色**。除黑白奶油外，全场只有这一个彩色
- **字体三角**：高对比度**衬线**做主角（`la plaza`、`Places is open`）+ **斜体衬线**做释义（`town square`）+ **全大写等宽小字宽字距**做标签（`WEATHER COMPLETE · STREAK SAVED`、`FIRST CARD`、`SWIPE TO ANSWER`）
- **结构装置**：`(05) (06) (07) (08)` 编号 + 右侧目录列表 + 细分隔线 + 圆点状态（实心/空心/青色填充）
- **进度不用条**：顶部用 **6 段短横线**表示 6 张卡，做完的段填青色
- 当前活跃卡片左侧一条青色竖线
- **圆角**：卡片 8–12px（克制），胶囊按钮全圆
- **气质**：编辑设计、克制、文学性、杂志排版 + 一个鲜明强调色

### 组 C — Education courses（图 5、6）

- **底色**：App 内白/奶油，外部粉色渐变
- **色彩策略**：**高饱和大色块**填充整张卡 — 荧光绿、亮粉、亮黄、淡紫蓝，卡内叠等高线纹理
- **圆角**：20–24px，最圆的一组
- **Tab bar**：黑色悬浮胶囊，激活项黄色图标+黄字，头像单独一个黑圆
- **Chip**：全圆，激活态淡紫填充+深字，非激活态白底细边
- **气质**：年轻、活力、Gen-Z

### 三者的共性（你的真实偏好）

1. 全部是**浅底**，没有一个深色模式 → 主界面走浅色
2. 全部有一个明确的**品牌强调色专门表示"选中/激活"**
3. 圆角全部圆润，**没有一个用直角**
4. 全部有**大数字 + 小标签**的数据展示
5. 全部有胶囊形状（chip / tab / badge）
6. 全部有**游戏化进度**：streak days / level / 百分比

### 分歧（我必须替你选一边）

| 轴 | 组 A | 组 B | 组 C |
|---|---|---|---|
| 字体 | 几何无衬线 | 衬线 + 等宽 | 几何无衬线 |
| 色彩 | 多彩语义色 | 单色克制 | 高饱和大色块 |
| 结构 | 卡片网格 | 编号 + 目录 + 细线 | 大色块横滑 |
| 圆角 | 中（16–20） | 小（8–12） | 大（20–24） |

---

## 二、推荐方向：以 B 为骨架，A 为血肉，舍弃 C

**骨架取组 B（LA PLAZA 的编辑排版）**，理由有三条，不是审美偏好：

1. 它就是你的场景。翻卡背词的信息结构 — 大号单词、斜体释义、例句、连续天数 — LA PLAZA 已经解过一遍，可以直接借结构。
2. 你的产品里**单词本身是主角**，不是 App 界面。衬线大字让单词有分量，整个界面读起来像"读物"而不是"软件"，每天开 3 个月更耐看。
3. 市面上背单词 App（扇贝、百词斩、墨墨）全部是几何无衬线 + 多彩卡片。走编辑排版路线，你的工具第一眼就不像它们中的任何一个。

**血肉取组 A（数据卡与语义色）**：复习进度、连续天数、掌握度分布需要可视化，组 A 的"大数字 + 小单位 + 极淡同色描边卡"是成熟解法，而且**它的淡描边手法和组 B 的克制感天然兼容** — 都是"色彩只出现在边缘和数字上，不做大面积填充"。

**舍弃组 C**：高饱和大色块在每天 20–30 分钟的连续注视下会疲劳，而且会和单词内容抢注意力。它适合"课程列表"这种浏览型页面，不适合"逐张过卡"这种专注型页面。

一句话概括调性：**纸质单词本的克制，加一套能读懂进度的数据层。**

---

## 三、主色调

### 为什么不能直接选绿、黄、红

你的核心交互是三档自评 — **认识 / 模糊 / 不熟**。这三档天然想用交通灯（绿/黄/红）。这意味着绿、黄、红三个色相**已经被语义占用**，主色如果落在这三个色相里，就会和评分按钮混淆。

组 A 的紫罗兰在安全区，但紫罗兰是 2021 年 Dribbble 的通用色，也是当下 SaaS 产品的默认选择，用了就丢掉你在组 B 里真正被吸引的那个味道。

### 主色：青墨（Ink Teal）

```
brand        #0E8C7F   ← 主色
brand-deep   #0A6B60   ← 深阶，用于按钮填充底和小号文字
brand-soft   #E3F0EC   ← 浅阶，用于选中态背景、图标容器底
```

`#0E8C7F` 是组 B 的青绿松石**压深、去掉一点亮度**后的版本。为什么要压深：

- `#00B3A6` 那种亮青在浅底上做小面积点缀很漂亮，但一旦用于按钮填充，**白色文字对比度只有约 3.2:1，不满足无障碍标准**
- 压深到 `#0E8C7F` 后，它在浅底上的对比度约 **3.67:1** — 够做图标、描边、进度、大号数字，但仍不够做小号正文
- 所以再给一个 `#0A6B60`，浅底上约 **5.69:1**，白字在它上面约 **6.4:1**，两个方向都通过 AA

这就是"一个主色要配三阶"的原因：不是为了好看，是因为**同一个色相在填充态和文字态需要不同的明度才合规**。

### 一个关键决定：主色就是"认识"档

我建议**把品牌色直接用作"认识"档的颜色**，不给它另设一个绿色。

- 解决了主色与语义色的色相冲突 — 它们是同一个东西，不需要区分
- 每次你点"认识"，点到的是品牌色。品牌色 = 掌握感，是正向强化
- 三档只剩两个色相需要新增，配色总量更少，系统更干净

这是整套设计里我唯一"冒险"的一步，理由如上。如果你觉得"认识"必须是绿色，改动很小 — 加一个 `#1F7A4D`，但主色就要往蓝调再挪一点避让。

---

## 四、围绕主色推导的完整配色

### 4.1 三档评分（核心交互）

| 档 | 描边 / 图标 | 文字 | 淡填充 | 浅底对比度 |
|---|---|---|---|---|
| 认识 | `#0E8C7F` | `#0A6B60` | `#E3F0EC` | 3.67 / **5.69** |
| 模糊 | `#B87D22` | `#8A5E10` | `#F5EEDF` | 3.11 / **5.04** |
| 不熟 | `#A63D50` | `#A63D50` | `#F7E7E9` | **5.39** / 5.39 |

色相角 173° / 40° / 345° — 两两至少差 55°，在小面积色块上也能分清。三档全部是低饱和"编辑色"，不是荧光交通灯，和纸质底一致。

**三档按钮的视觉权重必须相等。** 不要因为"认识"是品牌色就把它做成实心深底 — 那会诱导你点"认识"，自评数据就废了，FSRS 调度会跟着一起失准。三个按钮统一是：淡填充 + 1px 同色描边 + 同色深字。

### 4.2 中性色阶（从主色带一点青调推导，不是纯灰）

```
ink        #131E1C   主文字          浅底对比 15.2
ink-2      #465250   次要文字        浅底对比 7.13
ink-3      #5F6B69   三级/占位文字   浅底对比 4.94
ink-4      #7C8886   仅非文字元素（刻度点、禁用图标），对比 3.26 不可承载文字
line       #DCE0D9   分隔线、卡片描边
line-soft  #E8EBE4   更弱的分隔
surface    #FDFDFB   卡片底（纸白）
bg         #F1F2ED   页面底（浅纸灰绿）
```

底色为什么不用暖奶油：`#F1F2ED` 去掉了黄调、保留一点绿灰，和主色同族，整套看起来是**一个系统推导出来的**，而不是"奶油底 + 随便配个青色"。两层底色（`bg` 画布 / `surface` 卡片）差值很小但足够分出层级，这是组 A 的做法。

### 4.3 按钮

| 类型 | 底 | 文字 | 描边 | 高 | 圆角 |
|---|---|---|---|---|---|
| 主操作（看释义 / 下一张） | `#0A6B60` | `#FDFDFB` | — | 52 | 12 |
| 三档评分 | 各档淡填充 | 各档文字色 | 1px 各档描边 | 56 | 12 |
| 次要 | 透明 | `#465250` | 1px `#DCE0D9` | 48 | 12 |
| 纯文字 | 透明 | `#0A6B60` | — | — | — |
| 禁用 | `#E8EBE4` | `#7C8886` | — | — | — |

主操作用 `brand-deep` 而不是 `brand`：白字在 `#0E8C7F` 上只有 4.13:1，不合规；在 `#0A6B60` 上是 6.4:1。

### 4.4 标签

| 用途 | 样式 |
|---|---|
| 词性（n. / vt.） | 无填充，等宽全大写 11px，字距 0.12em，`ink-3`，左右可加 12px 细线 |
| 真题年份（2015.12） | `brand-soft` 底 + `brand-deep` 字，等宽 11px，圆角 4 |
| eyebrow 小标题 | 等宽全大写 11px，字距 0.12em，`ink-3` |
| 模式切换（翻卡 / 选择题） | 全圆胶囊。未选：透明 + 1px `line` + `ink-2`；已选：`ink` 实心 + `surface` 白字 |

模式切换的已选态**用近黑而不用主色** — 主色已经承担"认识"这一语义，再让它表示"当前模式"会稀释含义。近黑胶囊借的是组 C 的黑 tab 手法。

### 4.5 选中态 / 激活态总规则

| 场景 | 未选 | 选中 / 激活 |
|---|---|---|
| 底部 tab | `ink-3` 线性图标 + `ink-3` 字 | `brand` 填充图标 + `brand-deep` 字 |
| 模式切换胶囊 | 透明 + `line` 边 + `ink-2` | `ink` 实心 + 白字 |
| 选择题选项（待答） | `surface` 底 + 1px `line` 边 | 2px `brand` 边 + `brand-soft` 底 |
| 选择题选项（判对） | — | 2px `brand` 边 + `brand-soft` 底 + `brand` 勾 |
| 选择题选项（判错） | — | 2px `#A63D50` 边 + `#F7E7E9` 底 + 胭脂 × |
| 队列进度段 | `line` 实心小段 | 按该卡评分结果填对应档色 |
| 设置项开关 | `line` 轨道 | `brand` 轨道 |
| 输入框聚焦 | 1px `line` | 1px `brand` + 3px `brand` 20% 外发光 |
| 键盘焦点环 | — | 2px `brand-deep`，2px 偏移（全站强制，无障碍底线） |

规则一句话：**填充表示"当前"，描边表示"可选"，颜色表示"结果"。** 三者不混用。

---

## 五、字号

移动端基准 16px，衬线做主角、无衬线做正文、等宽做标签，三种角色分工固定。

| 元素 | 字号 | 字体角色 | 字重 | 其他 |
|---|---|---|---|---|
| 单词本体 | 40px（长词自动缩到 28px） | 衬线 | 600 | 字距 -0.01em |
| 音标 | 15px | 等宽 | 400 | `ink-3` |
| 词性 | 11px | 等宽 | 500 | 全大写，字距 0.12em |
| 中文释义 | 20px | 无衬线 | 500 | `ink` |
| 例句英文 | 16px | 衬线斜体 | 400 | `ink-2`，行高 1.6 |
| 例句中文 | 14px | 无衬线 | 400 | `ink-3`，行高 1.7 |
| 按钮文字 | 15px | 等宽 | 500 | 全大写，字距 0.06em |
| 页面标题 | 24px | 衬线 | 600 | |
| eyebrow / 标签 | 11px | 等宽 | 500 | 全大写，字距 0.12em |
| 大数字（天数、进度） | 32px | 等宽 | 600 | 定宽数字（tabular） |
| 数字单位 | 12px | 无衬线 | 400 | `ink-3`，紧跟数字 |
| 正文 / 设置项 | 16px | 无衬线 | 400 | |
| 辅助说明 | 13px | 无衬线 | 400 | `ink-3` |

**字体选型（三个角色）**

- 衬线 display：`Newsreader` 或 `Fraunces` — 都有真斜体和可变字重，编辑感强
- 无衬线 body：`Inter` + 中文 `Noto Sans SC`
- 等宽 utility：`IBM Plex Mono` — 定宽数字，做进度和标签

**一个必须先验证的硬约束**：音标含 IPA 字符（ə ˈ æ ɪ ʌ θ ð ŋ）。这些字符在很多字体里缺字形，会掉成系统字体导致基线跳动。实现前我会先用实际词库里的音标串在候选字体下逐个渲染核对，如果 `IBM Plex Mono` 覆盖不全，音标就单独挂一个 IPA 覆盖完整的字体。

---

## 六、圆角

克制为主 — 组 B 的 8–12px 是这套调性的底子，只在触控目标上放宽到组 A 的量级。

```
4px    小标签（真题年份）
8px    列表项、输入框
12px   按钮、选择题选项
14px   数据卡
18px   词卡主体（最大的一个，独一份）
999px  胶囊（模式切换、chip、badge）
```

不用 20px 以上的圆角（组 C 的量级）— 那会把界面推向"活力 App"，和纸质编辑感冲突。

---

## 七、间距

4px 基数，8pt 网格。

```
4  8  12  16  20  24  32  40  48  64
```

| 位置 | 值 |
|---|---|
| 页面左右留白 | 20px（不是 16 — 稍宽更从容，呼应编辑排版） |
| 卡片内 padding | 20px |
| 词卡内 padding | 24px（主角多给一点） |
| 卡片之间 | 12px |
| 区块之间 | 32px |
| 三档评分按钮间隙 | 10px |
| 标签与正文之间 | 8px |
| 单词与音标之间 | 12px |
| 音标与释义之间 | 20px |
| 底部 tab bar 高 | 56px + 安全区 |
| 评分按钮距底部 | 16px + 安全区（拇指区，必须能单手够到） |

---

## 八、签名元素：横线段队列条

整套设计里我只留一个"要被记住的东西"，其余全部安静。

借组 B 顶部那排短横线，但**让它承载真实信息**：今日队列有多少张卡，就有多少段横线；每答完一张，对应段填上你给出的评分档色。

```
今日  ▬▬ ▬▬ ▬▬ ▬▬ ▬▬ ▬▬ ▬▬ ── ── ── ── ──        7 / 12
      青  青  琥  青  胭  青  琥  未做…
```

一眼能看出三件事：今天做了多少、还剩多少、**掌握分布是什么样**（青多说明状态好，胭脂多说明该降今日新词量了）。这比一根百分比进度条多出后面那条信息，而百分比条给不了。

这也是这套设计里唯一让三档色同屏出现的地方 — 正好是最需要它们的地方。

超过 20 张卡时不再一段一张，改为按 5 张一段聚合，段内取该组占比最高的档色。

---

## 九、这一版明确不做的

- **深色模式**：你给的三组参考全是浅色，先只做浅色。但 token 会用 CSS 变量组织，后面加深色主题不用重写组件。
- **等高线纹理、渐变、3D 插画**（组 C 的装饰层）：和纸质编辑感冲突。
- **进度环**（组 A 的粗描边环）：横线段队列条已经承担了进度表达，再加环是重复。第八节说的"只留一个签名"就是指这个 — 环要砍掉。

---

## 附录：token 清单（实现时直接用，不是页面代码）

```css
:root {
  /* 品牌 */
  --brand:       #0E8C7F;
  --brand-deep:  #0A6B60;
  --brand-soft:  #E3F0EC;

  /* 三档评分 */
  --know:        #0E8C7F;  --know-text:  #0A6B60;  --know-soft:  #E3F0EC;
  --vague:       #B87D22;  --vague-text: #8A5E10;  --vague-soft: #F5EEDF;
  --unknown:     #A63D50;  --unknown-text: #A63D50; --unknown-soft: #F7E7E9;

  /* 中性 */
  --ink:    #131E1C;  --ink-2: #465250;  --ink-3: #5F6B69;  --ink-4: #7C8886;
  --line:   #DCE0D9;  --line-soft: #E8EBE4;
  --surface:#FDFDFB;  --bg: #F1F2ED;

  /* 圆角 */
  --r-xs: 4px; --r-sm: 8px; --r-md: 12px; --r-card: 14px; --r-word: 18px; --r-pill: 999px;

  /* 间距 */
  --s1: 4px; --s2: 8px; --s3: 12px; --s4: 16px; --s5: 20px;
  --s6: 24px; --s7: 32px; --s8: 40px; --s9: 48px; --s10: 64px;
}
```

---

## 需要你确认的四点（已全部确认，2026-08-26）

1. **主色青墨 `#0E8C7F`** — ✅ 认可
2. **"认识"档直接用品牌色，不另设绿色** — ✅ 认可（2026-08-26，不设绿）
3. **衬线做单词主角** — ✅ 认可（2026-08-26）
4. **签名元素只留横线段队列条，砍掉进度环** — ✅ 认可（2026-08-26）

已定稿，可进入页面实现。

