# 六级词汇工具 — 形式确认单

日期：2026-08-26
定位：**全新重做**，只做词汇背记一个板块，不涉及听力/阅读/写作/翻译。
与旧项目关系：`E:\everlasting\项目\六级复习工具`（v1.0）保持原样不动，本项目只复用其**词库数据**和 **FSRS 库文件**。

## 一、已确认的形式（你选的）

| 维度 | 结论 |
|---|---|
| 主攻板块 | 只做词汇背记 |
| 使用设备 | 手机为主 |
| 访问方式 | 网页，最终部署到线上用网址访问；**当前阶段先不部署**，本地做完验证后再由你决定 |
| 背词交互 | 翻卡自评 + 选择题辨义，两种模式可切换 |
| 复习调度 | FSRS 智能调度（认识→Good / 模糊→Hard / 不熟→Again） |
| 进度存储 | 仅存手机浏览器本地，支持 JSON 手动导出备份 |
| 词表来源 | 复用旧项目 5651 词词库，按词频 rank 截取高频段 |
| 词量 | **不写死**，做成可调设置（滑块或输入框，默认 2500） |
| 发音 | 浏览器内置 TTS 朗读，无音频文件、无外部接口 |
| 备考周期 | 3 个月以上，按长线复习设计 |

## 二、由我定的技术默认项（沿用旧项目已验证的决策，如不同意请指出）

- **纯前端单页**：原生 HTML + CSS + JS，无框架、无构建工具。理由：手机浏览器直接打开即可用，部署时只需扔静态文件。
- **FSRS 实现**：复用旧项目 `lib/fsrs.js`（ts-fsrs UMD 版），不重新实现算法。
- **PWA 化**：加 `manifest.json` + service worker，支持"添加到主屏幕"和离线使用。手机为主的场景下这一步收益明显。
- **词库分片**：旧 `data/words.js` 为 1.8 MB 单文件，手机首屏加载偏重。改为按 rank 分片（每 500 词一片）按需加载，并精简例句字段（每词最多留 1 条例句）。这与"词量可调"是配套的——调大词量时增量加载后续分片。
- **移动端优先布局**：单手可达的大按钮，翻卡区域占满视口，评分按钮固定在底部拇指区。

## 三、需要你确认的两点

1. **"其他"板块补充**：第一轮你选了"其他"并补充词汇。如果你当时还想到了别的词汇相关子项（词根词缀、短语搭配、形近词辨析），现在说，我一并纳入；否则按纯单词背记做。
2. **项目文件夹命名**：当前目录名是 `NEW`，偏通用。建议改为 `六级词汇工具`。要改的话我可以在开工前处理。

## 四、下一步

待你确认上述两点后进入实现。计划顺序：

1. 从旧词库生成精简分片词表 → 验证：分片文件数、总词数、字段完整性核对
2. 词卡 UI + 翻卡自评交互（移动端布局） → 验证：手机视口尺寸下截图目检
3. 接入 FSRS 调度 + localStorage 进度 → 验证：模拟多日评分，检查 due 队列变化正确
4. 选择题辨义模式 + 模式切换 → 验证：干扰项不重复、不含正确答案
5. TTS 发音 + 词量可调设置 → 验证：真机或移动端模拟下发声与设置持久化
6. PWA 离线 + JSON 导出导入 → 验证：断网后可打开、导出文件可重新导入

部署不在此清单内，做完后单独找你确认。
