# 交接文档 — 六级词汇背记工具

> 给接手的开发者 / AI（如 Codex）看。读完这份文档即可接手本项目。
> 最后更新：2026-08-31

---

## 1. 项目是什么

一个**英语六级（CET-6）词汇背记工具**，定位是：

- **移动端 H5 网页应用**，在手机浏览器里用，**不打包成 App**，后续在线部署成网页。
- 单人使用（自己备考），数据存手机本地，无后端、无账号。

### 形式（已锁定，别改）

| 决策 | 内容 |
|---|---|
| 范围 | 只看词汇模块，先不做听力/阅读/写作 |
| 模式 | **双模式可切换**：① 翻卡自评 ② 选择题辨义 |
| 词库 | 复用旧的 5651 词数据，按词频排序（数据见第 9 节，版权受限不在仓库） |
| 进度 | 手机 `localStorage` 存 + 支持 JSON 导出备份 |
| 调度 | FSRS 间隔重复：认识→Good / 模糊→Hard / 不熟→Again |
| 发音 | 浏览器 TTS |
| 数量 | 可调背记单词数（默认 2500） |
| 周期 | 面向 3 个月以上备考周期 |
| 部署 | **先不部署，做完再说** |

---

## 2. 当前状态

### ✅ 已完成
- 技术选型、设计系统、工程规范三份文档（见第 3 节文件清单）
- 项目骨架：Vue3 + Vite + Vant4，文件路由、Pinia、PWA、NProgress、自动导入等已配好
- **设计 token 化完成并验证**：`tokens.less`（唯一数值源）+ `var.less`（Vant 映射层），px→vw 已验证生效、Vant 变量名已对照 4.10.0 全部正确
- 依赖已装、dev server 能跑
- 占位首页 + token 验收页（不是产品页）
- P0-1 类型检查阻断已修复，`pnpm typecheck` 通过
- P0-2 Lint 范围已收敛，`pnpm lint` 通过
- P0-3 Git 基线已建立，首个 commit 已创建
- P0-4 质量命令已建立：`pnpm test` 运行 Node 内置测试，`pnpm check` 串行执行类型检查、Lint、测试和生产构建
- P1-1 领域类型已定义并通过契约测试：词库、复习、会话、设置和备份类型均使用项目自有接口

### ⬜ 未完成（见第 8 节下一步）
- **产品页面一行没写**（首页只是占位）
- 衬线字体文件未接入、IPA 音标字形覆盖未验证
- favicon / PWA 图标还是模板橙红色，未改成青墨
- 词库数据未接入

---

## 3. 技术栈（唯一、别混）

| 项 | 选择 | 说明 |
|---|---|---|
| 框架 | **Vue 3**（组合式 API） | 只用 Vue 3 |
| 构建 | Vite | 文件路由、自动导入已配 |
| UI 库 | **Vant 4**（唯一 UI 库） | 明确支持 Vue3、活跃维护；**绝对不准混第二个库** |
| 打包 | postcss-mobile-forever | px→vw，375 设计宽，`maxDisplayWidth: 600` |
| 状态 | Pinia + pinia-plugin-persistedstate | |
| 路由 | vue-router + `vue-router/vite` 文件路由 | 路由名写在 `<route lang="json5">` |
| PWA | vite-plugin-pwa | |
| 样式 | Less + CSS 变量（设计 token） | 见第 5 节 |
| 包管理 | pnpm | 见第 6 节 exFAT 坑，项目现在在 D 盘 |

> **UI 库主题对齐 + 统一配置**：所有 Vant 外观改动**只能走主题配置（CSS 变量）**，**严禁逐组件覆盖样式**（禁止 `.van-*` 类、禁用 `theme-vars` 局部覆盖）。详见第 5 节。

---

## 4. 文件结构与归位规范

### 当前实际 src/ （已遵守规范）
```
src/
├── pages/                    页面（文件路由）
│   ├── index.vue             首页（占位，未写产品内容）
│   ├── tokens/index.vue      token 验收页（临时验收用，非产品页）
│   └── [...all].vue          404
├── components/
│   └── layout/               NavBar.vue、TabBar.vue
├── styles/
│   ├── app.less              全局基础样式
│   ├── tokens.less           设计 token（唯一数值源）
│   └── var.less              Vant 映射层（唯一含 --van-* 的文件）
├── constants/index.ts        常量（appName、路由标题等，已吸收原 src/config/）
├── composables/              （空，组合式函数归这）
├── services/                 （空，数据访问层归这）
├── stores/index.ts           Pinia
├── stores/modules/routeCache.ts
├── utils/                    inline-px-to-vw.ts、round.ts、set-page-title.ts
├── types/
│   ├── word.ts              词库实体与稳定 WordId
│   ├── review.ts            时间、三档评分与稳定复习状态
│   ├── study.ts             双模式学习会话与评分结果
│   ├── settings.ts          用户设置
│   ├── backup.ts            不含词库本体的备份信封
│   ├── domain.test.ts       领域类型契约与 JSON 往返测试
│   └── *.d.ts               5 个自动生成文件，别手改
└── router/index.ts
```

### `工程规范.md` 定死的内容（必须遵守）
1. **目录树定死**：`pages/` `components/{layout,ui,word}/` `composables/` `services/` `stores/` `utils/` `types/` `styles/` `constants/`，**不许新增顶层目录**，`components/` 就这三个子目录。
2. **三条机械边界**：
   - `utils` vs `composables` = 看文件是否 `import` 了 `vue`
   - `utils` vs `services` = 看是否碰外部世界（`localStorage`/`fetch`/动态 `import`/`speechSynthesis`/`Date.now`）
   - `ui` vs `word` = 看组件名是否含产品名词（词/卡/评分/释义/例句/队列/音标）
3. **本项目没有 `src/api/`**（无后端、无 HTTP 接口），数据访问层叫 `src/services/`。
4. **组件复用**：第二次出现就封装（不是三次法则）。四条硬约束：① 不许写字面样式值（颜色/字号/圆角/间距用 token，出现 `#0E8C7F`/`16px`/`border-radius: 12px` 即违规）；② 组件内部只写 `padding`，**不写 `margin`**；③ props > 6 就是抽错了；④ 抽完必须删掉原副本。
5. **检查命令**（在 `工程规范.md`，输出空即合规）：
   ```bash
   grep -rn -- "--van-" src --include="*.vue" --include="*.ts"   # 只许 var.less 含 --van-
   grep -rn "\.van-" src                                          # 任何地方不出现 .van- 类
   grep -rniE "#[0-9a-f]{3,8}\b" src/components src/pages         # 组件/页面无十六进制颜色
   test -d src/api && echo 违规 || echo OK
   ```

---

## 5. 设计系统（已定稿，不要再改）

主文档 `设计系统.md`。**四点已全部用户确认**，别擅自回改：

1. **主色：青墨 `#0E8C7F`**
2. **「认识」档直接用品牌色，不另设绿色**（唯一冒险的一步，用户已确认）
3. **单词本体用衬线**做主角
4. **砍掉进度环**，签名元素只留横线段队列条

### 配色
```
品牌     --brand #0E8C7F（浅底对比3.67，用于图标/描边/进度/大数字）
         --brand-deep #0A6B60（白字6.4，双方向过AA，用于按钮底）
         --brand-soft #E3F0EC（选中态背景）
三档评分  --know #0E8C7F / --know-text #0A6B60 / --know-soft #E3F0EC
         --vague #B87D22（模糊）/ --unknown #A63D50（不熟）各带 -text/-soft
中性     --ink #131E1C（主文字15.2）、--ink-2 #465250（7.13）、--ink-3 #5F6B69（4.94）
         --ink-4 #7C8886（对比3.26，只用于非文字元素）、--line #DCE0D9、--line-soft
         --surface #FDFDFB（卡片底）、--bg #F1F2ED（页面底）
```
对比度数字是算过的，**别随手改**。

### 三角分工（字体）
- **衬线做主角**：`Newsreader` / `Fraunces`（单词、页面标题）——**字体文件尚未接入**，当前回退系统字体
- **无衬线正文**：`Inter` + 中文 `Noto Sans SC`
- **等宽工具/音标**：`IBM Plex Mono`——**IPA 音标字形覆盖未验证**，可能要单独挂字体

### Token 分层（关键）
- `src/styles/tokens.less`：**唯一**放具体数值的文件（颜色/字号/圆角/间距/字体）
- `src/styles/var.less`：**唯一**允许写 `--van-*` 的文件，值一律用 `var(--xxx)` 引用 token（不重复抄 hex）
- 组件/页面：只能写 `var(--xxx)`，出现字面值即违规

### Vant 主题配置的两个大坑（务必读）
1. **不要用 `theme-vars` 做覆盖**：它默认 `scope="local"`，而 Vant 组件变量按**最近祖先**的基础变量解析，局部覆盖不会向外传播——文档里标了这个形式不可用（对应 youzan/vant#9080）。
2. **必须用 `:root:root`**：Vant 在 `:root` 声明默认值，普通 `:root` 优先级不够，要用 `:root:root` 提高特异性。见 `src/styles/var.less` 头部注释。

**验收标准**（`技术选型.md` §5）：只改 `tokens.less`/`var.less` 这个主题文件，Vant 的按钮/Switch/Slider/Cell/Popup 外观必须全部跟着变。已验证成立。

---

## 6. 踩过的坑（重要，避免重蹈）

1. **exFAT 盘跑不动 node 项目**：E 盘是 exFAT，装依赖 600s+ 没完、删目录慢、改目录名被系统锁（不支持符号链接、小文件 IO 极慢）。**解法：项目必须放 NTFS 盘**。现在在 `D:\工作\ddff`（D 是 NTFS、479GB）。**别把项目搬回 E 盘**。旧项目 `E:\everlasting\项目\六级复习工具`（v1.0）留在原处，不可修改。
2. **corepack enable pnpm 报 EPERM**：corepack 写 shims 到系统目录要管理员。改用 `npm i -g pnpm`（用户目录，无需管理员）。
3. **非 git 目录下 `prepare: simple-git-hooks` 会让 `pnpm install` 失败**：已从 `package.json` 移除该 script 及 simple-git-hooks/lint-staged/commitlint。
4. **`.npmrc` 里有 `node-linker=hoisted`**：当初为绕 exFAT 加的；现在 D 是 NTFS 其实用默认 symlink 布局更标准省空间，但 hoisted 已验证可用、能跑。介意可移除该行、删 node_modules 重装。
5. **端口**：dev server 现在跑在 **3001**（vite 默认 3000，因为有一次 3000 被占用自动跳了）。确认端口看终端输出。
6. **孤儿进程**：停 vite 时用 TaskStop 只停 bash 包装，真正的 node/vite 子进程可能还占端口/锁目录，需 `taskkill //F //PID <pid>` 单独杀。

---

## 7. 怎么跑

```bash
cd "D:\工作\ddff"
pnpm install
pnpm dev          # 看输出的端口（当前 3001）
```

- 电脑访问：`http://localhost:3001/`（首页占位）、`http://localhost:3001/tokens`（token 验收页）
- 手机同 Wi-Fi：`http://192.168.10.230:3001/`
- 其他脚本：`pnpm build:dev`、`pnpm build:pro`、`pnpm lint`、`pnpm test`、`pnpm typecheck`
- 完整质量门禁：`pnpm check`

---

## 8. 下一步做什么（按优先）

1. 由 L3 模型执行 P1-2，优先选择成熟 FSRS 实现并冻结三档评分映射、时间处理、初始化参数和序列化字段。
2. 使用 20 个模拟词完成 P2 垂直闭环并通过恢复验收。
3. P2 审查通过后再按 P3～P8 实施正式界面、FSRS 双模式、备份恢复、真实词库、字体/TTS/PWA 和发布验收。

> 不要直接按旧顺序先写产品页；具体状态、依赖和验收标准以 `项目实施计划.md` 为准。

---

## 9. 数据与版权注意

- 词库数据**不在仓库里**——是旧项目 `E:\everlasting\项目\六级复习工具` 从 `tools/_raw/` 生成的真题词，**版权受限**。接入时只取词表本身（单词/音标/释义），不处理版权内容；需要就找旧项目拿生成好的词表文件，别重新爬。
- 设计 3 个 Dribbble 参考图：Fitness Tracker、education courses、LA PLAZA / Places is open（用来定风格的）。

---

## 10. 其他文档

- `设计系统.md` — 完整设计规格与取舍（含为什么不走几何无衬线、主色为什么不选绿）
- `技术选型.md` — Vant 4 选型论证、主题配置坑、验收标准
- `工程规范.md` — 文件归位 + 组件复用规范 + 违规检查命令
- `六级词汇工具-形式确认.md` — 形式决策的问答记录
