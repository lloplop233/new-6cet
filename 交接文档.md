# 交接文档 — 当前项目状态

> 这是新 Codex 会话的唯一当前状态入口，只记录“现在什么是真的”。
> 最后核对：2026-09-05 P2-3 会话收口。主工作区 `main`，无任何 worktree；P1 与 P2 已完成任务均已推送至 `origin/main`（最近推送至 `e4ec7e3`），P2-3 的 feat/docs commit 与推送状态以实际 `git log` / `git status` 为准；本机推送均经 `127.0.0.1:10809` HTTP 代理完成（`git -c http.proxy=...`，未改动 git config）。
> 开始工作前必须重新运行本文的 Git 检查，不能把本快照当成永久事实。

## 1. 项目一句话

移动端 H5 六级词汇背记工具，单人使用、无后端、不打包 App；目标能力包括翻卡自评、选择题辨义、FSRS、本地持久化、JSON 备份、TTS、真实词库和 PWA。

长期工程硬规则见 `AGENTS.md`；路线图和 Acceptance Criteria 见 `项目实施计划.md`；产品、设计和工程细节按本文末尾索引定向阅读。

## 2. 当前仓库快照

V0 UI、P1-2 FSRS 策略、P1-3 存储协议、P1-4 模块接口、P2-1 模拟词库、P2-2 翻卡自评与 P2-3 最小会话恢复均已收口；P2-4 垂直闭环审查尚未开始。V0 仍是 Mock 体验（选择题分支仍走它），P1-2 仍是算法边界与契约测试，P1-3 仍是协议与纯函数边界，P1-4 是规则与守卫，P2-1 是纯函数与模拟数据，P2-2 是翻卡真会话流程，P2-3 是翻卡会话的持久化与刷新恢复——均不代表正式业务接线已经完成。

### A. `main`：V0 纯前端可视化验收版

- Git：V0 已纳入 `main@0fc6a24` checkpoint；后续 P1-2 集成没有修改其页面、组件、样式、Router、Store 或 Mock 数据。
- 内容：今日、学习、完成、进度、设置、404、TabBar、共享 UI/word 组件和 6 个 Mock 单词；首页学习方式改为两个直接入口，学习页评分和答题使用短暂原位反馈后自动推进。
- 未接入正式 Store、Session、FSRS、持久化、真实词库或 TTS；V0 评分和答案不会保存。
- Scope：只用于验收信息架构、视觉方向、基础点击流程、状态样式和响应式布局。
- Non-goals：正式学习计分、状态恢复、算法、存储协议、真实统计、真实词库、TTS、PWA 更新和后端。
- 任务文档：
  - `docs/superpowers/specs/2026-09-01-frontend-experience-design.md`
  - `docs/superpowers/plans/2026-09-01-v0-visual-implementation.md`
  - `PRODUCT.md`
- 验证状态：2026-09-01 当前 `pnpm check` 通过，包含类型检查、Lint、8/8 Node 测试和生产构建；浏览器验证覆盖两种学习模式连续完成、正确/错误反馈、首页直接入口和 320、375、430、600px 视口，控制台无阻断错误。
- 当前结论：用户已明确确认 V0 版本没有问题并授权形成 Git checkpoint；V0 到此收口，不再继续视觉打磨或扩展正式业务逻辑。

### B. `main`：P1-2 FSRS 策略已集成

- 历史来源：原 feature branch `codex/p1-fsrs-strategy@986f1f4` 已完成 Implementation、Verification 和 Task 6 文档收口，Git 有对应 commit 证据。该 commit 中书面记录过一次 `APPROVED`（Critical 0、Important 0、Minor 3）的 L3 Review 结论，但报告原文没有留存，用户也不确认它执行过；因此本轮重做独立 Review，旧结论不再作为 P1-2 的验证依据。
- 独立 L3 Review：2026-09-03 在本会话对最终 integration tree 补做，结论 `APPROVED WITH CONDITIONS`，Critical 0、Important 6、Minor 8。6 项 Important 集中在「测试没有锁住它声称锁住的契约」和类型精度不足，已在 `c8dd7e3` 全部修复，并用 6 个反证实验逐项证明修复后的测试真的能检测目标失败模式（丢字段、篡改 `stability`、权重漂移、项目参数被改动、版本常量说谎、签名被放宽）。
- 集成方式：先以 `d6ecb58` 独立保存项目级文档真相源规则，再从该最新 `main` 建立 `codex/p1-2-integration-finalization`；只选择性 cherry-pick 6 个纯实现/测试 commit，不直接引入 `986f1f4` 的旧项目级文档快照。
- 集成 commit：`aea4013`、`6755ce8`、`b572835`、`9bd0583`、`d1c1a87`、`53731d8`，Review 修复为 `c8dd7e3`；文档收口位于包含本节的后续 checkpoint。
- 实现范围：`package.json`、`pnpm-lock.yaml`、`src/constants/fsrs.ts`、`src/types/fsrs.ts`、`src/utils/fsrs.ts`、`src/utils/fsrs.test.ts`；精确依赖 `ts-fsrs@5.4.1`，共 6 个实现文件、417 行新增。
- 当前验证：2026-09-03 最终 integration tree 的 `pnpm check` 退出码为 `0`，包含 TypeScript、ESLint、22/22 Node 测试和 Vite `8.2.0` production build；工程规范扫描、V0 范围审计和 `git diff --check` 另行通过；production 产物不含 `ts-fsrs`，证实 FSRS 模块尚未接线。
- 当前结论：P1-2 为 `[x]`，已进入最新 `main`；其范围仍只是 FSRS 依赖、项目自有状态/常量、纯函数边界和契约测试，不代表 P4-1 正式调度 Service 或业务接线已完成。

### C. `main`：P1-3 存储协议已集成

- 任务来源：2026-09-03 用户确认四项决策（单一键、降级拒绝、默认目标词量 2500、进度字段命名 `progress`）后作为独立 coherent deliverable 实施。
- 决策记录：`P1-3 存储协议实现策略.md`（单一键 `cet6-vocab:state`、`SCHEMA_VERSION = 1` 且与 `libraryVersion` 职责分离、读写行为矩阵、迁移链机制、默认值、`pinia-plugin-persistedstate` 边界、校验深度「结构不语义」原则、P5 交接约束）。
- 实现范围：`src/types/storage.ts`、`src/constants/storage.ts`、`src/services/storage.ts`、`src/services/storage.test.ts` 四个新文件；不修改任何 P1-1/P1-2 既有文件和 V0 页面。
- 核心不变量：读取路径永不写盘；任何失败路径不破坏磁盘已有数据；未来版本拒绝加载且不覆盖；词库本体不进 `localStorage`（`PersistedData` 无 `words` 字段，类型反证 + 运行时键断言 + 测试三方锁定）。
- 独立 L3 Review：2026-09-03 对最终实现审查，结论 `APPROVED WITH CONDITIONS`（Critical 0、Important 1、Minor 2）。唯一条件「读取保留旧 scheduler 元数据无测试锁定」已补 round-trip 测试并经 E6 反证验证；Minor 两项（`validateScheduling` 错误消息带 wordId 定位、D8 文字补齐「非空」「≥1」措辞）已随修复落地。
- 反证实验：E1 校验置空（39 项变红）、E2 移除 future-version 检查（2 项变红）、E3 迁移失败静默回退默认值（3 项变红）、E4 配额误分类（1 项变红）、E5 读取写盘（1 项变红）、E6 读取时刷新 scheduler 元数据（1 项变红）。
- 当前验证：Review 修复后 `pnpm check` 退出码 0（TypeScript、ESLint、92/92 Node 测试含存储协议 70 项、production build）；`dist/assets/` 无 `ts-fsrs` 引用（服务未被应用代码导入，FSRS 依赖未进 bundle）。
- 当前结论：P1-3 为 `[x]`，已进入最新 `main`。它只冻结协议与纯函数边界；Store 接线（P5-1）、真实迁移（P5-2）、备份导出导入（P5-3）和恢复 UI（P5-4）均未开始。

### D. P1-4 模块接口冻结已完成

- 任务来源：2026-09-04 用户批准完整方案（调用方向矩阵、`services → utils` 允许、启用 `allowImportingTsExtensions`、`MAX_TIMESTAMP` 迁移、三项 Minor 修复）后作为独立 coherent deliverable 实施。
- 决策记录：`P1-4 模块接口冻结.md`（六层调用方向矩阵：types/constants 基础层 → utils → services → stores/composables → components/pages/router，只许向下依赖；`composables → stores` 单向；types/constants 互引仅 type-only；utils 禁 `vue`/`pinia`/`vant`/`@vue/*`）。
- 实现范围：`tsconfig.json`（`+noEmit`、`+allowImportingTsExtensions`）、`eslint.config.ts`（`layerRule` + 9 个 override 块的 `no-restricted-imports` 守卫，零新依赖）、删除 9 处 import 上的 `@ts-expect-error`（其余 19 处类型反证保留）、`MAX_TIMESTAMP` 迁移至新文件 `src/constants/timestamp.ts`、Node 基线断言对齐 `>=22.23.0`、补 `dateToTimestamp` 负值用例。不修改 V0 页面、P1-2/P1-3 冻结的协议语义。
- 关键约束：ts-fsrs 全项目禁 import，仅 `src/utils/fsrs.ts` 与其测试豁免；动态 `import()` 与 types/constants 的 type-only 互引是守卫已知边界（`P1-4 模块接口冻结.md` 第四节）。
- 独立 L3 Review：2026-09-04 结论 **APPROVED**（Critical 0、Important 0、Minor 3）；Minor 1（`@vue/*` 绕过）已修复并经探针验证，Minor 2/3 记为已知边界。
- 反证实验：14 项（E-A1~A10 方向/ts-fsrs/白名单/`.vue`/别名/中转 re-export、E-B1 拼错导出名 TS2724、E-C1 旧路径 MAX_TIMESTAMP TS2305、E-D1 修复后 `@vue/shared` 拦截）全部变红后还原。
- 当前验证：`pnpm check` 退出码 0（TypeScript、ESLint、93/93 Node 测试、production build）；`dist/assets/` 无 `ts-fsrs`；工程规范扫描通过；工作区改动与 Scope 一致（`tsconfig.json`、`eslint.config.ts`、4 个 utils/services 文件、`constants/fsrs.ts` + 新 `constants/timestamp.ts`）。
- 当前结论：P1-4 为 `[x]`。调用方向已冻结且有机械约束，P2 起 L1/L2 任务按矩阵直接实施，不再重新设计接口；它不代表任何业务模块已实现。

### E. P2-1 模拟词库与队列生成已完成

- 任务来源：2026-09-04 用户确认完整方案（20 词独立词库、rank 确定性排序、与 V0 六词零重叠、四函数契约）后作为独立 coherent deliverable 实施；风险等级 L1，按 Review Budget 走 Fast Path，无独立 Review。
- 实现范围：`src/constants/mock-vocabulary.ts`（20 个 CET-6 词，`as const satisfies readonly Word[]`，id/rank 1–20 唯一，`realExamYears: ['Mock']`，与 V0 六词零重叠）、`src/utils/study-session.ts`（`buildStudyQueue`：按 rank 升序、重复 id 抛错；`createStudySession`：初始 active 会话、空队列/重复 id 抛错；`currentWordId`：走完返回 null；`isSessionFinished`）、`src/utils/study-session.test.ts`（16 项契约测试）。不修改任何已有文件。
- 关键设计：队列顺序确定性（rank 升序，非随机）——P2-3 刷新恢复要求队列可重演；函数吃任意词库入参，不写死 20 词——换真实词库时原样存活；时间一律入参注入（P1-2 时间纪律）。
- 反证实验：3 项。注入重复 id → 6 项测试变红；去掉 rank 排序 → 恰好排序测试变红；去掉 `currentWordId` 越界防御 → 不变红（有效实验：「走完返回 null」是行为契约，`?? null` 已兜底，显式 length 检查是冗余防御——测试锁行为而非实现）。
- 当前验证：`pnpm check` 退出码 0（TypeScript、ESLint、109/109 Node 测试、production build）；diff 审计恰 3 个新文件、零已有文件改动；V0 预览热重载后控制台零错误（新文件未被应用代码导入）。
- 当前结论：P2-1 为 `[x]`。队列与结束状态纯函数已锁定；评分、推进、UI 与持久化均属 P2-2/P2-3。

### F. P2-2 翻卡自评最小流程已完成

- 任务来源：2026-09-04 用户按推荐确认三项决策（选择题保持 V0 Mock 原样、完成页用路由 state 传 JSON、首页 6 词文案暂不动并记录不一致）后作为独立 coherent deliverable 实施；风险等级 L2，Review Budget 1 次。
- 实现范围：`src/utils/study-session.ts`（+`rateWord` 纯函数：评分+推进+完成原子化、幂等返回原会话防连点重复计分、不可变返回、当前位置校验抛错）、`src/utils/study-session.test.ts`（+6 项，115/115）、`src/pages/study/index.vue`（翻卡分支接真会话：20 词、视觉档名→`Rating` 用 P1-2 冻结映射 know=good/vague=hard/unknown=again；选择题分支 V0 Mock 原样）、`src/pages/study/complete.vue`（真实统计 + state 丢失空态 + 选择题 V0 原样）。
- 独立 L2 Feature Review：首轮 **REJECTED**——Critical off-by-one：`goNext` 用 `isLast = currentIndex === length - 1` 在评分推进后求值，末词（第 20 词）永不显示、会话永不 `completed`、完成页统计 19 词三处矛盾。实施者初验时误把「统计 19 词」当正常时序，被 Review 纠正。修复：改用 `isSessionFinished`（`currentIndex >= length`）判定 + `progressIndex` Math.min 钳制；浏览器复验通过（完成 20 词、0/20/0 统计如实、20 段全亮、`status=completed`、`completedAt` 有值）；聚焦条件复审 **APPROVED**（发现两项不阻塞：末词 450ms 反馈期无卡瞬态、评完立刻返回吞掉跳转——均属 P2-3 中断恢复范围）。
- 反证实验：3 项（去幂等→恰好 double-count 变红；推进 +2→步进测试变红；去位置校验→恰好位置测试变红）。
- 浏览器验证：20 词全流程、真实统计与点击一致、连点三层守卫（选中标记+disabled+幂等）、队列条段色、选择题回归（abandon 1/6 四选项）、375px 视口、控制台零错误。
- 当前验证：`pnpm check` 退出码 0（TypeScript、ESLint、115/115 Node 测试、production build）；diff 审计恰 4 个文件（2 纯函数+测试、2 页面），零越界文件。
- 当前结论：P2-2 为 `[x]`。翻卡模式为真会话；已知过渡态见下方风险清单。

### G. P2-3 最小会话恢复已完成

- 任务来源：2026-09-04/05 用户确认五项决策 D1–D5（新建 `composables/useStudySession.ts` 不建 Pinia store、静默恢复不加弹窗、保留 `status: 'completed'` 会话且完成页优先从存储读、只处理与恢复相关的瞬态、恢复匹配取宽松规则——queue 每个 id 存在于当前词库即恢复，词库版本变更处置留 P6）后作为独立 coherent deliverable 实施；风险等级 L2，实际未触发 L3 升级条件（选择题零存储交互、存储协议零改动）。
- 实现范围：`src/utils/session-recovery.ts`（`planSessionRecovery`：读取失败→内存新建零写盘；可恢复→原样返回会话；否则新建并经 `withActiveSession` 落盘。`withActiveSession`：不可变只改 `data.activeSession` 与 `savedAt`，scheduler/settings/progress 原样透传）、`src/composables/useStudySession.ts`（`readPersistedSession` 只读不写；`useStudySession` 薄胶水：恢复、评分即落盘、写失败静默降级内存会话不阻塞学习）、`src/pages/study/index.vue` 翻卡分支接 composable（`watch(isQuiz, immediate)` 触发恢复——比计划的 onMounted 多覆盖 quiz→flashcard 同组件切换路径；恢复时重置 `isRevealed`/`selectedRating`）、`src/pages/study/complete.vue` 数据源三级（存储中 completed 会话→路由 state→空态，第一级只收 completed 修复完成页刷新空态）、`src/services/storage.test.ts` 末尾追加 2 项（round-trip + 失败路径零写入），共 3 新增 + 3 修改 + 自动生成 `auto-imports.d.ts`。
- 硬约束遵守：全程走 `loadState`/`saveState` 未直写 `localStorage`；未开 `pinia-plugin-persistedstate`；未改 `STORAGE_KEY`、`src/services/storage.ts`、`eslint.config.ts`、选择题分支与任何样式文件。
- 独立 L2 Feature Review（独立 agent，首轮 APPROVED WITH CONDITIONS，Critical 0、Important 2、Minor 2）：I-1 `status === 'active'` 检查未被测试独立锁定（原测试的 completed 数据同时被 `isSessionFinished` 兜住，P1-3 校验放行 completed+索引居中的组合）——已补独立用例 + E8 反证（删检查恰该用例变红）修复，聚焦复审 APPROVED；I-2 交接文档 bundle 基线过期——已随本节收口改写（见第 5 节）；M-1 `advanceTimer` 跨模式泄漏（P2-2 遗留反馈期机制）归 P3；M-2 计划反证预期表两处与实际不符（E5 不红现有读取侧测试、E4 多红测试 12）已记入计划文档执行记录。
- 反证实验：8 项（E1–E7 按计划 + E8 Review 条件修复新增）全部先破坏实现、确认指定测试变红、再还原。
- 浏览器验证：10 项全过——第 7 词刷新恢复位置/进度 7/20/队列条着色、恢复后卡片未翻开、继续学完 20 词统计与点击一致且 `status=completed`、完成页刷新统计仍在、评完立刻返回再进入停正确下一词、再学一遍新会话 1/20、选择题全程存储零写入、翻卡↔选择题来回切换会话保位不污染、375px 无溢出零控制台错误、`localStorage` 阻断下 20 词全流程靠路由 state 兜底、future-version 损坏数据 `{"schemaVersion":999}` 学习正常且刷新后不被覆盖。
- 当前验证：`pnpm check` 退出码 0（TypeScript、ESLint、130/130 Node 测试、production build）；diff 审计恰 6 个文件 + 自动生成 `auto-imports.d.ts`；`git diff --check` 通过。
- 已知偏差（计划预判错误，非实现错误）：production bundle 的 `useStudySession` chunk 含 ts-fsrs 的 `generatorParameters` 与 Rating 枚举片段——「必须走 `loadState`」+ `storage.ts → utils/fsrs.ts`（P1-3 冻结的值依赖）的结构性后果，消除它需改 `storage.ts`（本任务明令不许碰）；FSRS 调度本体（`forgetting_curve`/`next_states` 等）未进 bundle，P4-1 前不得视为 FSRS 已接线。
- 当前结论：P2-3 为 `[x]`。翻卡会话已持久化、刷新静默恢复到正确位置（验收标准达成）；遗留边界见第 4 节 P2-4 起点事实。

## 3. 已验证基线

- P0-1～P0-4 已完成：类型检查、Lint、Git 基线和聚合质量命令已经建立。
- P1-1 已完成：词库、复习、学习会话、设置和备份领域类型及契约测试已建立。
- 设计 token 与 Vant 主题映射已经建立；`/tokens` 是设计验收页。
- 正常完整质量门禁为 `pnpm check`，依次运行类型检查、Lint、Node 测试和生产构建。
- 2026-09-01 治理 checkpoint：Markdown 代码围栏、默认入口链接、陈旧状态扫描、`git diff --check`、禁止新增治理文件和 Git 状态计数检查通过；本次只改文档，未重新运行项目代码质量门禁。
- 2026-09-02 文档生命周期规则以独立 commit `d6ecb58` 纳入 `main`。
- 2026-09-03 P1-2 补做独立 L3 Review 并修复 6 项 Important；最终 integration tree 通过完整 `pnpm check`、工程规范扫描、V0 范围审计和 `git diff --check`。
- 2026-09-03 会话结束前在 `main@6230e1f` 复跑 `pnpm check`：退出码 `0`，TypeScript、ESLint、22/22 Node 测试和 production build 全部通过，且未产生任何工作区副作用（含 `src/types/*.d.ts`）。源码中无 TODO / FIXME / HACK；唯一的 `@ts-ignore` 位于自动生成的 `src/types/auto-imports.d.ts`，不得手改。
- 2026-09-03 后续会话完成 P1-3 存储协议：Review 修复后 `pnpm check` 退出码 `0`，TypeScript、ESLint、92/92 Node 测试（其中存储协议 70 项）和 production build 全部通过；6 项反证实验（E1–E6）证明关键测试能检测目标失败模式；`dist/assets/` 无 `ts-fsrs` 引用。
- 2026-09-04 会话完成 P1-4 模块接口冻结：启用 `allowImportingTsExtensions`（删除 9 处 import expect-error，兼容性证据为恰 9 处 TS2578）、ESLint 六层方向守卫 + ts-fsrs 白名单、`MAX_TIMESTAMP` 迁移、Node 基线断言收紧、负值用例补齐；14 项反证实验全部变红后还原；最终 `pnpm check` 退出码 `0`（93/93 Node 测试）；独立 L3 Review 结论 APPROVED（Critical 0、Important 0、Minor 3，Minor 1 已修复验证）。已 commit（`908d39d` feat + `3374269` docs）并推送 `origin/main`。
- 2026-09-04 后续会话完成 P2-1 模拟词库与队列生成：3 个新文件（20 词词库 + 队列四纯函数 + 16 项契约测试），零已有文件改动；3 项反证实验；最终 `pnpm check` 退出码 `0`（109/109 Node 测试）；L1 Fast Path 无独立 Review。
- 2026-09-04 再次会话完成 P2-2 翻卡自评最小流程：`rateWord` 纯函数 + 学习页翻卡真会话 + 完成页真实统计；独立 L2 Review 首轮 REJECTED（Critical off-by-one 末词永不显示）→ 修复后浏览器复验 + 条件复审 APPROVED；最终 `pnpm check` 退出码 `0`（115/115 Node 测试）。
- 2026-09-05 会话完成 P2-3 最小会话恢复：`session-recovery` 纯函数 + `useStudySession` composable + 学习页/完成页接线（3 新增 + 3 修改文件）；8 项反证实验全部变红后还原；浏览器 10 项验证全过（含 future-version 数据不被覆盖的 P1-3 不变量现场证明、localStorage 阻断降级）；独立 L2 Review 首轮 APPROVED WITH CONDITIONS（Critical 0、Important 2、Minor 2）→ I-1 补测试修复后聚焦复审 APPROVED；最终 `pnpm check` 退出码 `0`（130/130 Node 测试），diff 审计零越界。

历史验证详情只在追溯时阅读 `P0交接文档.md` 和 `P1交接文档.md`，不要把历史结果自动当成当前工作区的通过证明。

## 4. 当前决策点与下一步

路线图上的下一工程任务是 **P2-4 垂直闭环审查**（L2 Feature/Batch Checkpoint），仍为未开始状态，必须作为新的 coherent deliverable 单独领取；本次会话到 P2-3 收口 checkpoint 为止。

V0 UI 已通过验收，但它只冻结产品结构、视觉方向和 Mock 点击流程，不代表 P2 或 P3 已完成。P1-2 只冻结 FSRS 依赖、评分/状态/时间、参数和序列化边界，不代表 P4-1 或正式业务 Feature 已完成。P1-3 只冻结存储协议与纯函数边界，不代表 P5-1 正式接线、P5-2 真实迁移、P5-3 备份导出导入或 P5-4 恢复 UI 已完成。P1-4 只冻结调用方向与守卫，不代表任何业务模块已实现。P2-1 只交付队列纯函数与模拟词库。P2-2 只交付翻卡真会话流程。P2-3 只交付翻卡会话的持久化与刷新恢复；选择题仍是 V0 Mock，均不代表完整学习闭环。

### 待用户决定（本轮刻意未做）

（P2-3 已于 2026-09-05 完成实现、Review 与文档收口，形成 feat 与 docs 两个 commit；推送需经 `127.0.0.1:10809` 代理，推送前须用户确认，本快照不预设其已完成。）

### P2-4 起点事实

- 任务内容（`项目实施计划.md` 第五节）：垂直闭环审查，验收为「确认状态边界可支持后续选择题和 FSRS，不需要推翻重写」；风险等级 L2；推荐独立审查 Agent。
- P2-3 交付后的已知边界即 P2-4 的审查输入：①末词 450ms 反馈期无卡瞬态与 `advanceTimer` 跨模式泄漏（翻卡反馈期内切 `?mode=multiple-choice`，timer 触发时 `goNext` 走 quiz 分支导致选择题跳题——同属反馈期机制，P2-3 Review 定性为 P3 处理，P2-4 审查时确认该决策）；②选择题仍 V0 Mock（评分→`Rating` 映射未冻结，属 P4-3/P4-4，P2-3 已机械保证其零存储交互）；③`?mode` 切换复用组件不重置翻卡会话（P2-3 用 `watch(isQuiz, immediate)` 覆盖了 quiz→flashcard 的 null 会话路径，保位语义经浏览器验证）；④首页 6 词文案与 20 词词库不一致（P3-3 消除）。
- 存储对接现状：`PersistedEnvelope.activeSession` 已被 P2-3 实际使用（翻卡会话落盘/恢复/完成页读取）；`progress` 字段仍只透传不写入（P4-1/P5-1 接线）；`results.ratedAt` 等运行时字段以 `storage.test.ts` 现有断言为准。
- 教训沿用：新契约测试做反证实验；组件级行为（恢复位置、跨模式切换）必须浏览器实测。P2-3 新增的教训——Review 优先攻击「测试是否锁住它声称锁住的契约」（I-1：completed+索引居中的组合此前无独立锁定，测试数据被其他闸门兜住造成虚假覆盖）。

## 5. 已知风险与禁止事项

- `main` 的 V0 checkpoint 已由用户确认；后续任务不得回写、重做或顺手扩展这批视觉验收内容。
- 构建后若 `src/types/*.d.ts` 只出现自动生成的换行副作用，应先核对语义内容再精确恢复；不要手工编辑生成文件。
- P1-2 已集成；不要重新实现同一 FSRS 边界，也不要把它误写成正式调度 Service 或页面接线。
- P1-3 已集成；不要重新实现存储协议，不要绕过 `loadState` / `saveState` 直写 `localStorage`，也不要给业务 store 开启 `pinia-plugin-persistedstate`（业务数据无版本化、无迁移、无损坏恢复，与协议目标冲突；该插件仅限 UI 噪声状态）。
- `STORAGE_KEY`（`cet6-vocab:state`）已由测试冻结：重命名会孤儿化用户已有数据，必须走 schema 迁移而不是改常量。
- 选择题答对/答错到正式 `Rating` 的转换规则尚未冻结，属于 P4-3/P4-4；不得从 P1-2 的三档翻卡映射自行推导。
- 原 `codex/p1-fsrs-strategy` branch 已于 2026-09-03 经用户确认删除（删除前指向 `986f1f4`，reflog 短期内仍可找回）；其 worktree 更早前已清理，`.worktrees/` 现为空。删除意味着早期 Review 结论不再有任何书面载体，它本来也不再作为 P1-2 的验证依据；该 branch 中的项目级文档自始至终未覆盖当前 `main`。
- 独立 Review 的 8 项 Minor：2 项已随 `c8dd7e3` 解决（永真的 `Date` 字符串断言、文档与 Review 结论冲突）；`FsrsSchedulingState.learningSteps`（当前步骤索引）与 `FsrsParametersSnapshot.learningSteps`（步骤配置）同名不同义，用户已决定保留现状，读代码时注意区分。
- P1-4 已集成；不要绕过方向守卫（静态绕过只有动态 `import()` 一条已知边界，见 `P1-4 模块接口冻结.md` 第四节），不要再给 `.ts` 扩展导入加回 `@ts-expect-error`，也不要在 `utils/fsrs.*` 之外 import `ts-fsrs`。原 P1-2 遗留 Minor 已随 P1-4 处置（`@ts-expect-error` 清理、Node 基线断言、`MAX_TIMESTAMP` 归位、ts-fsrs 守卫、负值用例）。
- **bundle 基线（P2-3 起更新）**：production 产物的 `useStudySession` chunk 含 ts-fsrs 的 `generatorParameters` 与 Rating 枚举片段，来源是 `storage.ts → utils/fsrs.ts` 的 P1-3 冻结值依赖（`loadState` fresh 路径必需），属结构性结果而非 FSRS 接线；FSRS 调度本体（`forgetting_curve`/`next_states` 等）未进 bundle，P4-1 前不要把该片段当作调度已接线或需要「修复」的回归（P2-3 Review I-2 定性）。
- 构建仍有 `.env` 中 `NODE_ENV` 与 PostCSS `from` 两项非阻断警告，不在 P1-2 范围内。
- 真实 5651 词词库不在仓库且有版权边界；旧项目 `E:\everlasting\项目\六级复习工具` 只读。
- 项目必须留在 NTFS 盘；不要搬回 E 盘。
- 文档与 Git/Code/Tests 冲突时，先明确报告冲突并更新本文件，不能从聊天记录推断。

## 6. 新会话最小恢复顺序

```text
AGENTS.md
→ 交接文档.md
→ git status --short --branch
→ git status --porcelain -uall
→ git diff --stat / git diff / git diff --cached
→ 必要时 git worktree list
→ 选择一个 coherent deliverable，确认 Scope / Non-goals
→ 读取对应任务小节、spec、plan、相关代码和测试
→ 开始工作
```

只有信息不足、状态冲突或找不到入口时，才扩大到其他源码、完整计划或近期 Git 历史。

## 7. 常用命令

```powershell
pnpm install
pnpm dev       # 端口以终端实际输出为准
pnpm check
```

## 8. 按需文档索引

- `项目实施计划.md`：P0～P8 路线图、依赖和 Acceptance Criteria。
- `模型与Agent协作指南.md`：L0～L3、Agent、Review、checkpoint；复杂任务时按需阅读。
- `工程规范.md`：目录归位、组件复用、工程扫描。
- `设计系统.md`：已确认视觉方向和 token。
- `技术选型.md`：技术栈和 Vant 主题机制。
- `六级词汇工具-形式确认.md`：已确认产品形式。
- `P1领域模型设计.md`：P1-1 领域契约。
- `P1-2 FSRS实现策略.md`：P1-2 算法和序列化边界。
- `P1-3 存储协议实现策略.md`：P1-3 存储键、版本语义、读写行为矩阵、迁移机制和 P5 交接约束。
- `P1-4 模块接口冻结.md`：P1-4 六层调用方向矩阵、ts-fsrs 白名单、守卫实现与已知边界、P2/P4/P5 交接约束。
- `P2-3 会话恢复实施计划.md`：P2-3 的决策 D1–D5、分层设计、反证与浏览器验证清单、Review 重点与执行记录。
- `docs/superpowers/plans/2026-09-02-p1-2-integration-finalization.md`：本次选择性集成、范围审计和最终验证记录。

不创建 `CURRENT_TASK.md`、`PROJECT_STATE.md` 或 `PROJECT_MAP.md`；本文件就是唯一状态接力棒。
