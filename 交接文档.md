# 交接文档 — 当前项目状态

> 这是新 Codex 会话的唯一当前状态入口，只记录“现在什么是真的”。
> 最后核对：2026-09-03；主工作区：`main`，P1-2 integration checkpoint 以包含本文件的 commit 为准。
> 开始工作前必须重新运行本文的 Git 检查，不能把本快照当成永久事实。

## 1. 项目一句话

移动端 H5 六级词汇背记工具，单人使用、无后端、不打包 App；目标能力包括翻卡自评、选择题辨义、FSRS、本地持久化、JSON 备份、TTS、真实词库和 PWA。

长期工程硬规则见 `AGENTS.md`；路线图和 Acceptance Criteria 见 `项目实施计划.md`；产品、设计和工程细节按本文末尾索引定向阅读。

## 2. 当前仓库快照

V0 UI 与 P1-2 FSRS 策略均已在 `main` 收口；P1-3 存储协议尚未开始。V0 仍是 Mock 体验，P1-2 仍是算法边界与契约测试，二者都不代表正式业务接线已经完成。

### A. `main`：V0 纯前端可视化验收版

- Git：V0 已纳入 `main@0fc6a24` checkpoint；后续 P1-2 集成没有修改其页面、组件、样式、Router、Store 或 Mock 数据。
- 内容：今日、学习、完成、进度、设置、404、TabBar、共享 UI/word 组件和 6 个 Mock 单词；首页学习方式改为两个直接入口，学习页评分和答题使用短暂原位反馈后自动推进。
- 未接入正式 Store、Session、FSRS、持久化、真实词库或 TTS；V0 评分和答案不会保存。
- Scope：只用于验收信息架构、视觉方向、基础点击流程、状态样式和响应式布局。
- Non-goals：正式学习计分、状态恢复、算法、存储协议、真实统计、真实词库、TTS、PWA 更新和后端。
- 任务文档：
  - `docs/superpowers/specs/2026-09-01-frontend-experience-design.md`
  - `docs/superpowers/plans/2026-09-01-v0-visual-implementation.md`
  - `PRODUCT.md`
- 验证状态：2026-09-01 当前 `pnpm check` 通过，包含类型检查、Lint、8/8 Node 测试和生产构建；浏览器验证覆盖两种学习模式连续完成、正确/错误反馈、首页直接入口和 320、375、430、600px 视口，控制台无阻断错误。
- 当前结论：用户已明确确认 V0 版本没有问题并授权形成 Git checkpoint；V0 到此收口，不再继续视觉打磨或扩展正式业务逻辑。

### B. `main`：P1-2 FSRS 策略已集成

- 历史来源：原 feature branch `codex/p1-fsrs-strategy@986f1f4` 已完成 Implementation、Verification 和 Task 6 文档收口，Git 有对应 commit 证据；但该 branch 没有留下任何独立 Review 报告，本文件早先出现过的 `APPROVED`（Critical 0、Important 0、Minor 3）无证据支撑，已作废。
- 独立 L3 Review：2026-09-03 在本会话对最终 integration tree 补做，结论 `APPROVED WITH CONDITIONS`，Critical 0、Important 6、Minor 8。6 项 Important 集中在「测试没有锁住它声称锁住的契约」和类型精度不足，已在 `c8dd7e3` 全部修复，并用 6 个反证实验逐项证明修复后的测试真的能检测目标失败模式（丢字段、篡改 `stability`、权重漂移、项目参数被改动、版本常量说谎、签名被放宽）。
- 集成方式：先以 `d6ecb58` 独立保存项目级文档真相源规则，再从该最新 `main` 建立 `codex/p1-2-integration-finalization`；只选择性 cherry-pick 6 个纯实现/测试 commit，不直接引入 `986f1f4` 的旧项目级文档快照。
- 集成 commit：`aea4013`、`6755ce8`、`b572835`、`9bd0583`、`d1c1a87`、`53731d8`，Review 修复为 `c8dd7e3`；文档收口位于包含本节的后续 checkpoint。
- 实现范围：`package.json`、`pnpm-lock.yaml`、`src/constants/fsrs.ts`、`src/types/fsrs.ts`、`src/utils/fsrs.ts`、`src/utils/fsrs.test.ts`；精确依赖 `ts-fsrs@5.4.1`，共 6 个实现文件、417 行新增。
- 当前验证：2026-09-03 最终 integration tree 的 `pnpm check` 退出码为 `0`，包含 TypeScript、ESLint、22/22 Node 测试和 Vite `8.2.0` production build；工程规范扫描、V0 范围审计和 `git diff --check` 另行通过；production 产物不含 `ts-fsrs`，证实 FSRS 模块尚未接线。
- 当前结论：P1-2 为 `[x]`，已进入最新 `main`。P1-3 仍为 `[ ]`；本轮没有开始 P1-3，也没有接入正式 Store、Session、FSRS Service、页面或持久化。

## 3. 已验证基线

- P0-1～P0-4 已完成：类型检查、Lint、Git 基线和聚合质量命令已经建立。
- P1-1 已完成：词库、复习、学习会话、设置和备份领域类型及契约测试已建立。
- 设计 token 与 Vant 主题映射已经建立；`/tokens` 是设计验收页。
- 正常完整质量门禁为 `pnpm check`，依次运行类型检查、Lint、Node 测试和生产构建。
- 2026-09-01 治理 checkpoint：Markdown 代码围栏、默认入口链接、陈旧状态扫描、`git diff --check`、禁止新增治理文件和 Git 状态计数检查通过；本次只改文档，未重新运行项目代码质量门禁。
- 2026-09-02 文档生命周期规则以独立 commit `d6ecb58` 纳入 `main`。
- 2026-09-03 P1-2 补做独立 L3 Review 并修复 6 项 Important；最终 integration tree 通过完整 `pnpm check`、工程规范扫描、V0 范围审计和 `git diff --check`。

历史验证详情只在追溯时阅读 `P0交接文档.md` 和 `P1交接文档.md`，不要把历史结果自动当成当前工作区的通过证明。

## 4. 当前决策点与下一步

路线图上的下一工程任务是 **P1-3 存储协议**，但它仍为未开始状态，必须作为新的 coherent deliverable 单独领取；本次会话到 P1-2 集成 checkpoint 为止。

V0 UI 已通过验收，但它只冻结产品结构、视觉方向和 Mock 点击流程，不代表 P2 或 P3 已完成。P1-2 只冻结 FSRS 依赖、评分/状态/时间、参数和序列化边界，不代表 P4-1 或正式业务 Feature 已完成。

## 5. 已知风险与禁止事项

- `main` 的 V0 checkpoint 已由用户确认；后续任务不得回写、重做或顺手扩展这批视觉验收内容。
- 构建后若 `src/types/*.d.ts` 只出现自动生成的换行副作用，应先核对语义内容再精确恢复；不要手工编辑生成文件。
- P1-2 已集成；不要重新实现同一 FSRS 边界，也不要把它误写成正式调度 Service 或页面接线。
- 选择题答对/答错到正式 `Rating` 的转换规则尚未冻结，属于 P4-3/P4-4；不得从 P1-2 的三档翻卡映射自行推导。
- 原 `codex/p1-fsrs-strategy` branch/worktree 仅保留为历史证据；其中项目级文档不得覆盖当前 `main`。
- 独立 Review 的 8 项 Minor：2 项已随 `c8dd7e3` 解决（永真的 `Date` 字符串断言、文档与 Review 结论冲突）；`FsrsSchedulingState.learningSteps`（当前步骤索引）与 `FsrsParametersSnapshot.learningSteps`（步骤配置）同名不同义，用户已决定保留现状，读代码时注意区分。
- 未处理的 5 项 Minor，P1-3/P4-1 接线前再评估：3 处 `@ts-expect-error` 挂在 import 语句上，会连带吞掉未来的导出名错误；Node 基线测试只验 `>= 20`（库下限）而非项目声明的 `>= 22.23.0`；`MAX_TIMESTAMP` 是 `Timestamp` 的领域边界却放在 FSRS 常量文件里；`dateToTimestamp` 的负值拒绝路径无用例；`P1-2 FSRS实现策略.md` 中「UI、Store 和会话状态机不得直接 import `ts-fsrs` `Rating`」缺 ESLint 守卫。
- 当前应用 bundle 未接线 FSRS 模块，本次 production 产物不含 `ts-fsrs`；不要把它当作 P4-1 的最终增量。
- 构建仍有 `.env` 中 `NODE_ENV` 与 PostCSS `from` 两项非阻断警告，不在 P1-2 范围内。
- 真实 5651 词词库不在仓库且有版权边界；旧项目 `E:\everlasting\项目\六级复习工具` 只读。
- 项目必须留在 NTFS 盘；不要搬回 E 盘。
- 文档与 Git/Code/Tests 冲突时，先明确报告冲突并更新本文件，不能从聊天记录推断。

## 6. 新会话最小恢复顺序

```text
AGENTS.md
→ 交接文档.md
→ git status --short --branch
→ git status --porcelain -uall
→ git diff --stat / git diff / git diff --cached
→ 必要时 git worktree list
→ 选择一个 coherent deliverable，确认 Scope / Non-goals
→ 读取对应任务小节、spec、plan、相关代码和测试
→ 开始工作
```

只有信息不足、状态冲突或找不到入口时，才扩大到其他源码、完整计划或近期 Git 历史。

## 7. 常用命令

```powershell
pnpm install
pnpm dev       # 端口以终端实际输出为准
pnpm check
```

## 8. 按需文档索引

- `项目实施计划.md`：P0～P8 路线图、依赖和 Acceptance Criteria。
- `模型与Agent协作指南.md`：L0～L3、Agent、Review、checkpoint；复杂任务时按需阅读。
- `工程规范.md`：目录归位、组件复用、工程扫描。
- `设计系统.md`：已确认视觉方向和 token。
- `技术选型.md`：技术栈和 Vant 主题机制。
- `六级词汇工具-形式确认.md`：已确认产品形式。
- `P1领域模型设计.md`：P1-1 领域契约。
- `P1-2 FSRS实现策略.md`：P1-2 算法和序列化边界。
- `docs/superpowers/plans/2026-09-02-p1-2-integration-finalization.md`：本次选择性集成、范围审计和最终验证记录。

不创建 `CURRENT_TASK.md`、`PROJECT_STATE.md` 或 `PROJECT_MAP.md`；本文件就是唯一状态接力棒。
