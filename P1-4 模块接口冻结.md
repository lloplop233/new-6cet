# P1-4 模块接口冻结

> 定位：P1-4 的决策记录、冻结契约和后续任务交接约束。属于历史性 / 专项性文档：文中「当前状态」类文字只描述本任务收口时的 checkpoint，不构成项目当前事实；项目当前状态以 `交接文档.md` 为准。
>
> 风险等级 L3。用户已于 2026-09-04 批准完整方案（调用方向矩阵、两议题定夺方向、MAX_TIMESTAMP 迁移、三项 Minor 修复），本文是这些决策的书面载体。

## 一、背景与目标

P1-3 集成后，`工程规范.md` 已冻结目录归位（哪个文件放哪），但**层与层之间谁能调用谁**没有书面契约，也没有机械约束。路线图上 P2 起的全部 L1/L2 任务都以「无需重新设计接口即可实施」为前提，P1-3 交接时留下两个待定夺议题：

1. `services → utils` 的依赖方向是否成立（P1-3 的 `services/storage.ts` 为构造默认 scheduler 元数据导入了 `utils/fsrs.ts`）。
2. `.ts` 扩展导入的 `@ts-expect-error` 惯例是否改为 tsconfig `allowImportingTsExtensions`。

另有交接文档点名的 P1-2 遗留 Minor（ts-fsrs 直 import 缺 ESLint 守卫、Node 基线断言过松、`dateToTimestamp` 负值路径无用例、`MAX_TIMESTAMP` 归属）约定在本任务一并处置。

本任务交付：**六层调用方向矩阵（书面 + ESLint 机械守卫）+ 两议题定夺 + 遗留 Minor 处置**。验收标准：后续 L1/L2 任务无需重新设计接口即可实施。

## 二、Scope 与 Non-goals

**Scope**

1. 本决策文档。
2. `tsconfig.json`：`noEmit: true`、`allowImportingTsExtensions: true`。
3. `eslint.config.ts`：`layerRule` helper + 9 个 override 块（ts-fsrs 全局兜底、六层方向守卫、`utils/fsrs` 白名单块）。
4. 删除 9 处挂在 import 语句上的 `@ts-expect-error`（`utils/fsrs.ts` 1 处、`utils/fsrs.test.ts` 2 处、`services/storage.ts` 3 处、`services/storage.test.ts` 3 处）。
5. `MAX_TIMESTAMP` 从 `constants/fsrs.ts` 迁移到新文件 `constants/timestamp.ts`（值不变，3 处 import 更新）。
6. Node 基线断言收紧、`dateToTimestamp` 负值拒绝路径测试用例（均在 `utils/fsrs.test.ts`）。

**Non-goals**

- 不实现任何新模块、Store、composable 或 Service（`composables/` 仍为空，守卫先于代码存在正是「冻结」的意义）。
- 不修改 V0 页面、组件、样式或 Mock 数据。
- 不修改 P1-2 / P1-3 已冻结的协议**语义**（`MAX_TIMESTAMP` 是纯位置移动，数值与行为不变）。
- 不修改 `工程规范.md`（其修改属 L3 且需用户单独授权；方向矩阵由本文承载）。
- 不引入 `eslint-plugin-import` 等新 ESLint 依赖（守卫只用核心规则 `no-restricted-imports`）。
- 不给 UI 层内部（pages → components、`word/` → `ui/`）加机械守卫；方向写入 D1 矩阵，由 `工程规范.md` 2.x 现有条款把守。

## 三、决策记录

### D1 六层调用方向矩阵

分层从低到高，**只许向下依赖**：

| 当前层 | 允许 import | 禁止 import |
|---|---|---|
| `types/` | `types/`；`constants/`（仅 type-only） | utils、services、stores、composables、pages、components、router |
| `constants/` | `types/`（仅 type-only） | 其余全部层的运行时代码 |
| `utils/` | types、constants、utils | services、stores、composables、pages、components、router；`vue`/`pinia`/`vant`/`@vue/*` |
| `services/` | types、constants、utils、services | stores、composables、pages、components、router |
| `stores/` | types、constants、utils、services、stores | composables、pages、components、router |
| `composables/` | types、constants、utils、services、stores、composables | pages、components、router |
| `components/` `pages/` `router/` | 以上全部（顶点） | — |

三条设计理由：

1. **`types` 与 `constants` 互相引用合法，但仅限 `import type`**。这不是妥协：`PersistedEnvelope.schemaVersion: typeof SCHEMA_VERSION`（P1-3 D2 冻结契约）与 `DEFAULT_SETTINGS satisfies Settings` 必然产生双向类型引用。运行时方向仍清晰：utils/services 运行时导入 constants，constants 永不运行时导入任何层。
2. **`composables → stores` 允许，反向禁止**。composable 内 `useXxxStore()` 是从小组合大；store 依赖 composable 会让全局单例依赖临时上下文，是反模式。
3. **`utils` 禁 `vue`/`pinia`/`vant`/`@vue/*`**：`工程规范.md` 1.3「文件里有没有 `import vue`」判定标准的机械化。

### D2 `services → utils` 依赖方向（定夺）

**成立，单向允许；反向由守卫锁死。**

- `utils` 按定义是零外部依赖纯函数，天然底层；`storage.ts → fsrs.ts` 取 `createFsrsParametersSnapshot` 组装默认信封是「数据访问层用纯函数组装数据」的正常形态。
- 若禁止，唯一出路是把该函数复制进 services 或移层——前者违反「第二次出现就封装」，后者破坏 P1-2 已冻结的 `utils/fsrs.ts` 归位（它 import `ts-fsrs` 又被 types/constants 类型引用，只能留在 utils）。

### D3 `.ts` 扩展导入惯例（定夺）

**启用 tsconfig `allowImportingTsExtensions`，删除 9 处 import 上的 `@ts-expect-error`；`.ts` 扩展导入本身保留。**

- 原 9 处 expect-error 会连带吞掉未来的导出名错误（TS2305/TS2724 被压制），反证实验 E-B1 证明删除后这类错误正常报出且带拼写建议。
- 启用条件满足：项目全部类型检查入口（`typecheck`、`build:dev/pro`）都是 `vue-tsc --noEmit`，tsc 从不发射产物；tsconfig 补 `noEmit: true` 后语义零变化。
- `.ts` 扩展导入必须保留：`pnpm test` 是 `node --test` 直跑 TS，Node 原生模块解析要求显式扩展名（P0 定下的测试架构）。
- 删除范围仅 9 处 import 标记；其余 19 处类型反证 expect-error（P1-1/P1-2/P1-3 的冻结手段）全部保留。
- 兼容性证据：启用后 `vue-tsc` 恰好对 9 处报 TS2578（Unused directive），一一对应、一个不多一个不少，证明其余 expect-error 不受影响。

### D4 `ts-fsrs` 白名单

`ts-fsrs` 全项目禁止导入，仅 `src/utils/fsrs.ts`（FSRS 适配层）与 `src/utils/fsrs.test.ts`（golden test 以官方库为基准）豁免。这是把 P1-2 书面契约「UI、Store 和会话状态机不得直接 import `ts-fsrs`」变成机械约束；业务代码一律经 `utils/fsrs.ts` 间接使用。

### D5 机械守卫实现

`eslint.config.ts` 用核心规则 `no-restricted-imports` + 9 个 override 块，零新依赖：

```
块 1  files: src/**/*.{ts,vue}（ignores: utils/fsrs 两文件）  → ts-fsrs 兜底（覆盖 pages/components/router 等无层块文件）
块 2  src/types/**       → D1 禁令 + ts-fsrs
块 3  src/constants/**   → D1 禁令 + ts-fsrs
块 4  src/utils/**       → D1 禁令 + vue/pinia/vant/@vue/* + ts-fsrs
块 5  src/utils/fsrs.ts、fsrs.test.ts → D1 禁令 + vue/pinia/vant/@vue/*（无 ts-fsrs 禁令，即白名单）
块 6  src/services/**    → D1 禁令 + ts-fsrs
块 7  src/stores/**      → D1 禁令 + ts-fsrs
块 8  src/composables/** → D1 禁令 + ts-fsrs
```

两个实现要点：

- **flat config 后块覆盖前块**：每个层块必须重复声明 ts-fsrs 禁令，否则该层块对 `no-restricted-imports` 的整体覆盖会把禁令一并抹掉；白名单块必须位于 utils 层块之后，才能在放行 ts-fsrs 的同时保留其余 utils 禁令（反证 E-A9 验证）。
- **patterns 形态**：每层禁令为 `**/<layer>` 与 `**/<layer>/**` 两种 glob，同时覆盖 `@/stores`（别名、指向 index）与 `../services/storage.ts`（相对路径）；外部包为 `[pkg, pkg/*]`。静态 `export ... from` 中转再导出同样被拦截（Review 探针实测）。

### D6 遗留 Minor 处置

| 项 | 处置 |
|---|---|
| ts-fsrs 直 import 缺 ESLint 守卫 | 已落地（D4/D5） |
| Node 基线断言只验 `>= 20` | 已收紧为 `major > 22 \|\| (major === 22 && minor >= 23)`，对齐 package.json `engines.node >= 22.23.0` |
| `dateToTimestamp` 负值拒绝路径无用例 | 已补：`new Date('1969-12-31T23:59:59.999Z')`（getTime = -1）断言抛 `RangeError` |
| `MAX_TIMESTAMP` 放在 `constants/fsrs.ts` | 已迁移到 `src/constants/timestamp.ts`（`Timestamp` 是 P1-1 领域边界而非 FSRS 算法边界；值不变，3 处 import 更新；反证 E-C1 验证旧路径编译报错） |
| `learningSteps` 同名不同义 | 维持用户既有决定（P1-2 Review 时已确认保留），不处理 |

## 四、已知边界（守卫不覆盖的场景）

1. **动态 `import()`**：`no-restricted-imports` 不检查动态导入表达式，`import('@/composables')` 可绕过方向守卫（Review 探针实测确认）。单人项目 + Review 把守，接受此边界。
2. **types/constants 互引的 type-only 约定无机械强制**：`no-restricted-imports` 不区分 `import type` 与值导入；「types 值导入 constants」静态守卫拦不住。`verbatimModuleSyntax: true` 已强制 type-only 必须显式写 `import type`，把最常见的笔误（把值导入写成普通 import 而实际只用类型）交给编译器。
3. **`@vueuse` 等第三方范围包未列入 utils 禁令**：工程规范只冻结了 `vue` 主包及其官方范围包 `@vue/*`；`@vueuse/core` 等若未来需要禁入 utils，另行追加。
4. **`composables/` 当前为空目录**：空目录无文件可匹配守卫，规则在该层形同虚设但无害；文件落地即生效，这正是「冻结先于实现」。

## 五、后续任务交接约束

1. **P2-1 起的所有新代码**按 D1 矩阵归位与引用；违规 import 会在 `pnpm lint` 变红，报错信息指向本文。
2. **新增目录**：顶层目录与 `components/` 子目录新增仍需用户点头（`工程规范.md` 1.1）；若新增层，必须同步在 `eslint.config.ts` 追加对应守卫块，否则该层处于无守卫状态。
3. **P4-1（调度 Service）**：调度实现放 `services/`，经 `utils/fsrs.ts` 适配层使用 `ts-fsrs`，不得直接 import（D4）。
4. **P5-1（接线）**：业务 store 调用 `loadState`/`saveState` 走 `stores → services` 方向（D1 已允许）；`pinia-plugin-persistedstate` 边界不变（P1-3 D7）。
5. **测试文件**：`*.test.ts` 与被测文件同层，遵守同层守卫；测试需要跨层构造数据时用被测文件的公开导出，不绕方向。

## 六、验收清单

1. `pnpm check` 退出码 0（TypeScript、ESLint、全部 Node 测试、production build）。
2. 调用方向矩阵书面化 + 机械守卫落地，每条禁令经反证实验验证。
3. 两议题定夺落地且有书面决策记录。
4. production 产物不含 `ts-fsrs`（本任务未给应用代码新增任何 ts-fsrs 导入，反而加了禁令）。
5. 独立 L3 Review 通过（Critical 0 / Important 0）。

## 七、验证记录（2026-09-04 实施会话回填）

- **兼容性快验**：tsconfig 启用 `allowImportingTsExtensions` 后、删除 expect-error 前，`vue-tsc` 恰好对 9 处报 TS2578（Unused directive）——开关生效的直接证据，且证明其余 expect-error 不受影响。
- **反证实验**（一次性注入、确认变红后还原，未进 commit）：
  - **E-A1** types 探针 import `../utils/round` → 拦截（方向违规）。
  - **E-A2** constants 探针 import `../services/storage.ts` → 拦截。
  - **E-A3** utils 探针 import `../services/storage.ts` → 拦截。
  - **E-A4** utils 探针 import `vue` → 拦截（层禁包）。
  - **E-A5** services 探针 import `../stores` → 拦截。
  - **E-A6** stores 探针 import `../composables/useQueue` → 拦截。
  - **E-A7** composables 探针 import `../pages/study/index.vue` → 拦截。
  - **E-A8** services 探针与 pages 下 `.vue` 探针 import `ts-fsrs` → 均拦截（证明 `.vue` 文件中规则同样生效）。
  - **E-A9** 白名单文件 `utils/fsrs.ts` 内注入 import `../services/storage.ts` → 拦截（白名单只放行 ts-fsrs，不放行方向违规）。
  - **E-A10** Review 阶段补充：`@/stores` 别名、`export { x } from '@/stores'` 中转、`export * from '../../utils/fsrs.ts'` 相对子路径、utils 层 `import 'vue'` → 全部拦截。
  - **E-B1** 拼错导出名 `createFsrsParametersSnapsho` → TS2724（附拼写建议）——正是原先被 expect-error 吞掉的错误类型。
  - **E-C1** 从旧路径 `constants/fsrs.ts` import `MAX_TIMESTAMP` → TS2305。
  - **E-D1** Review 修复后：utils 探针 import `@vue/shared` → 拦截（Minor 1 修复生效）。
- **最终验证**：`pnpm check` 退出码 0，TypeScript、ESLint、93/93 Node 测试（较 P1-3 的 92 新增 1 项负值用例）、Vite production build 全部通过；`dist/assets/` 无 `ts-fsrs` 引用；工程规范扫描（`--van-` 变量、`.van-` 选择器、`src/api` 不存在）通过；`git status` 改动范围与 Scope 完全一致（7 个修改 + 1 个新文件），无 `src/types/*.d.ts` 副作用。
- **独立 L3 Review**（2026-09-04，独立 Agent 审查）：结论 **APPROVED**，Critical 0、Important 0、Minor 3。
  - Minor 1：`@vue/*` 范围包绕过 utils 层包禁令（探针实测）。已修复（patterns 追加 `@vue/*`，E-D1 验证），非实质性修复，按 Review Budget 不触发条件复审。
  - Minor 2：动态 `import()` 绕过方向守卫。记入第四节已知边界。
  - Minor 3：types/constants 互引 type-only 无机械强制。记入第四节已知边界。
  - Reviewer 同时核查属实：MAX_TIMESTAMP 无旧路径遗漏、Node 断言逻辑对 22.23.0/23.0.0/22.22.9 判定正确、负值用例真实有效、tsconfig 与全部 scripts 兼容、flat config 覆盖顺序正确、dist 无 ts-fsrs。
