# P1 领域模型设计

> 日期：2026-08-30  
> 对应任务：`项目实施计划.md` P1-1  
> 状态：聊天设计已确认，等待书面审阅  
> 范围：只冻结项目自有领域类型，不选择 FSRS 库、不设计存储键、不实现业务流程

## 一、目标

为六级词汇背记工具建立稳定、可序列化、与第三方库解耦的领域类型，使 P1-2～P1-4 可以在不推翻业务语义的前提下继续设计 FSRS 适配、存储协议和模块接口。

本阶段定义：

- 词库中的学习单元是什么；
- 评分、复习状态和学习会话如何表达；
- 设置和备份的顶层数据形状；
- 时间、唯一标识、可选字段和 JSON 序列化规则；
- 后续阶段必须遵守的不变量。

本阶段不实现：

- FSRS 算法或第三方库适配；
- `localStorage`、迁移和损坏恢复；
- 队列生成、会话推进和评分副作用；
- 真实词库转换；
- Store、Service、Composable、页面或组件。

## 二、事实基础

旧项目 `E:\everlasting\项目\六级复习工具` 只读检查结果：

| 项目 | 数量 |
|---|---:|
| 原始词条记录 | 5651 |
| 规范化后唯一拼写 | 3991 |
| 重复拼写组 | 1424 |
| 重复产生的额外记录 | 1660 |
| 缺少音标 | 41 |
| 缺少词性 | 1 |
| 缺少中文释义 | 0 |
| rank 范围 | 1～2345 |

旧词库没有稳定 ID。同一拼写可能出现多次，并携带不同 rank、词性和释义。若把原始记录直接作为学习单元，同一单词会在队列中重复出现并产生多份 FSRS 进度。

## 三、核心决策

### 3.1 学习单元和稳定 ID

同一规范化拼写只对应一个 `Word` 和一份复习进度。

规范化算法固定为：

1. 对原始单词执行 Unicode `NFKC` 规范化；
2. 删除首尾空白；
3. 转为小写；
4. 保留内部空格、连字符和撇号，不做词干化或拼写替换。

规范化结果作为 `WordId`。例如：

```text
" Dignity " -> "dignity"
```

P6 转换真实词库时，重复记录按 `WordId` 合并：

- `rank` 取所有来源记录的最小值；
- 不同词性和释义合并到 `senses`，相同组合去重；
- 音标按 rank 升序选择第一条非空值；
- 例句去重后最多保留一条；
- 真题年份去重并排序。

不使用数组下标、rank 或随机 UUID 作为 `WordId`，因为这些值不能保证跨词库版本稳定。

### 3.2 项目类型与 FSRS 解耦

项目拥有自己的 `Rating`、`ReviewState` 和时间类型。页面、Store、备份和迁移不得直接引用第三方 FSRS 类型。

P1-2 负责：

- 选择成熟 FSRS 实现；
- 定义项目调度状态与第三方 Card 类型之间的适配；
- 补齐算法需要持久化的内部字段；
- 验证时间转换和序列化。

P1-1 不提前猜测 `stability`、`difficulty`、`scheduledDays` 等第三方字段。P1-4 完成后再冻结完整跨模块接口。

### 3.3 时间表示

所有跨模块或可持久化时间统一使用：

```ts
type Timestamp = number
```

语义固定为 UTC Unix epoch milliseconds。

不在领域对象中保存 `Date`、格式化日期字符串或隐式本地时区。创建当前时间属于外部副作用，必须由后续 Service 或调用方注入；纯函数不得直接调用 `Date.now()`。

所有 `Timestamp` 必须是大于等于 0 的有限安全整数。该约束由后续运行时边界校验负责，TypeScript 类型只负责拒绝明显的非数字输入。

## 四、类型文件与依赖方向

只新增以下手写 `.ts` 文件：

```text
src/types/word.ts
src/types/review.ts
src/types/study.ts
src/types/settings.ts
src/types/backup.ts
src/types/domain.test.ts
```

依赖方向：

```text
word.ts
  -> 无项目内类型依赖

review.ts
  -> word.ts

study.ts
  -> word.ts
  -> review.ts

settings.ts
  -> review.ts（Timestamp）
  -> study.ts（StudyMode）

backup.ts
  -> word.ts
  -> review.ts
  -> study.ts
  -> settings.ts
```

不得形成循环 import。5 个自动生成的 `.d.ts` 不得修改。

## 五、词库类型

### 5.1 `WordId`

```ts
type WordId = string
```

`WordId` 的值必须满足第 3.1 节的规范化规则。P1-1 不引入 branded type；运行时规范化函数和数据校验在 P6 实现。

### 5.2 `WordSense`

字段：

- `partOfSpeech: string | null`：词性允许缺失；
- `definition: string`：非空中文释义。

同一 `Word` 的 `senses` 至少包含一项。

### 5.3 `WordExample`

字段：

- `english: string`：非空英文例句；
- `chinese: string | null`：中文翻译允许缺失。

精简词库每个 `Word` 最多保留一条例句。

### 5.4 `Word`

字段：

- `id: WordId`；
- `text: string`：原始展示拼写；
- `rank: number`：正整数，越小越常见；
- `phonetic: string | null`；
- `senses: readonly WordSense[]`；
- `examples: readonly WordExample[]`；
- `realExamYears: readonly string[]`。

`Word` 及其嵌套集合全部使用 `readonly`，表达词库数据在运行时不可修改。

## 六、复习类型

### 6.1 `Timestamp`

`Timestamp` 在 `review.ts` 中导出，供其他领域类型复用。它只表示 UTC Unix epoch milliseconds。

### 6.2 `Rating`

```ts
type Rating = 'again' | 'hard' | 'good'
```

产品语义映射固定为：

```text
不熟 -> again
模糊 -> hard
认识 -> good
```

第三方 FSRS 等级映射只允许在 P1-2 的适配层中出现。

### 6.3 `ReviewPhase`

```ts
type ReviewPhase = 'new' | 'learning' | 'review' | 'relearning'
```

### 6.4 `ReviewState`

稳定业务字段：

- `wordId: WordId`；
- `phase: ReviewPhase`；
- `dueAt: Timestamp`；
- `lastReviewedAt: Timestamp | null`；
- `reviewCount: number`：大于等于 0 的整数；
- `lapseCount: number`：大于等于 0 且不大于 `reviewCount` 的整数。

FSRS 算法内部字段不进入本阶段的 `ReviewState`。P1-2 可以通过项目自有的组合类型补充调度状态，但不得改变这里已经冻结的字段语义。

## 七、学习会话类型

### 7.1 `StudyMode`

```ts
type StudyMode = 'flashcard' | 'multiple-choice'
```

### 7.2 `SessionStatus`

```ts
type SessionStatus = 'active' | 'completed'
```

### 7.3 `SessionResult`

字段：

- `wordId: WordId`；
- `rating: Rating`；
- `ratedAt: Timestamp`。

### 7.4 `StudySession`

字段：

- `id: string`：非空且在本机数据中唯一；
- `mode: StudyMode`；
- `queue: readonly WordId[]`；
- `currentIndex: number`；
- `results: Readonly<Record<WordId, SessionResult>>`；
- `status: SessionStatus`；
- `startedAt: Timestamp`；
- `updatedAt: Timestamp`；
- `completedAt: Timestamp | null`。

不变量：

1. `queue` 不得包含重复 `WordId`；
2. 活动会话的 `currentIndex` 必须位于 `0..queue.length - 1`；
3. 空队列不构成活动会话；
4. `results` 的键必须等于对应 `SessionResult.wordId`；
5. 一个 `WordId` 在一次会话中最多存在一条最终结果；
6. `completed` 会话必须有 `completedAt`，`active` 会话必须为 `null`；
7. `updatedAt` 不早于 `startedAt`；
8. 已评分结果和队列位置共同构成刷新恢复的事实来源。

类型只表达结构；队列去重、索引推进、快速连点和恢复校验在 P2 通过纯函数与测试实现。

## 八、设置类型

`Settings` 字段：

- `targetWordCount: number`：正整数且不超过可用唯一词数；
- `preferredMode: StudyMode`；
- `autoPronounce: boolean`；
- `updatedAt: Timestamp`。

默认目标词量 2500 属于后续 `constants/`，不写入类型定义。

本阶段不加入主题、账号、云同步、每日提醒、自定义 FSRS 参数、词根词缀或其他未确认设置。

## 九、备份类型

### 9.1 `BackupData`

字段：

- `settings: Settings`；
- `reviews: Readonly<Record<WordId, ReviewState>>`；
- `activeSession: StudySession | null`。

词库本体不进入备份，备份只引用稳定 `WordId`。

### 9.2 `BackupEnvelope`

字段：

- `schemaVersion: number`：正整数，具体初始值由 P1-3 决定；
- `appVersion: string`：导出时的应用版本；
- `exportedAt: Timestamp`；
- `data: BackupData`。

所有字段必须能由原生 `JSON.stringify` 和 `JSON.parse` 往返，不允许出现 `Date`、`Map`、`Set`、函数或类实例。

存储键、迁移链、导入校验、旧数据保护和损坏恢复由 P1-3、P5 决定。

## 十、验证策略

P1-1 使用编译期契约测试与最小运行时 JSON 测试：

1. 先新增 `src/types/domain.test.ts`，引用尚不存在的领域类型；
2. 运行 `pnpm typecheck`，确认因目标类型缺失而失败；
3. 新增最小类型定义；
4. 使用 `satisfies` 验证合法领域对象；
5. 使用 `@ts-expect-error` 验证非法 `Rating`、`Date` 时间和缺失必要字段会被 TypeScript 拒绝；
6. 使用 Node 内置测试验证完整 `BackupEnvelope` 经 JSON 往返后值不变；
7. 运行 `pnpm check`；
8. 执行工程规范扫描；
9. 人工检查没有修改自动生成的 `.d.ts`、UI、Store、Service 或设计 token。

## 十一、P1-1 完成标准

以下条件全部满足后，P1-1 才能标记为 `[x]`：

- 六个目标文件按本设计落地；
- 字段语义、可选性、时间单位和唯一标识明确；
- 项目领域类型不依赖第三方 FSRS 类型；
- `pnpm typecheck`、`pnpm lint`、`pnpm test` 和生产构建通过；
- JSON 往返测试通过；
- 工程规范扫描通过；
- 更新 `项目实施计划.md`、`交接文档.md` 和 P1 交接记录；
- 交付说明列出修改文件、验证结果、关键决策和剩余风险。

## 十二、后续阶段边界

- P1-2：选择 FSRS 实现，定义项目调度状态和适配层；
- P1-3：定义存储键、`schemaVersion`、迁移和异常恢复；
- P1-4：冻结 Store、Service、Composable 和工具函数接口；
- P2：用 20 个模拟词实现队列、评分、刷新恢复和完成状态；
- P6：读取旧项目生成词表，执行规范化、去重、字段合并和分片。

任何下游任务发现本设计存在系统性错误时，应回退到 P1 修正，不在页面或 Store 中增加补丁掩盖。
